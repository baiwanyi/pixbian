# 2026-09-07

## Short 页第二轮调整（进度条平滑 / 按钮精修 / 更多菜单与删除）
- 需求（方案经用户确认）：①进度条平滑 + 前景 60% 白；②悬停底色改 10% 白、去 tooltip；③新增「更多」菜单（\uE712）承载【查看原视频】与【删除 DEL】，DEL 键同样生效；删除移入回收站并自动播放下一个。
- 进度条平滑：根因是 250ms 轮询一跳。方案 = 轮询间隔不变，页面侧对 `ProgressFill.Value` 做 250ms 线性补间（`DoubleAnimation` + `EnableDependentAnimation=true`，每轮现场建 Storyboard、显式 From、新 Begin 替换旧动画无 Completed 清理）；`ShortViewModel.ProgressInterval` 改 public 供页面对齐时长；`x:Bind Value` 移除（绑定赋值会打断动画），改 code-behind 订阅 `ViewModel.PropertyChanged`（值不变不触发，生成属性带相等检查）。
- 删除设计（关键时序）：「先切走再后台删」——正在播放的文件被 MediaPlayer 持有句柄，立即删必失败；同步 Dispose 旧源又与引擎卸载竞态（已实测卡 UI ~6s）。`DeleteCurrent()`：停轮询/音乐 → `Pause` + `Source=null` 脱离 → 候选池 `RemoveAt(游标)`（防自然轮转命中已删文件，池尾越界时重洗落首、删空置 HasError「候选池已空」）→ fire `DeleteInBackgroundAsync`（后台串行：`Task.Delay(StaleReleaseDelay)` → `stale.Dispose()` → `Task.Run(RecycleBinHelper.SendToRecycleBin)` → `DeleteByPathsAsync` 清索引，失败仅 `SHORTDEL|*` 留痕）→ `_ = PlayCurrentAsync()` 直接起播当前索引（原下一条已前移）。**注意 `PlayCurrentAsync` 的取条目在 try 之外，索引必须先保证合法再调用**。
- XAML：进度条 `Foreground=#99FFFFFF`；hover/pressed 资源键 `#33FFFFFF`→`#1AFFFFFF`；五个按钮全部去 `ToolTipService.ToolTip`；查看原视频按钮移入 MenuFlyout（内联 `Button.Flyout` 为专属实例，非共享单例，无重复弹出 E_INVALIDARG 之虞）；删除菜单项挂 `KeyboardAccelerator(Key=Delete)` + `KeyboardAcceleratorTextOverride="DEL"`——主判定在页面 KeyDown（菜单打开时焦点在弹出层、页面收不到，无双触发），菜单项 accelerator 仅为显示文本。
- `ShortPage.xaml.cs`：订阅/退订 ViewModel.PropertyChanged（Dispose 退订）；`AnimateProgressTo`；`OnDeleteClick` 转发；KeyDown 加 `VirtualKey.Delete` 分支。
- 坑：终端 here-string 传中文多行命令报「字符串缺少终止符」→ 日志追加改用编辑工具。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误；lint 无诊断。运行观感（平滑度、透明度、菜单交互、删除续播）待用户实测。
- 涉及文件：`src/Pixbian/Views/ShortPage.xaml`、`src/Pixbian/Views/ShortPage.xaml.cs`、`src/Pixbian/ViewModels/ShortViewModel.cs`。

## Short 页第三轮微调（用户反馈）
- 四项：①「更多」按钮移到竖列首位（更多/静音/收藏/下一个）；②悬停/按下底色改 10% 黑（#1A000000）；③已收藏爱心 60% 红——`ShortViewModel` 加 `FavoriteActiveBrush/InactiveBrush`（0x99 红/白静态画刷，沿用 MediaItemViewModel 模式）+ `FavoriteBrush` 属性，`OnIsFavoriteChanged` 同步通知，FontIcon 显式 `Foreground` 绑定（显式值不受 Button VisualState Foreground 影响，hover 不再提亮属预期）；④进度条去圆角——页面级覆盖主题键 `ProgressBarCornerRadius=0`（默认 1.5，包内 generic.xaml 2.3.6 已核实模板 StaticResource 引用，直接键覆盖生效、无需重写模板）。
- 涉及文件：`src/Pixbian/Views/ShortPage.xaml`、`src/Pixbian/ViewModels/ShortViewModel.cs`。构建 0 警告 0 错误。

## Short 页第四轮微调（用户反馈）
- 两项：①「更多」按钮移到右上角（Margin 0,24,24,0），竖列恢复三按钮（静音/收藏/下一个）；②进度条前景改 50% 透明系统蓝 `#800078D4`（写死 Windows 默认蓝，与用户主题强调色解耦——与页面「舞台恒暗写死色值」既有口径一致）。
- 涉及文件：`src/Pixbian/Views/ShortPage.xaml`。构建 0 警告 0 错误。

## Short 页第五轮：暂停中央播放按钮 + 菜单精修（用户确认方案后实施）
- ①暂停时画面正中央显示 64px 圆形播放按钮（\uE768）：复用 ShortOverlayButtonStyle 本地值覆盖尺寸/圆角，悬停沿用页面级资源键，Tapped 就地拦截；点击转发 `TogglePlayPause()`。显隐条件 `ShortViewModel.IsPlayOverlayVisible`（已载入 && !IsPlaying && !IsLoading && !HasError），在四个状态属性各自的 `OnXxxChanged` partial 中通知——x:Bind 函数绑定不可用时的计算属性模式。
- ②更多菜单：两项间加 `MenuFlyoutSeparator`；删除项 `Foreground="#CCFF0000"`（80% 红；KeyboardAccelerator 文本仍次要色，可接受）。
- 注意：用户已手动微调 ShortPage.xaml（更多按钮/竖列 Margin 收窄为 12、`/>` 同行格式）——replace 失败先重读再改。
- 涉及文件：`src/Pixbian/Views/ShortPage.xaml`、`src/Pixbian/Views/ShortPage.xaml.cs`、`src/Pixbian/ViewModels/ShortViewModel.cs`。构建 0 警告 0 错误。

## Short 页第六轮：空格误触修复 + 播放按钮外观（用户反馈）
- **空格误触根因（重要）**：点击覆盖层按钮后焦点留在按钮上，空格键变成「激活聚焦按钮」——焦点在收藏按钮上时空格执行的是收藏而非页面暂停（页面 KeyDown 收不到，被按钮吞）。修复：`OnOverlayButtonTapped` 拦截 Tapped 时顺带 `Focus(FocusState.Programmatic)` 归位；菜单项（OnViewOriginalClick/OnDeleteClick）点击后也归位——MenuFlyout 关闭会把焦点还给锚定按钮，不归位空格会再次触发它。**通用结论：页面级键盘快捷键与可聚焦控件并存时，任何点击路径都必须把焦点交还页面，否则空格/回车会被聚焦控件劫持。**
- 中央播放按钮外观：常态背景 20% 白（`#33FFFFFF`，本地值覆盖 Style 的 Transparent）、图标 80% 白（FontIcon 显式 Foreground，独立于按钮 VisualState 前景）。
- 涉及文件：`src/Pixbian/Views/ShortPage.xaml`、`src/Pixbian/Views/ShortPage.xaml.cs`。构建 0 警告 0 错误。

## 幻灯片支持视频/混合放映（方案 B：独立放映页，四阶段实施）
- 方案经用户确认：幻灯片独立成 `SlideShowPage/SlideShowViewModel/SlideShowWindow`（黑色不透明舞台，FullscreenWindowBase 基类），`ImageViewerPage` 保持纯图片查看；图片查看 / 视频播放 / 幻灯片放映为三条平行链路。查看器内放映按钮改为「移交」（`SlideShowHandoffRequested` 事件 → MainWindow 关查看器、开放映窗口）。
- 阶段 1（纯重构）：`SlideShowSequencer`（Core，列表序走完 false / 随机序循环重洗 / `TryAdvance` 自动推进 vs `TryMove` 手动跳转使序列失效）；`FullscreenWindowBase`（无边框贴边/直角/SetIcon/IsAlwaysOnTop，ThinAcrylicBackdrop 留在 ImageViewerWindow）；`TransitionAnimationFactory`（淡出/滑动 Storyboard 构造，两页共用）；工具栏按钮样式提升 App.xaml 共享。教训：`_player?.Event -= handler` 对 WinRT 投影事件报 CS0079 → 显式判空退订。
- 阶段 2：放映 ViewModel 沿 ImageViewerViewModel 风格（BitmapImage 装载 + 装载序号防错配 + TransitionRequested/CompleteTransition）；Page 双 Image 层传统 Binding（窗口根非 FrameworkElement 约定）；MainWindow 单例窗口引用跟踪 + `OpenSlideShowAsync`（设置经 `_settings.Settings` 快照传入）；查看器 `ApplySettings` 收缩为滚轮/缩放/`TransitionMode`（查看器翻页转场复用 SlideShowTransition 设置项）。
- 阶段 3 视频管线（关键语义）：视频条目不按间隔计时——装载成功 `SyncTimer()` 停表由 `MediaEnded` 驱动（`NotifyVideoEnded` 仅 IsPlaying 时推进，暂停时停在末帧）；装载失败保持计时按间隔跳过坏条目不卡死；`MediaFailed` 按播完处理；`VideoPlaybackItem` 随切换/翻页/Dispose 释放（`ReleasePlayback`）；静音经 `IsVideoMuted` 属性推送页面。转场：图片↔图片按设置交叉/滑动；**涉及视频一律淡出**（视频无帧留存，退场即黑场过渡；`PreviousImage` 自然为 null → 走单帧淡入分支）；换源时先置 `Opacity=0` 防淡入前闪帧。
- 设置：`SlideShowIncludeVideos`（默认 true）/ `SlideShowVideoMuted`（默认 true）加 AppSettings + SettingsViewModel + 设置页两行 ToggleSwitch。**坑：ToggleSwitch 开关属性是 `IsOn` 不是 `IsChecked`（WMC0011）**。
- 限时播放（TimeLimited）按确认不做。GalleryPage 放映起点不再排除视频（过滤由 ViewModel 按设置承担，起点被过滤时回落首条）；CA1826 下 IReadOnlyList 取首元素用 `Count > 0 ? list[0] : null`。
- 验证：`dotnet build -warnaserror` 全解决方案 0 警告 0 错误；测试 Core 170（+8 序列器）/ WebServer 33 / Imaging 12 全绿。运行观感（放映转场、视频播放、移交链路）待用户实测。
- 涉及文件：新增 `Core/Services/SlideShowSequencer.cs`、`Core.Tests/SlideShowSequencerTests.cs`、`Views/FullscreenWindowBase.cs`、`Views/SlideShowPage.xaml(.cs)`、`Views/SlideShowWindow.cs`、`Services/TransitionAnimationFactory.cs`；修改 `ImageViewerWindow.cs`、`ImageViewerPage.xaml(.cs)`、`ImageViewerViewModel.cs`、`SlideShowViewModel.cs`、`MainWindow.xaml.cs`、`GalleryPage.xaml.cs`、`App.xaml`、`App.xaml.cs`、`AppSettings.cs`、`SettingsViewModel.cs`、`SettingsPage.xaml(.cs)`。

## Short 页第七轮：空格根治（全局加速器）+ 移除点击暂停（用户反馈）
- **空格失灵根治**：焦点归位补丁与 IsTabStop=False 均无效 → 换方案：页面级 `Page.KeyboardAccelerators`（Space/Left/Right/Delete，`Invoked=OnAcceleratorInvoked` 统一入口），**不设 ScopeOwner 即默认全局范围**——窗口内任意焦点位置都触发，彻底不依赖焦点落点；触发即 `args.Handled=true`，菜单项 DEL 不会双重执行；移除页面 KeyDown 订阅与 OnPageKeyDown（避免双路径）。
- **关键事实（XamlCompiler 实测）**：WASDK 2.3.6 的 `KeyboardAccelerator` **没有 Scope 属性**（WMC0011）——成员经包内 Microsoft.WinUI.xml 核实只有 Key/Modifiers/IsEnabled/ScopeOwner；`ScopeOwner=null` 即全局。**另外印证记忆条目「Page.KeyboardAccelerators 污染页面内所有 ToolTip」在本页无碍——页面 ToolTip 已全部移除。**
- 移除「点击画面播放/暂停」（StageRoot.Tapped + OnStageTapped）：与中央播放按钮冲突（用户反馈会造成无法继续播放）；播放/暂停唯一入口 = 空格 + 中央按钮。OnOverlayButtonTapped 的 Focus 归位一并移除（无意义），保留 e.Handled 防御性拦截。OnLoaded 的 Focus 也移除（不再依赖焦点）。
- 涉及文件：`src/Pixbian/Views/ShortPage.xaml`、`src/Pixbian/Views/ShortPage.xaml.cs`。构建 0 警告 0 错误。
- **二次修复（Tapped 归位不可靠）**：用户反馈空格仍失灵 → 根除方案 `ShortOverlayButtonStyle` 加 `IsTabStop=False`（Setter 级一处覆盖全部覆盖层按钮，含中央播放按钮）：按钮不参与键盘焦点、点击也不抢焦点，焦点永不离开页面，空格恒由页面 KeyDown 接管。教训：**页面级快捷键与按钮并存时，靠事件回调归位焦点是补丁不可靠，按钮级 IsTabStop=False 才是正解**（前提：本页无键盘 Tab 导航需求）。构建 0 警告。

## Short 页第八轮：快捷键 tooltip 规避（用户反馈）
- 现象：悬停画面出现「空格键」tooltip——KeyboardAccelerators 挂在 Page 上时，框架为挂接元素覆盖的**整个区域**自动注入快捷键 tooltip（记忆条目「污染 ToolTip」的实锤形态，即使页面没有显式 ToolTip）。
- 规避：加速器挪进 0×0 `Grid` 宿主（`KeyboardAcceleratorHost`，无命中区域 → 无 tooltip），全局触发不受影响（ScopeOwner=null 与挂载位置无关）。XAML 中 Page.KeyboardAccelerators 块移除。
- **记忆修正**：旧条目「Page.KeyboardAccelerators 污染 ToolTip → 用代码后置 KeyDown」的后半句已证伪（焦点落点不可控即失灵），正解 = **0 尺寸宿主 + 全局 KeyboardAccelerator**。
- 涉及文件：`src/Pixbian/Views/ShortPage.xaml`。构建 0 警告 0 错误。

## Short 页第九轮：进入页面空格失灵（用户反馈，第八轮删焦点逻辑的回归）
- 现象：点【片段】进入页面后空格/方向键全部失灵（点击下一个按钮也无效），**点击一次画面后才恢复**——用户实验定位了「焦点停留在导航项等窗口控件上时，全局 KeyboardAccelerator 也会被该控件的内部按键处理抢先而失效」。第八轮删掉 OnLoaded 聚焦与点击归位是回归根因：accelerator 的「全局」对「焦点在窗口控件上」的边界并不成立。
- 修复（不恢复点击暂停）：①`OnLoaded` 恢复 `Focus(FocusState.Programmatic)`；②新增 `StageRoot.PointerPressed → Focus` 纯焦点归位（不标记 Handled、不执行播放/暂停，与用户「移除点击暂停」诉求不冲突）；③OnOverlayButtonTapped 保持纯 Handled（按钮 IsTabStop=False 点击不抢焦点，焦点已在页面）。
- **通用结论（修正第八轮教训）**：全局 KeyboardAccelerator 不是焦点免疫——焦点被窗口其他控件持有时，其内部按键处理（如 NavigationViewItem 空格激活）会先于加速器吃掉按键；页面级快捷键仍须「进入页面聚焦 + 点击归位」双保障。
- 涉及文件：`src/Pixbian/Views/ShortPage.xaml.cs`。构建 0 警告 0 错误。

## Short 页第十轮：键盘控制整体移除，回归点击画面（用户最终决策）
- 用户决策：空格键彻底放弃，恢复「点击画面任意位置播放/暂停」，并强调与中央播放按钮不冲突。
- 实施：①移除全部键盘加速器（宿主 Grid、OnAcceleratorInvoked、菜单项 DEL accelerator 与 DEL 文本）——空格/方向键/DEL 同机制同病（焦点被窗口控件持有时加速器被其内部处理抢先，实测无法可靠工作），键盘路径整体放弃；②恢复 `StageRoot.Tapped=OnStageTapped`（单击画面播放/暂停）；③中央播放按钮的 Tapped 拦截是防冲突关键——按钮点击 Handled 后不冒泡到舞台，仅 Click 触发一次 TogglePlayPause（此前「暂停后无法继续播放」实为点击落在按钮之外画面区域所致，属预期行为，已向用户说明）；④清理焦点补丁（OnLoaded Focus、PointerPressed 归位）与 Windows.System using。
- **最终结论（写入记忆以固）**：WinUI 页面级键盘快捷键（KeyDown / KeyboardAccelerator 全局）在与窗口导航控件（NavigationViewItem 等）共存时均不可靠——焦点被导航项持有时按键被其内部处理抢先。本项目 Short 页放弃键盘控制，交互 = 点击画面 + 覆盖层按钮 + 中央播放按钮。
- 涉及文件：`src/Pixbian/Views/ShortPage.xaml`、`src/Pixbian/Views/ShortPage.xaml.cs`。构建 0 警告 0 错误。

## Short 页第十一轮：恢复方向键/DEL，空格维持移除（用户最终确认）
- 用户决策：方向键（←/→）与 DEL 需要保留，空格维持移除。
- 实施：①0×0 宿主恢复（仅 Right/Left/Delete，无 Space——无 tooltip 顾虑）；②恢复 OnLoaded 聚焦 + StageRoot.PointerPressed 焦点归位（加速器可靠性的前提）；③恢复 OnAcceleratorInvoked（无 Space case）；④菜单项 DEL accelerator + KeyboardAcceleratorTextOverride="DEL" 恢复（菜单打开时焦点在菜单项，靠它保障 DEL 生效；与宿主加速器靠 Handled 互斥）；⑤注意用户自行把删除项前景调成 #80FF0000（50% 红），保留勿动。
- **最终交互口径**：播放/暂停 = 点击画面 / 中央播放按钮（键盘不承载）；切换 = →/← 或「下一个」按钮；删除 = DEL 或「更多→删除」；收藏/静音/查看原视频 = 按钮。
- 涉及文件：`src/Pixbian/Views/ShortPage.xaml`、`src/Pixbian/Views/ShortPage.xaml.cs`。构建 0 警告 0 错误。

## 标题栏按钮悬停/按下底色修复
- 现象：暗色模式系统标题栏按钮（最小化/最大化/关闭）悬停时出现明显偏亮的灰底矩形，对比 Windows 照片过重。
- 根因：`MainWindow.UpdateCaptionButtonColors` 只设常态/非激活颜色，未设 `ButtonHoverBackgroundColor` / `ButtonPressedBackgroundColor`，系统回落默认高亮（暗色约 20% 白）。
- 修复：显式设置悬停/按下底色与前景，取值对齐 Fluent Subtle 令牌（暗 8%/6% 白、浅 6%/4% 黑），与设置按钮悬停观感一致。`ImageViewerWindow` 无边框无系统按钮、播放态配色同走主窗口该方法，均无需改。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml.cs`（`UpdateCaptionButtonColors` 及 remarks）。编译 0 警告；构建拷贝阶段因应用运行中（PID 锁 DLL）失败，待关闭应用后重新构建验证观感。

## 固定 UI 组件回归默认配色（用户反馈）
- 三项诉求：①标题栏系统按钮；②设置按钮悬停背景（对标左栏汉堡按钮，深浅色都应有）；③左栏菜单项深色模式下看不到悬停背景。
- **根因（通用）**：应用级顶层资源字典（`App.xaml` 的 `Application.Resources`）里的 `StaticResource` 重定向**只在启动主题下解析一次**，主题切换后仍钉死旧主题画刷——浅色启动切深色后，`NavigationViewItemBackgroundPointerOver` 等仍是浅色黑系画刷叠深底 ≈ 不可见。设置按钮 `Button.Resources` 内的三个 StaticResource 同病。
- 修复：①删除 `App.xaml` 中 NavigationViewItemBackground* 七键抄录（默认键本就随主题反转，注释留了防回归说明）；②设置按钮三键改 `ThemeResource` 引用（随主题重解析，颜色仍取系统 Subtle 令牌不写死）。
- 保留不动：标题栏系统按钮显式配色——`ExtendsContentIntoTitleBar` 后系统按钮前景不随应用主题（默认黑字深底不可见）+ 默认悬停 20% 白过亮，显式配置必要，已向用户说明。
- **教训（待沉淀长期记忆）**：`Application.Resources` 顶层/页面 `Button.Resources` 等非 ThemeDictionaries 字典里，状态键重定向一律用 `ThemeResource`，不用 `StaticResource`（后者钉死启动主题）。
- 涉及文件：`src/Pixbian/App.xaml`、`src/Pixbian/Views/MainWindow.xaml`。构建 0 警告 0 错误。

## 设置按钮改回「常态透明 + 默认模板悬停」（用户反馈）
- 诉求：设置按钮与左栏菜单按钮一致——默认无背景，仅悬停显示。
- 实现：废弃 `Button.Resources` 三键覆盖（ThemeResource 方案），改用与图库页 `ToolBarButtonStyle` 同一机制：内联 Style（不 BasedOn、不写 Template Setter——WinUI 3 显式 Style 缺 Template 时回落默认模板）`Background=Transparent / BorderThickness=0 / CornerRadius=4 / Padding=0`，悬停/按下由默认模板 VisualState 的系统键（Control 系 subtle，随主题）驱动。
- **机制结论**：「常态透明 + hover 有反馈」在本项目已验证的正解 = Style Setter 设透明（Style Setter 优先级低于 VisualState）；元素本地 Background 会被模板 TemplateBinding 压死 hover（旧教训）。`Button.Resources` 键覆盖虽理论可行，但 ThemeResource 条目方案用户实测常态仍出现底色，弃用。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml`。编译 0 警告（构建拷贝阶段又遇应用运行锁 DLL，MSB3026）。

## 删除与系统默认同值的冗余资源配置（用户确认）
- 对照包内 `microsoft.windowsappsdk.winui/2.3.6/lib/net6.0-windows10.0.17763.0/Microsoft.WinUI/Themes/generic.xaml` 核实默认值后，删除 4 条冗余：
  - `NavigationViewExpandedPaneBackground` / `NavigationViewTopPaneBackground`：默认即 `SolidBackgroundFillColorTransparent`（1580/1582 行，透明 Color 资源），与覆盖值相同；
  - `TextControlBorderBrushFocused` / `Disabled` 抄录：与默认键同值（882/883 行），删除顺带消除 StaticResource 钉死主题隐患。
- 保留的有意偏离（已核实默认值）：`NavigationViewDefaultPaneBackground`（默认 AcrylicInAppFillColorDefaultBrush）、`NavigationViewContentBackground`（默认 LayerFillColorDefaultBrush）、`ContentGridCornerRadius`（默认 8,0,0,0）、两个 BorderThickness（默认 1 / 1,1,0,0）、`TextControlBorderBrush` 与 `PointerOver`（默认 Elevation 渐变，有意改纯色）。
- **方法论沉淀**：判定「覆盖键与默认是否同值」以包内 generic.xaml 为准（Default 字典区按行号引用），不凭注释或记忆；新增覆盖前先核对，同值不覆盖。
- 涉及文件：`src/Pixbian/App.xaml`（含注释改为行号级核实依据）。编译 0 警告（应用运行中锁 DLL，拷贝阶段 MSB3026，关闭应用后重构建验证）。

## App.xaml 整体规范化（用户要求）
- 五分区结构：①合并字典 ②主题字典（自定义语义键）③共享样式 ④常量语义键 ⑤系统键覆盖区；头部注释含分区导览与关键约束。
- **关键修复**：`TextControlBorderBrush` / `PointerOver` 两条 StaticResource 从顶层字典移入 ThemeDictionaries（Default/Dark 各两条，与包内 generic.xaml 同构做法）——顶层 StaticResource 钉死启动主题，移入后随主题正确解析。至此 App.xaml 顶层字典不再有任何系统键 StaticResource。
- 注释规范化：删除过时的「抄录式状态键」总策略说明；每条系统键覆盖注明包内 generic.xaml 默认值与偏离理由；保留「与默认同值不覆盖」「随主题的覆盖放主题字典」两条通用原则。
- 键集合零增删：仅位置与注释调整，视觉行为唯一变化 = TextControl 边框深浅切换后正确跟随主题（修复）。
- 涉及文件：`src/Pixbian/App.xaml`。编译 0 警告（应用运行中锁 DLL，拷贝 MSB3026）。

## 主窗口背景：jpg 壁纸改为照片应用同款双层渐变（实测取色）
- 需求：去除 light.jpg / dark.jpg 全窗背景图，改为「线性基调 + 径向光晕」双层矢量渐变，颜色完全实测自 Windows 照片应用截图（PowerShell + System.Drawing 采样两轮）。
- **实测结论（照片应用背景真相）**：窗口底色近乎纯色的垂直微渐变——深色 #1D2125→#1F2022、浅色 #EFF4F9→#F1F3F8，「光晕感」实质是顶部冷蓝调；「内容区比左栏亮一档」由内容层底色承担，Pixbian 用半透明内容卡片叠加自动复现（合成值验算与照片内容区吻合），故卡片色未动。
- **踩坑**：第一轮采样有落点在设置行/搜索框上（#303335 / #FCFEFE 为行底色非窗口背景），凭单点色值会误推出「右下更亮的对角渐变」；补采样只取左栏/边缘条带纯背景点才还原真相。方法论：UI 取色必须多点多轮、采样点避开一切控件。
- 实施：WallpaperImage(Image) → WallpaperLayer(Grid 包双 Rectangle：PixbianWallpaperGradient 基调 + PixbianWallpaperGlow 光晕，光晕层 IsHitTestVisible=False)；UpdateWallpaper 方法及 3 处调用删除，OnActualThemeChanged 仅剩 UpdateCaptionButtonColors；播放态只切容器 Visibility；App.xaml 两主题字典各加两键（Dark 光晕 #0AFFFFFF、Default #08FFFFFF）。
- 清理：csproj 移除两 jpg 的 Content 条目；Assets 下 jpg 已删（app.ico 保留）。
- 构建：`dotnet build -warnaserror` 通过，0 警告 0 错误。
- 涉及文件：`src/Pixbian/App.xaml`、`src/Pixbian/Views/MainWindow.xaml`、`src/Pixbian/Views/MainWindow.xaml.cs`、`src/Pixbian/Pixbian.csproj`；删除 `src/Pixbian/Assets/light.jpg`、`dark.jpg`。

## 深色背景二轮调整：去径向光斑，改纯线性光（用户反馈）
- 反馈：深色下径向光晕观感突兀（左上一团亮斑），要求线性光渐变；浅色 OK 不动。
- 实施：Dark 的 `PixbianWallpaperGradient` 改三色线性 `#222830`→(0.45)`#1D2125`→`#1F2022`（顶部泛蓝光承载纵深感）；`PixbianWallpaperGlow` 在 Dark 字典改全透明 SolidColorBrush——**键必须保留**（MainWindow.xaml 模板引用该键，Dark 字典缺键 = 资源解析失败启动即崩），仅浅色保留径向光晕。
- 涉及文件：`src/Pixbian/App.xaml`、`src/Pixbian/Views/MainWindow.xaml`（仅注释）。构建 0 警告 0 错误。

## 浅色背景同步改三色线性光（用户反馈）
- Default 的 `PixbianWallpaperGradient` 改三色线性 `#EBF1F8`→(0.45)`#F0F4F8`→`#F2F3F6`；`PixbianWallpaperGlow` 同 Dark 转全透明 SolidColorBrush。至此两主题均纯线性渐变、无径向光斑，双层结构保留（光晕层恒全透明，纯作模板占位）。
- 涉及文件：`src/Pixbian/App.xaml`。构建 0 警告 0 错误。

## 深色线性光按参考图重校准（用户给新截图）
- 三轮采样用户指定参考图纯背景点：顶部 `#1B2225`（冷蓝）→ 中部 `#1D2124` → 底部 `#1E2023`，整体比此前实现更暗更收敛（此前顶部 `#222830` 偏亮）。按实测替换 Dark 三色标。
- 涉及文件：`src/Pixbian/App.xaml`。构建 0 警告 0 错误。

## 浅色模式设置按钮悬停背景不可见修复（用户反馈）
- 根因：设置按钮悬停走默认模板 VisualState 的系统 Control 系键（≈5% 黑），在新浅色渐变背景（近白 #EDF5F9）上合成后仅差几个灰阶，肉眼不可见；深色下 7% 白相对可见故未报。
- 修复：App.xaml 两主题字典各加 `PixbianTitleButtonHover`（浅 #14000000 / 深 #14FFFFFF）与 `PixbianTitleButtonPressed`（浅 #1F000000 / 深 #1FFFFFFF）；设置按钮 `Button.Resources` 用 ThemeResource 只覆盖 `ButtonBackgroundPointerOver/Pressed` 两键。**关键：不覆盖常态键 ButtonBackground**——常态透明仍由 Style Setter 控制（覆盖常态键致常态出底色是旧坑）。
- 涉及文件：`src/Pixbian/App.xaml`、`src/Pixbian/Views/MainWindow.xaml`。编译 0 警告；构建拷贝阶段遇应用运行锁 DLL（MSB3026，PID 13352），待关闭应用后重新构建验证。

## 图库项 chevron 悬停背景（用户反馈，参考分类刷新按钮）
- **关键模板事实（包内 generic.xaml 核实）**：NavigationViewItem 模板内 chevron 是 `Grid x:Name="ExpandCollapseChevron"`（32748 行），背景为**字面量 Transparent**、不引用资源键 → 无法经资源覆盖实现；重写模板成本过高。注意 19124 行同名的 ExpandCollapseChevron 属 TreeViewItem 模板，勿混淆。
- 实现：代码后置方案——`NavigationViewControl.Loaded` 后经视觉树深度优先按 x:Name 定位元素（FindName 对模板内元素无效），`PointerMoved` 按 chevron 实际左缘（TransformToVisual）判定落点、进亮离熄（Background 置 null 回落模板字面量），8% 主题反色与设置按钮悬停同档（浅黑深白各缓存一支 brush）。
- 坑：CA1859（-warnaserror 下为错误）要求方法返回类型收窄 Brush→SolidColorBrush。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml.cs`。构建 0 警告 0 错误。

## chevron 需求撤销 + 设置按钮悬停再加深（用户反馈）
- chevron 悬停背景：8% 实测依然不可见（叠在整行 hover 背景上无对比），用户授权取消——**已完整撤销**代码后置方案（构造函数订阅 + 方法块）。结论固化：NavigationViewItem chevron 悬停背景在「不改模板」前提下无可靠实现路径，需求关闭。
- 设置按钮悬停：8% 黑用户实测「依然不清晰」，对齐照片应用观感加深——Hover 12%（#1F000000 / #1FFFFFFF）、Pressed 20%（#33000000 / #33FFFFFF）。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml.cs`、`src/Pixbian/App.xaml`。构建 0 警告 0 错误。

## 设置按钮悬停「调色无变化」根因确诊 + 修复（用户反馈）
- **关键确诊**：用户把 Hover 改成纯黑 #000000 仍无变化 → Button.Resources 级的 `<ThemeResource>` 键重定向**根本不被默认模板 VisualState 解析**——此前看到的淡色一直是系统默认 5% 黑，历次调色值自然全部无效。
- **修复（借鉴 Short 页已验证模式）**：把 `ButtonBackgroundPointerOver/Pressed` 两键的 ThemeResource 重定向从 `Button.Resources` 挪到 **`TitleBar` Grid.Resources（模板外父级）**——Short 页在 Page.Resources（同为模板外父级）的同机制覆盖是实证有效的。Hover 12% / Pressed 20% 色值不变。
- **通用结论（沉淀）**：WinUI 3 默认模板 VisualState 的 ThemeResource 解析会跳过 templated parent 自身的 Button.Resources；覆盖模板状态键必须放在「模板外」的祖先级 Resources（Page/父 Grid/App 主题字典）。控件模板内状态调色无效时先查覆盖位置是否在模板内。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml`、`src/Pixbian/App.xaml`（注释更新，恢复用户误改的 #000000 → #1F000000）。构建 0 警告 0 错误。

## 设置按钮悬停最终方案：事件驱动（资源覆盖三级全败后）
- **实证结论**：默认模板 VisualState 的状态键覆盖在 Button.Resources、TitleBar Grid.Resources（祖先级 ThemeResource 条目）两级均实测不生效（用户调纯黑仍无变化）——「模板内状态键可被元素树资源覆盖」在本项目环境对 Button 默认模板不成立，勿再尝试该路径。
- **最终方案**：PointerEntered/Exited 事件直接驱动 `SettingsButton.Background`（12% 主题反色，浅深画刷各缓存；退出置 null 回落透明）。本地 Background 压死 VisualState 无妨——进出成对设/清即可。App.xaml 死键 PixbianTitleButton* 已清理。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml`、`src/Pixbian/Views/MainWindow.xaml.cs`、`src/Pixbian/App.xaml`。构建 0 警告 0 错误。

## 悬停底色二轮：浅色加深 + 主题切换残留刷新（用户反馈）
- 反馈：深色悬停可见、浅色不可见。两处修正：①浅色加深到 18% 黑（近白底上人眼对暗块敏感度更低，比深色需更高 alpha 才达同等观感；深色维持 12% 白）；②`OnActualThemeChanged` 中按钮 Background 非 null（指针停在按钮上）时按新主题重设——旧主题画刷残留（深色白块压浅底）是"浅色看不见"的候选根因之一。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml.cs`。构建 0 警告 0 错误。

## 设置按钮悬停终局：自定义 ControlTemplate（浅色仍非所设色）
- 现象：事件驱动下深色可见、浅色"有反馈但不是设定的 18% 黑"——默认模板状态下事件设置的画刷疑似被系统状态行为稀释/干扰，根因未能定位。
- **终局方案**：设置按钮改自定义 ControlTemplate——模板仅 `Border(TemplateBinding Background/CornerRadius) + ContentPresenter`，**不含任何 VisualState**，背景所见即所设，彻底排除默认模板状态的一切干扰；事件驱动保留（浅 18% 黑/深 12% 白）。代价：失去默认模板的按下微交互（此前本地 Background 也早已压死状态背景，观感无退化）。
- **通用结论**：Button 悬停色定制历经「资源键覆盖（Button/祖先两级）→ 事件驱动 → 自定义模板」三级演进；若事件驱动仍被干扰，自定义模板剔除全部状态是最终确定性手段。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml`。构建 0 警告 0 错误。

## 设置按钮悬停色对齐系统标题栏按钮（用户确认可见后的微调）
- 自定义模板方案验证有效（用户确认可见）。悬停色从 18%/12% 改为与 `UpdateCaptionButtonColors` 的系统标题栏按钮完全同值：深 8% 白 `#14FFFFFF`、浅 6% 黑 `#0F000000`（ColorHelper.FromArgb 直接 alpha 进色，弃用 Brush.Opacity 双机制叠加）。应用内按钮与系统按钮悬停观感统一。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml.cs`。构建 0 警告 0 错误。

## 浅色线性光按参考图重校准（用户给新截图）
- 采样浅色参考图纯背景点：顶部 `#EDF5F9` → 中部 `#EEF4F9` → 底部 `#F0F3F9`，替换此前偏灰暗的实现（`#EBF1F8` 起）。两主题至此均以各自参考截图实测校准。
- **注意**：用户会手动重排 XAML 属性换行（每属性独立成行）且微调过卡片色值（Dark 卡片 `#66282828`/描边 `#59191919`）——编辑前必须重读文件当前内容，不能凭记忆 old_str。
- 涉及文件：`src/Pixbian/App.xaml`。构建 0 警告 0 错误。

## 设置页「媒体库」分区合并（用户确认方案后实施）
- 需求：将「媒体库」「音乐库」两个独立分区合并为单一「媒体库」卡片，行结构参照 Windows 照片应用「库」设置（用户确认三项取舍：Expander 默认收起、条目精简为路径 + ×、状态并入对应行副标题）。
- 结构：卡片内三行——「图库位置」Expander（ED25，Header 右侧「添加文件夹」，展开内嵌 Folders 列表 + 底部移除提示 caption）→ 分隔线 →「音乐库位置」Expander（E189，副标题 = MusicStatusText 动态，展开内嵌 MusicFolders 列表）→ 分隔线 →「刷新库」普通行（E72C，副标题 = ProgressRing + StatusText，右侧「立即索引」Accent 按钮）。
- 实现要点：Expander 资源覆盖与「端口与密码」展开区同构（三厚度归零 + 五画刷键指 SubtleFillColorTransparentBrush）；条目模板精简为路径（OneTime）+ × 图标按钮（E711，Padding=6，AutomationProperties.Name=移除），事件通路原样（OnRemoveFolderClick/OnRemoveMusicFolderClick 含二次确认）；LibraryFolderRow.Path 绑定由 OneWay 改 OneTime（路径恒定）。
- ViewModel 零改动；code-behind 仅头注释措辞同步（6 个事件处理器全部复用）。
- 涉及文件：`src/Pixbian/Views/SettingsPage.xaml`、`src/Pixbian/Views/SettingsPage.xaml.cs`（仅注释）。构建 0 警告 0 错误；lint 无诊断。
- 待运行时实测：Expander Header 内「添加文件夹」按钮点击是否连带切换展开（行为可接受：点添加后列表展开正好可见新路径；违和再处理）。

## 设置页媒体库二轮微调（用户 6 项反馈）
- ①图库/音乐列表每个条目底部加分隔线（DataTemplate 内 ColumnSpan=2、VerticalAlignment=Bottom 的 1px DividerStroke Border）；②「立即索引」按钮去 AccentButtonStyle 改普通样式；③音乐库位置行图标 E189 音符 → ED25 空心文件夹（与图库位置行统一，对齐照片应用模板两位置行同图标）；④删除图库列表下方「移除会清理索引记录」提示文字（信息已在二次确认对话框保留）；⑤「图库位置」副标题改绑新属性 `FolderCountText`（"尚未添加文件夹"/"共 N 个文件夹"，LoadAsync 中更新，对齐音乐库的计数文案）；⑥「刷新库」副标题改绑新计算属性 `IndexLineText`（IsIndexing ? StatusText : LastIndexText），默认显示 `LastIndexText`（"尚未索引"/"上次索引：yyyy-MM-dd HH:mm"，RefreshLastIndexText 取 Folders 中最大 LastScanUtc，LoadAsync 与 ScanFolderAsync 完成处刷新）。
- **坑（Toolkit 8.4.0 实测）**：用 `partial void OnIsIndexChanged(bool)` 等 hook 联动通知计算属性报 CS0759「无定义声明」——源生成器生成的 hook 签名与手写实现不匹配（可空性差异），且连锁引发整批 XAML "Unknown type" 假错误。**正解：构造函数中自订阅自身 PropertyChanged，按 e.PropertyName 过滤后通知计算属性**，零生成器耦合。
- 连带语义变化（已向用户呈现）：非索引态的 StatusText 消息（"已添加并完成索引：X"等）不再显示在刷新库行，由 LastIndexText 时间语义等价替代。
- 涉及文件：`src/Pixbian/Views/SettingsPage.xaml`、`src/Pixbian/ViewModels/SettingsViewModel.cs`。构建 0 警告 0 错误。

## 设置页三轮：移除条目分隔线（用户反馈）
- 图库与音乐列表条目底部的 1px 分隔线整体移除（上轮加的两处一并撤销，保持一致；行间分隔线仅在 Expander 之间保留）。构建遇应用运行锁 DLL（MSB3026，PID 3784），编译 0 警告，待关闭应用后重构建确认。

## 设置页四轮：Expander 行与普通行左缘对齐（用户截图反馈）
- 现象：「图库位置」「音乐库位置」（Expander 行）的图标文字比「刷新库」（普通 Grid 行）整体右缩 16px。
- **根因（包内 generic.xaml 23288 行核实）**：Expander 模板 Header 是 ToggleButton，`ExpanderHeaderPadding` 默认 `16,0,0,0`——与行内 SettingsRowStyle 的 Padding 16,12 叠加后左侧 32px。
- 修复：两个 Expander 的 Resources 各覆盖 `<Thickness x:Key="ExpanderHeaderPadding">0</Thickness>`（与既有的 BorderThickness/ContentPadding 归零同一处）。WebSettingsExpander 未动（仅 Header 文字，缩进不显眼，非本次诉求）。
- **沉淀**：WinUI Expander Header 内嵌「与普通行同构」的内容时，须额外归零 ExpanderHeaderPadding（默认左 16）。
- 涉及文件：`src/Pixbian/Views/SettingsPage.xaml`。构建 0 警告 0 错误。

## 设置页五轮：Header 对齐真修（上轮资源覆盖无效的根因确诊）
- 上轮 `ExpanderHeaderPadding` 资源覆盖实测无效（用户再截图仍缩 16px）。
- **根因（generic.xaml 确诊，重要机制事实）**：Expander 样式里生效的覆盖键（BorderBrush/BorderThickness/各 Background）在 Setter/模板中是 **ThemeResource**（运行时沿元素树解析，实例资源可穿透）；而两个 Padding——Expander 主样式 Setter（21727 行 `StaticResource ExpanderContentPadding`）与 Header ToggleButton 样式 Setter（35879 行 `StaticResource ExpanderHeaderPadding`）——是 **StaticResource**（generic.xaml 加载时一次性解析，实例资源覆盖无法穿透）。推论：WebSettingsExpander 的 ExpanderContentPadding 归零其实也从未生效，其内容区「恰好」由 ContentPadding 16 + 行 Grid 16 = 32 与 Header 32 对齐而无异常。
- **正解（本地值方案）**：Expander 元素本地 `Padding="0"`（本地值 > 样式 Setter）；Header 的 ToggleButton 在 `Loaded` 后经视觉树深度优先按 x:Name 定位（`ExpanderHeader`），以本地值 `Padding=0` 归零——SettingsPage 新增 `OnLibraryExpanderLoaded` + `FindDescendantByName<T>`（与 GalleryPage 同模式）。删除两个 Expander 中无效的 ContentPadding/HeaderPadding 覆盖行。
- **沉淀（勿再试错）**：实例级 Resources 覆盖对「样式 Setter 中的 StaticResource」无效，只对 ThemeResource 有效；判定改外观走哪条路前先看包内 generic.xaml 中该 Setter 是 ThemeResource 还是 StaticResource。ToggleButton 在 `Microsoft.UI.Xaml.Controls.Primitives`（漏 using 报 CS0246 并连锁整批 WMC 假错误）。
- 涉及文件：`src/Pixbian/Views/SettingsPage.xaml`、`src/Pixbian/Views/SettingsPage.xaml.cs`。构建 0 警告 0 错误。

## 设置页六轮：WebSettingsExpander 一并本地值规范化（用户要求）
- `WebSettingsExpander`（端口与密码）同样删除无效的 `ExpanderContentPadding` 资源覆盖，改 Expander 本地 `Padding="0"`。归零真正生效后展开区各行（SettingsRowStyle 16）、状态文字（Margin 16）、访问链接（Margin 12 + HyperlinkButton Padding 4 = 16）与 Header 文字（ToggleButton Padding 16）左缘统一为 16——此前覆盖无效时行左缘实为 32、链接 28，与 Header 16 并未对齐，属隐性错位被「恰好差不多的观感」掩盖。
- Header 未动：其 ToggleButton 的 16px 左缩进在此处是期望值（无 SettingsRowStyle 同构行），与媒体库两行归零 + 行自身补 16 的口径等价。
- 涉及文件：`src/Pixbian/Views/SettingsPage.xaml`。构建 0 警告 0 错误。

## 设置页七轮：移除「秒」文字 + 局域网分区去 Expander（用户反馈）
- ①幻灯片间隔行右侧的「秒」TextBlock 移除（用户已手动把单位并入标题「幻灯片间隔（秒）」，注释同步）；②「允许局域网访问」分区拆除「端口与访问密码」Expander：开关打开时经 Visibility（IsWebSharingOn）直接显示端口行、密码行、状态文字与访问链接，删除 Expander 标签、Resources 覆盖与 Header（分隔线保留）。
- **应用时机替代**：原「收起即应用」（Expander Collapsed）随 Expander 移除失效 → 改为两个输入框 `LostFocus="OnWebSettingLostFocus"` 触发 ApplyWebSharingAsync，保留「端口未变且无新密码则跳过」判定（_appliedPortText）；删除 OnWebSettingsCollapsed，x:Name WebSettingsExpander 无代码引用一并移除。文件头与 cs 注释同步（注意：局域网交互描述在 XAML 文件头，不在 cs 头——先确认再改）。
- 涉及文件：`src/Pixbian/Views/SettingsPage.xaml`、`src/Pixbian/Views/SettingsPage.xaml.cs`。构建 0 警告 0 错误。

## 设置页八轮：列表条目贴边（用户截图反馈）
- 现象：展开后的文件夹列表条目内容比 Header 缩进约 16px。
- **根因（generic.xaml 27531 行核实）**：`DefaultListViewItemStyle` 的 `Padding="16,0,12,0"` 是**字面量**（非资源引用），经模板 `ListViewItemPresenter ContentMargin="{TemplateBinding Padding}"` 生效 → 条目文字左缘 = 容器 16 + 条目 Grid 16 = 32，Header 仅 16。
- 修复：两个列表加 `ItemContainerStyle`（`BasedOn="{StaticResource DefaultListViewItemStyle}"` + `Padding=0`）——本地 Setter 覆盖同层默认 Setter，无需视觉树/负 Margin。
- **沉淀**：ListView 条目默认左缩进来源是 ListViewItem 样式的 Padding 字面量 16,0,12,0（经 ContentMargin 生效）；贴边列表用 ItemContainerStyle 覆盖 Padding=0，可 BasedOn DefaultListViewItemStyle（generic.xaml 非 ThemeDictionary 区的样式键可被页面 StaticResource 解析）。
- 涉及文件：`src/Pixbian/Views/SettingsPage.xaml`。编译 0 警告；构建拷贝阶段遇应用运行锁 DLL（MSB3026，PID 3352），待关闭应用后重构建确认。

## 设置页九轮：条目行距压到 24 + 发现用户手动改动（重要协作事实）
- 需求：条目行距「控制在 24」。
- **发现用户在会话间隙手动改了条目模板**：图库条目 Grid `Padding="12"`（非我写的 16,4）并自加了 `BorderThickness="0,1,0,0"` 分隔线；音乐条目仍是 16,4——这才是「音乐列表与图库不一致」的真正来源（此前误判为构建版本问题）。
- 统一实施：两列表条目 Grid `Padding="12,2"`（保留用户的水平 12 与 Border 分隔线）+ `ItemContainerStyle` 加 `MinHeight=24`（覆盖默认 44）+ × 按钮 Padding 6→2、FontIcon 14→12（防撑破 24 行高）。行高 = 文字 20 + 垂直 4 ≈ 24（顶部 Border 另占 1）。
- **协作教训**：用户会并行手改 XAML；replace 失败或「不一致」类反馈先重读文件现状再下结论，别先入为主归因于构建版本。
- 涉及文件：`src/Pixbian/Views/SettingsPage.xaml`。构建 0 警告 0 错误。

## 标题栏搜索框改照片应用风格（无边框无底色）
- 目标（用户给截图）：对齐 Windows 照片应用——静默态无边框、无底色，占位文字直接压标题栏背景。
- 实现：搜索框父 Grid（非 templated parent 自身，遵循「祖先级覆盖」口径）覆盖 TextBox 系键——BorderBrush 三态 + TextControlBackground → `ControlFillColorTransparentBrush`；BackgroundPointerOver/Focused → `SubtleFillColorSecondaryBrush`（Fluent 次级填充，随主题反转的淡底反馈）。AutoSuggestBox 本地 Background="Transparent" / BorderThickness="0" 双保险。作用域限定该 Grid，不影响页面其他输入框。
- **机制依据**：AutoSuggestBox 内部 TextBox（AutoSuggestBoxTextBoxStyle，包内 16034-16037 行）对这批键均为 ThemeResource 引用 → 祖先级覆盖可穿透（StaticResource 则无效）。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml`。构建 0 警告 0 错误；聚焦淡底与占位文字颜色观感待用户实测。

## 删除 App.xaml 输入框边框系统键覆盖（用户确认）
- 删除 ThemeDictionaries 内成对的 `TextControlBorderBrush` 系覆盖（Default 字典 4 条含用户手改的 PointerOver→ElevationBorderFocusedBrush；Dark 字典 3 条），应用内输入框（设置页端口/密码等）回退 WinUI 默认竖向渐变边框；标题栏搜索框不受影响（MainWindow 搜索框 Grid.Resources 的作用域覆盖优先）。
- 注意：此轮期间用户还手动改过 96 行（PointerOver→ElevationBorderFocusedBrush）——成对覆盖删除时须检查两个主题字典各一份，只删一份会造成两主题不一致。
- 涉及文件：`src/Pixbian/App.xaml`。构建 0 警告 0 错误。

## 设置页：局域网状态与地址链接合并一行（用户反馈）
- 「已启动，点击地址可在浏览器中打开：」状态文字与 WebAccessUrls 链接（ItemsControl）合并为一行：外层横向 StackPanel（Margin 16,16,16,16 保留用户手调值）+ 状态 TextBlock（去 TextWrapping，VerticalAlignment=Center）+ ItemsControl 改横向 ItemsPanel（多条地址横向排列、Spacing 8）。
- 注意：用户手动改过该区域 Margin（4→16 垂直）——replace 失败即重读，勿用旧记忆 old_str。
- 涉及文件：`src/Pixbian/Views/SettingsPage.xaml`。构建 0 警告 0 错误。
- 澄清：此前「间隔控制 24」指的是 Header 右侧「添加文件夹」按钮与 Expander 展开箭头的**水平间距**（非条目行距；行距 24 的改动保留未撤）。
- 实现：按钮加 `Margin="0,0,4,0"`——模板箭头区自带左间距 20（generic.xaml 35871 行 `ExpanderChevronMargin` 默认 `20,0,8,0`），相加即 24；两处「添加文件夹」按钮同改。

## 设置页十一轮：按钮与箭头间距改 8（用户反馈）
- 方案升级：撤掉按钮 Margin 补距，改在两个 Expander 的 Resources 覆盖 `<Thickness x:Key="ExpanderChevronMargin">8,0,8,0</Thickness>`——箭头 Border 的 Margin 是 **ThemeResource**（36091/36320 行，可穿透实例资源），资源覆盖有效；左 8 = 按钮与箭头间距，右 8 保留箭头到卡片右缘。
- **判别式补全**：模板/样式内 ThemeResource 引用 → 实例资源覆盖有效（如 ChevronMargin、BorderBrush）；StaticResource → 无效（如两个 Padding Setter）。覆盖前先查引用方式。
- 涉及文件：`src/Pixbian/Views/SettingsPage.xaml`。构建 0 警告 0 错误。

## 搜索框边框恢复可见（用户反馈：还是要显示边框）
- 搜索框 Grid.Resources 的三个 BorderBrush 键由透明改为：静默 `ControlStrokeColorDefaultBrush`（一级描边）、悬停/聚焦 `ControlStrokeColorSecondaryBrush`（二级描边加深），底色逻辑不变（静默透明、悬停/聚焦淡底）。用户已自行把 BorderThickness 设回 1。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml`。构建 0 警告 0 错误。

## 搜索框浅色悬停/聚焦底色补齐（用户反馈：背景色也要设置）
- 用户手动重构了搜索框 ThemeDictionaries（浅色用 `x:Key="Default"`、深色 `x:Key="Dark"`，色值全改：浅色静默底 #33111111、深色边框 #1ACCCCCC 系）但浅色字典漏了 `TextControlBackgroundPointerOver/Focused`——缺失会回落系统默认纯白底，压渐变标题栏突兀。补齐为 `#33222222`（比静默底深一档，与 Dark 字典同思路）。构建 0 警告 0 错误。
- **注意键名语义**：用户以 `Default` 字典承载浅色（WinUI 惯例 Default=深色），当前运行正常是因应用按字面量取值；后续若有人按 WinUI 惯例理解会踩反——保持现状，勿「纠正」。
- 色值核实（包内 generic.xaml）：深色 ControlStrokeColorDefault #12FFFFFF（7% 白）/ Secondary #18FFFFFF（9% 白）——系统描边级在近黑标题栏上都不可见，升级无意义 → 深色用字面量：静默 `#40FFFFFF`（25% 白）、悬停/聚焦 `#59FFFFFF`（35% 白）；浅色维持系统描边（Default 6% 黑 / Secondary 16% 黑，浅底已可见）。
- 结构：Grid.Resources 内显式包 `<ResourceDictionary>` 再放 `<ResourceDictionary.ThemeDictionaries>`（Light/Default 两分支）——**坑：`Grid.Resources` 下直接写 `<ResourceDictionary.ThemeDictionaries>` 属性元素报 WMC9997 解析错误，必须显式包一层 ResourceDictionary**。
- 顺带修复隐患：底色三键（TextControlBackground* → SubtleFillColorSecondaryBrush）从 Grid.Resources 顶层移入主题字典——顶层 StaticResource 重定向会钉死启动主题（App.xaml 旧教训同样适用于元素级字典）。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml`。构建 0 警告 0 错误。

## 搜索框注释对齐代码（用户要求）
- 用户手动改色后（浅色底 25%/40% 白、深色边框 10%/15% 浅灰 + 底 20%/40% 灰），三处注释（外层大注释、浅色字典、深色字典）仍描述旧方案（无边框无底色/Subtle 淡底/25% 白边框）——全部重写对齐当前代码。构建 0 警告 0 错误。
