# 2026-09-02 工作日志

## README.md 基于代码实际内容重写

- 逐项目读源码取证（csproj / Directory.Build.props / global.json / Core 模型与服务 / Data Schema / WebServer 路由与鉴权 / UI 页面与 ViewModel），重写根目录 `README.md`。
- 修正旧 README 与代码不一致之处：
  - 入口脚本由不存在的 `run.ps1` / `run.cmd` 改为实际的 `Pixbian.ps1`；`THIRD-PARTY-NOTICES.md` 实际位于 `docs/` 而非根目录。
  - 删除已全链路移除的「发现模式 / DiscoverService」相关描述（仅保留图库排序里的「随机」排序键）。
  - 明确 FFmpegInteropX 未引入（视频走系统解码器）；明确未使用 WASDK 2.4 的触觉反馈 / LanguageModel 新 API。
  - 新增「当前状态与已知未完成项」章节，如实列出未接线项。
- 新增章节：技术栈与包版本表、两阶段索引 / 缩略图管线 / 自适应布局 / 局域网服务的实现要点、数据模型与安全设计、测试基线与快捷键表。
- `tools/_probe.ps1` 含硬编码个人路径、属一次性临时脚本，未写入 README 的工具列表。

## 长期记忆维护

- `MEMORY.md` 因超出注入上限被截断，按「只收规范与稳定事实」原则合并去重后原地重写（约 130 行），保留分层约束、WinUI 踩坑、缩略图管线、索引回填、性能方法论等条目。

## docs/ 三份文档对齐代码（用户确认方案后执行）

改动前先取证逐条比对，共修正 15 类偏差。三个文件的处理方向由用户在弹窗中确认。

### 可行性开发方案.md（全量重写，1823 → 约 480 行）
- 从「历史方案」改为**现行架构文档**：保留分层 / ADR / Schema 骨架，「计划·待定·阻塞」全部改为落地现状。
- 新增 §8 十二项 ADR（.NET 8 选型、平台、XAML 编译器属性、系统解码器、自研 HTTP、PBKDF2、两阶段索引、
  档位量化、分页重匹配、物理删除、移除发现模式、随机排序取模）。
- 新增 §9「与初版方案的偏差总表」15 条，逐条标注初版 → 实际 → 类别。
- 修正的硬错误：索引为**物理 DELETE 非软删除**（`deleted_utc` 列存在、查询过滤但从未写入）；
  分类截断 **4096 非 260**；Justified 用 **3 档非 5 档**；`/api/items` 无 category/date/ETag/二维码。

### WinUI3-界面设计规范.md（修正 §11/§12 + 新增 §12.1）
- **§11 更正**：`StaticResource` **无法解析 `ThemeDictionaries` 内的资源**（初版记为「可行」）。
  实证见 `App.xaml` 第 133-134 行注释：卡片底色/描边刻意定义在主题字典之外。
- **§12.1 更正**：Justified 画廊是**单个非分组 GridView + JustifiedPanel**，条目纯平铺不分组，
  非初版所记的「ItemsControl 按组迭代 + 组内 GridView」；方形视图同样非分组（换内建 ItemsWrapGrid）。
  依据 `GalleryPage.xaml` 第 12-13 行与第 723-725 行注释。
- 新增 §12.1「布局与渲染（踩坑集）」按主题分组：LayoutCycle、模板/绑定/状态、菜单与工具栏、
  外观与资源、窗口视觉分层、骨架屏与淡入、异步管线、符号字体码点。
- §13 验收清单补入 8 条布局与渲染自查项。

### THIRD-PARTY-NOTICES.md（修正许可与过时结论）
- `Microsoft.Data.Sqlite` 许可 **MIT → Apache-2.0**（托管代码在 dotnet/aspnetcore，该仓库用 Apache-2.0；
  MIT 是 dotnet/runtime 等另几处所用，此前混用）。SQLite 本体仍为 Public Domain。
- FFmpegInteropX 决策记录重写：原 CS1705 阻塞**前提已消除**（TFM 已升 `net8.0-windows10.0.26100.0`，
  不再与 19041 目标冲突），改以**包体积 175.6 MB + 必要性不足**作为不引入理由。
- 补齐测试期组件 `xunit.runner.visualstudio` 2.8.2；说明 `FFmpegInteropXVersion` 在
  `Directory.Build.props` 中定义但**零项目引用**，不进发行包、不产生 LGPL 义务。
- 「本项目许可」改为如实说明：**仓库无 LICENSE 文件**，并提示若选 GPL-3.0 会与 ImageSharp 双许可冲突。

### 连带修改
- `README.md` docs 目录树中对三份文档的描述同步更新。

## 方法备忘

- 文档与代码对齐前，**先列偏差清单再动手**：本次先比对出 15 类偏差，弹窗让用户选处理方向，避免返工。
- 判定文档描述真伪时优先找**代码内注释**（`App.xaml`、`GalleryPage.xaml` 中已有明确实测结论），
  比重新读实现更快且更权威。
