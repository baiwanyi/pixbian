# 2026-09-09

## 等高视图多选错位修复（未提交，4 文件）
- 现象：等高模式点 checkbox 能选中并进入选择模式；再点其他**图片**就退出选择模式（外框全消、选中态“还在”、悬停才看得见）；多点几次后点图片与点 checkbox 都退出。方形模式正常。
- 根因（两条叠加）：
  1. 等高布局 Arrange 只排本 pass realized 的元素；已滚出但 Repeater 尚未回收的元素（幽灵槽位）仍停留在旧矩形并继续参与命中测试，点图片主体命中它们 → `ResolveHit` 沿树取到**旧条目** DataContext → `Toggle` 了不属于当前集合的游离条目。
  2. `GallerySelectionService.SelectedItems = _selected ∩ Items`，游离条目进入 `_selected` 后交集恒空 → `HasSelection=false` → 每一次 SelectionChanged 都走 `ExitSelectionMode`（这就是“点什么都是退出”的唯一出口）。
- 修复：
  - `JustifiedVirtualizingLayout`：新增 `PreviousElements` 映射与 `SwapRealizedElements()`；Arrange 前把「上一 pass 有、本 pass 无」的元素 `Arrange(default)` 收拢到零矩形（**不碰 Visibility**——Repeater 元素运行期改它必 fail-fast 0xc000027b）。
  - `GalleryPage.ResolveHit` / `OnJustifiedCheckClick`：改以 `JustifiedRepeater.GetElementIndex(element)` 反查集合为唯一真相源，DataContext 仅作“元素已失去映射”时的兜底；`ResolveHit` 由 static 改实例方法。
  - `GallerySelectionService`：新增 `PruneStale()`（剔除游离条目并复位其 IsSelected），`Toggle` 遇到不在当前集合的条目直接忽略。
  - 等高 CheckBox 的 `Opacity` 改绑 `IsSelectionCheckVisible`（复用已定义但闲置的 `BoolToSelectionOpacityXBind`），删除 `SyncJustifiedCheckOpacity()` 视觉树快照补写；`ElementPrepared` 回调就地补 `IsSelectionCheckVisible`（覆盖分页/滚动新条目）；`ExitSelectionMode` 按当前集合全量复位 `IsSelected`（清残留勾）。
- 构建：`-c Release -warnaserror` → 0 警告 0 错误（50s）。
- 结果：**用户手工验证通过（等高多选恢复正常）**；取证埋点 `HIT|idx=|ctx=` 已删除并重新构建 → 0 警告 0 错误（31s）。
- 复盘要点：本次「点图片退出选择模式」这类症状，与其逐个调交互分支，不如先找**退出选择模式的唯一出口**（`HasSelection=false`）再倒查为何判空——选择集合用「交集」语义时，游离条目会让判空恒真，这是自建选择服务的通用陷阱。

## 等高视图翻页被弹回顶部（未提交，2 文件）
- 现象：等高模式滚到底部触发翻页后被弹回顶部，看不到新加载内容（数据其实加载了）。
- 根因：`GalleryViewModel` 的翻页追加 / 集合替换 / 删除**三条路径共用 `ItemCount` 通知**，而页面把「回顶 + 重建元素」挂在 ItemCount 上 → 翻页后 `RebuildJustifiedElements()` 强制 `ChangeView(null,0,…)`。方形视图的回顶挂在 `IsQuerying` 分支（只有 reset 置 true），故只有等高模式中招。删除路径同样误回顶。
- 修复：VM 新增 `ItemsReplaced` 事件（仅 `ReplaceItemsCore` 触发），页面把 `RebuildJustifiedElements` 的触发源从 ItemCount 换成该事件；ItemCount 通知保留（仍驱动统计行 / 空状态 / 复选框同步）。教训：**语义型通知（回顶/重建）不能挂在共用计数通知上**，事件名即语义。
- 构建 `-c Release -warnaserror` → 0 警告 0 错误（32s）。待用户手工验证连续翻页滚动位置稳定。
- 结果：用户手工验证通过（翻页滚动位置稳定）。

## 等高视图选中后 DEL 键无效（未提交，2 文件）
- 现象：等高模式选中若干条目后按 DEL 无反应。
- 根因：`RestoreContentFocus` 给等高宿主 `JustifiedView`（ScrollViewer）做 `Focus(Programmatic)`，但 **WinUI 的 ScrollViewer 默认 IsTabStop=False → Focus 静默失败**；模式切换时框架已把焦点自动转移到搜索框，DEL 被文本编辑消费，页面 KeyDown 收不到。P2 把等高宿主从 GridView（IsTabStop=true，聚焦成功）换成 ScrollViewer+ItemsRepeater 引入的回归；方形模式 GridView 聚焦成功故正常。
- 修复：XAML 给 `JustifiedView` 加 `IsTabStop="True"`；`RestoreContentFocus` 注释写明该前提（不检查 Focus 返回值是有条件的）。
- 构建 → 0 警告 0 错误（41s）。待用户手工验证。
- 结果：焦点修复生效（F2/Ctrl+A 能到页面），但 DEL/Ctrl+C/Ctrl+D/Esc 仍"无效"。KEY 埋点取证：**所有键都到达页面且 targets 非空** → DEL 实际执行了删除（Ctrl+A 全选 397 = 400−3 铁证），但**删除路径 `JustifiedSelection.Remove` 不触发 `SelectionChanged`** → 页面 Selection 缓存停留在已删条目 → 删除零反馈 + Ctrl+C 拿过期列表复制已删文件（异常被 fire-and-forget 吞）+ Ctrl+D/Esc 清空集（Clear 提前 return）全部静默无效。
- 修复：`GallerySelectionService.Remove` 在真移除选中项时触发 `SelectionChanged`（页面实时刷新 Selection、空选择自动退出选择模式）；`CopyItemToClipboardAsync` 改实例方法 + try/catch 弹错误对话框（fire-and-forget 异常不再静默）。KEY 埋点已删。
- **新增长期事实候选**：① WinUI 的 ScrollViewer 默认 IsTabStop=False，`Focus(Programmatic)` 静默失败——凡「把焦点还给 ScrollViewer」的代码必须先确保 IsTabStop=True；② 自建选择服务里，**除页面交互外的任何选中集合变更（如删除联动）都必须触发 SelectionChanged**，否则页面缓存的选择列表过期，后续快捷键全部操作幽灵条目且零反馈；③ fire-and-forget 的复制/删除类操作必须自带 catch 反馈。
- 结果：用户验证除 Ctrl+C 外全部通过（Ctrl+C 用户明确放弃修复）。剩余问题：DEL/Ctrl+D/Esc 后焦点被强设到列表框。
- 焦点还原条件化（1 文件）：`RestoreContentFocus` 加 `IsFocusWithinPage()` 前置判断——焦点仍在本页视觉树内（如刚点过的复选框）时不抢，仅焦点丢失/被框架转移到页面外时还原。判定用 `FocusManager.GotFocus` 全局事件跟踪 `_lastFocusedElement`（**GetFocusedElement 在键处理栈内返回 null 的怪癖不可靠**，KEY 埋点实证）；GotFocus 订阅随 Loaded/Unloaded 对（静态事件持引用）。坑：`FocusManager.GotFocus` 委托是 `EventHandler<FocusManagerGotFocusEventArgs>` 而非 TypedEventHandler（CS0123 会连锁引发 XAML 假错误，先修 CS）。构建 0 警告 0 错误（37s）。
- 结果：**用户验证全部通过**。今日四个修复（等高多选错位、翻页回顶、DEL 焦点链、删除选择失同步 + 条件焦点还原）共 5 个源码文件，未提交。

## 视频播放器 UI 改版（未提交，4 文件）
- 按设计图 + 用户决策实施：底栏改**独立布局行**（常驻、无遮罩、主题背景，画面纵向缩短）、顶栏轻量化（返回 + 「退出播放」双入口，文件名移除，保留覆盖式 3s 自动隐藏）。
- 新增：播放队列（图库双击传当前筛选的视频子集 + 索引；短片页入口单条队列）、四态循环 `PlaybackLoopMode { Off, Shuffle, List, RepeatOne }`（进入 Shuffle 时 Fisher–Yates 打乱且当前项固定头部）、上/下一条（`Position>5s` 先回零）、播完自动连播（**Tick 回调内经 `DispatcherQueue.TryEnqueue` 投递，严禁栈内重入装载链路**）、48px 大圆播放按钮（元素级 Resources 覆盖悬停/按压强调色底）、倍速收进「…」MenuFlyout（RadioMenuFlyoutItem，Opening 时按实际倍率容差刷新勾选）、音量 Flyout（滑块 + 静音 ToggleButton，避免与弹出行为抢点击）。
- 画面旋转：`VideoContainer` + `RotateTransform`（Origin 0.5,0.5），90°/270° 时代码交换容器宽高（W=舞台高/H=舞台宽）并居中，0°/180° 拉伸铺满；切视频（含连播）经 `CurrentItem` 通知复位 0°；纯合成器变换不动解码管线，**旋转态可能脱离 MPO 直扫路径，CPU/GPU 占用待用户实测对比**。
- 键盘（PreviewKeyDown 拦截置 Handled，防空格在聚焦按钮 KeyUp 双重触发）：空格播放/暂停、←/→ 单击 ±10s（KeyUp 300ms 内）、按住 → 临时 3×（VM `BeginTemporaryRate`/`EndTemporaryRate` 恢复用户倍率）、按住 ← 节拍跳转（250ms/-10s，PlaybackRate 不支持负值故两方向不对称）、Esc 两级退出（全屏先退全屏，`MainWindow.IsFullScreen` 新属性）。
- 架构简化收益：底栏常驻后旧「Flyout 暂停自动隐藏」互斥逻辑（_isRateDropDownOpen/_isChromeHovered）整体移除，`IsChromeVisible` 更名 `IsTopBarVisible`；`ChromeVisibilityChanged` 事件名保留。
- 坑：枚举成员命名 `Single` 触发 CA1720（含类型名），改 `RepeatOne`。
- 构建 `-warnaserror` → 0 警告 0 错误。待用户手工验证（含旋转态性能对比）。

## 修复：点击视频闪退（播放器改版引入）
- 现象：改版后双击视频立即闪退（页面首次构造时机）。
- 根因：大圆播放按钮元素级 Resources 引用了 `AccentFillColorDefault/Secondary/Tertiary` 的 **Color 键——WinUI 3 主题字典只有这三个的 `...Brush` 画刷键，Color 键不存在**（仅有 `AccentFillColorDisabled` 一个 Color 键）；ThemeResource 运行时解析失败 → 无堆栈崩溃。框架 generic.xaml 实证（2079-2083 行）。
- 修复：三态底色改用官方系统级 Color 键 `SystemAccentColor / Light1 / Light2`（框架自带画刷正是引用它们，运行时由系统注入）。
- **长期事实候选**：① 元素级 Resources 引用主题色时，画刷键（`...Brush`）与 Color 键是两套键名，WinUI 3 部分 token 只有画刷键——引用前必须查包内 `Themes/generic.xaml` 确认键存在，不能想当然；② 系统级 `SystemAccentColor*` Color 键运行时可用（官方文档列出）。
- 构建 → 0 警告 0 错误（34s）。待用户手工验证打开视频不再闪退。

## 播放器改版第二轮（用户反馈 6 项，未提交）
1. 顶栏整体移除（含自动隐藏机制 `_topBarTimer`/`IsTopBarVisible`/`ChromeVisibilityChanged` 事件），改为**左上角常驻返回按钮**（无遮罩，图标白 60%、悬停 80% 经 PointerEntered/Exited 改 Opacity）；单击画面 toggle/指针唤出逻辑随之删除。
2. 播放模式扩为**五态**：Off(E5E7) → Shuffle(E8B1) → Sequential(EA37 顺序播放，播完即停) → RepeatOne(E8ED) → RepeatList(E8EE)；原 `List` 改名 `RepeatList`（回绕），新增 `Sequential`（可切条目不回绕、播完停）；CanGoPrevious/Next 统一为「仅 Off 禁用」，回绕判定收口 `IsWrapMode` 属性。
3. 进度行改三列：左当前时间 / 中进度条 / 右总时长（VM `CurrentTimeText`/`TotalTimeText` 替代 `PositionText`，经 `OnPositionChanged`/`OnDurationChanged` partial 钩子级联通知）。
4. 图标按钮与大圆播放按钮垂直居中：所有 AppBarButton 加 `VerticalAlignment="Center"`。
5. 更多菜单改 `MenuFlyoutSubItem`「速度」（EC4A 图标）→ 子项 0.25x–2.0x；勾选态刷新改为先取 SubItem 再遍历。
6. 快捷键失效修复：点击画面后焦点丢失（Page 非 TabStop，点击非聚焦元素清焦点）→ PreviewKeyDown 隧道不再经过 Page。**修法：StageRoot `IsTabStop="True"`，点击画面自动聚焦舞台，隧道路由恢复**（同 ScrollViewer IsTabStop 家族问题）。
- 附带：`ChromeVisibilityChanged` 删除后，MainWindow 在 `AttachVideoPage` 内**装载后补一次 `SchedulePassthroughRefresh()`**——调用链上 ApplyViewerChrome 的刷新排在装载前，那一拍 VideoHost.Content 还是空，返回按钮会收不到 Passthrough。
- 坑：删属性后 `OnViewModelPropertyChanged` 里的 `nameof(LoopIconOpacity)` 残留 → CS0103 连锁引发 GalleryPage.xaml 的 WMC0001「Unknown type ThumbnailPresenter」等**假错误（先修 CS 再看 XAML 报错）**。
- 构建 → 0 警告 0 错误（37s）。待用户手工验证。

## 播放器改版第三轮（用户反馈 6 项，未提交）
1. 底栏改**单行三列**：左（⏮/大圆播放/⏭）、中（进度条）、右（时间合并格式 + 播放模式移入右侧 + 旋转/音量/全屏/更多）；`PositionText` 恢复 `0:00 / 0:00` 合并格式（删 CurrentTimeText/TotalTimeText）。
2. 快捷键「进入播放必生效」根因：**OnLoaded 的 Focus 作用在 Page 自身，Page 不可作焦点目标 → Focus 静默失败**（ScrollViewer 同族问题）。修复：聚焦目标改 `StageRoot`（IsTabStop=True），OnLoaded + OpenAsync 末尾（`_isStageLoaded` 门控）双兜底。
3. 播放模式移除 `Off`，四态：Shuffle → **Sequential(默认)** → RepeatOne → RepeatList；CanGoNext 动态（Sequential 队尾禁用），CanGoPrevious 恒 = count>1。
4. 图标按钮统一 `Width="40"`、Spacing 2、大圆 Margin 4。
5. 【音量】【更多】改普通 `Button`（Transparent 背景）——**AppBarButton compact + Flyout 会渲染下拉箭头，无法移除**，普通 Button 挂 `Button.Flyout` 点击自动弹出无箭头。
6. 音量 Flyout 水平布局：静音 ToggleButton（图标 E74F/E767 复用 VolumeGlyph）与滑块同行平齐。
- 构建 → 0 警告 0 错误（41s）。待用户手工验证。

## 播放器改版第四轮（用户反馈，1 文件）
- 播放/暂停按钮弃用 48px 大圆特例，改与其他图标按钮一致 40x40 + 默认圆角，仅单独加强调色背景（`AccentFillColorDefaultBrush` 直接设 Background，悬停/按压仍用 SystemAccentColor Light1/Light2 元素级覆盖，图标显式白色）。
- 用户确认：快捷键已全局激活（MPE `IsTabStop=True` + Tapped 兜底聚焦方案有效）；按住 ←/→ **保持现状**（临时倍速方案未改）。
- 构建 → 0 警告 0 错误（35s）。

## 播放器改版第五轮（图标对齐，1 文件）
- 图标水平不齐的根因：**AppBarButton 模板是「图标在上、标签在下」的垂直布局，IsCompact 隐藏标签后图标仍停靠上方**，与普通 Button（内容真居中）混排必然错位。
- 修复：底栏 7 个图标按钮（上一条/下一条/播放模式/旋转/音量/全屏/更多）全部统一为普通 `Button` + 页面级 `IconButtonStyle`（40x40、透明底、内容居中），图标 FontSize 统一 16，tooltip 改 `ToolTipService.ToolTip` 字面量（动态 tooltip 仍由代码后置设置）。左上角返回按钮保留 AppBarButton（独立无混排）。
- **长期事实候选**：底栏图标按钮禁用 AppBarButton（图标停靠上方不居中），一律普通 Button + 统一样式。
- 构建 → 0 警告 0 错误（35s）。待用户手工验证。

## 播放器改版第六轮（用户反馈 5 项，3 文件）
1. 全屏时隐藏底栏（`IsWindowFullScreen` + InvertedBoolToVisibility）；全屏按钮窗口态 E740【全屏播放】/ 全屏态 E73F【返回窗口】，tooltip 代码切换；**全屏状态真相在 MainWindow**：`ToggleFullScreen` 末尾回推 `videoPage.OnWindowFullScreenChanged(...)`，页面不自持。
2. `IconButtonStyle` 加 `CornerRadius=4`（播放/暂停按钮默认 ControlCornerRadius 即 4，无需设）。
3. MoreButton 加 tooltip【更多选项】。
4. 新快捷键（PreviewKeyDown 统一入口）：F11 全屏、Ctrl+R 旋转、Ctrl+H 播放模式、Ctrl+←/→ 上/下一项（切条目不做长按）；修饰键检测用 `InputKeyboardSource.GetKeyStateForCurrentThread(VirtualKey.Control).HasFlag(CoreVirtualKeyStates.Down)` —— **WinUI 3 的 InputKeyboardSource 没有 GetForCurrentView（那是 UWP API），桌面端只有 GetKeyStateForCurrentThread**（CS0117 踩坑）。
5. 进度条 + 时间合为中列一体的 Grid（ColumnSpacing 8），右列功能组 `Margin="16,0,0,0"` → 组间大间隔。
- 构建 → 0 警告 0 错误（35s）。待用户手工验证。

## 播放器改版第七轮（全屏联动缺陷，2 文件）
- **第五轮方案的两处焦点修复（MPE IsTabStop=True + 舞台 Tapped 兜底聚焦）当时只写在回复里、没落进 XAML**——点击画面命中不可聚焦的 MPE 后焦点悬空 → Esc/F11 失效，正是用户本轮报的问题 1。教训：**方案逐项落地时必须逐条核对 XAML diff，不能只凭回复口径**。
- 全屏态底栏自动隐藏：新增 `_bottomBarTimer`（3s）+ `IsBottomBarVisible` 属性（窗口态恒 true、全屏默认收起、点击画面/移动指针经 `ShowBottomBar` 唤出）；全屏切换后 `StageRoot.Focus` 把焦点拉回舞台。
- 问题 2（时间/⇢/🔊 显示消失但结构完好）：根因未能远程定论（三个元素共同点是 x:Bind OneWay 动态更新），**防御性改为代码驱动**——XAML 去 x:Bind，元素命名（PositionTextBlock/LoopModeIcon/VolumeIcon/MuteToggleIcon），OnLoaded + 对应 PropertyChanged 分支显式赋值。
- 用户改了旋转图标 E7A6→E7AD（尊重保留）。
- 未解：画面中央圆圈感叹图标（疑似 E783 错误提示出现，但视频在播与 HasError 矛盾），待用户消融确认（出现时机：旋转后？切条目后？暂停时？）。
- 构建 → 0 警告 0 错误（37s）。待用户手工验证。

## 播放器改版第八轮（全屏浮层底栏，2 文件）
- 用户确认：中央 ⓘ 已自行消失（瞬态，不处理）。
- 全屏态底栏改**浮层**：窗口态仍占独立行（Auto），全屏时代码 `Grid.SetRow(BottomBar, 0)` + `VerticalAlignment=Bottom` + 占位行高归零——底栏覆盖画面底部；退出恢复。底栏背景改为**主题底色 30% 不透明**（独立 `Border ColumnSpan=3` 承载 `Opacity=0.3`，内容不受影响，主题随动）。
- XAML 坑：Grid 的 `ColumnDefinitions` 属性元素必须位于所有子元素之前，先写 Border 子元素再写 ColumnDefinitions 会报 WMC0035「Children 重复赋值」。
- 构建 → 0 警告 0 错误（33s）。待用户手工验证。

## 播放器改版第九轮（底栏透明度仅限全屏，1 文件微调）
- 用户纠正：上一轮把底栏背景常驻改成 30% 影响了窗口态。修正：`BottomBarBackdrop` 透明度运行时切换——窗口态 1.0（恢复原样）、全屏态 0.3；margin/布局窗口态一律不动。
- 构建 → 0 警告 0 错误（35s）。待用户手工验证。

## 播放器改版第十轮（底栏背景贴边，1 文件）
- 背景「不贴边」根因：`Grid.Padding` 会缩进**所有子元素**（包括 ColumnSpan=3 的背景 Border）→ 白色背景四周露出画面。
- 修复：外层 `BottomBar` Grid 去 Padding（背景 Border 铺满整条、真正贴边），内边距移入内层内容 Grid。**通用教训：需要「背景铺满 + 内容带内边距」时，Padding 必须放内容层，不能放同层 Grid**。
- 构建 → 0 警告 0 错误（31s）。待用户手工验证。
- 结果：**用户确认全部功能验证通过**（视频播放器改版共十轮，改 4 个源文件：VideoPlayerPage.xaml/.cs、VideoPlayerViewModel.cs、MainWindow.xaml.cs，未提交）。

## 浅色模式按钮悬停色统一加深（1 文件 App.xaml）
- 现状：App.xaml 主题字典已覆盖 `ButtonBackgroundPointerOver/Pressed`（浅色 8%/12% 黑、深色 10%/15% 白）。用户反馈浅色下悬停不可见（8% 黑太淡），深色已达标。
- 修复：浅色加深为 **12%（#1F000000）/ 18%（#2E000000）**，并新增覆盖 `ToggleButtonBackgroundPointerOver/Pressed`（键名已查包内 generic.xaml 确认）统一开关类按钮；深色字典不动。
- 构建 → 0 警告 0 错误（32s）。待用户手工验证浅色观感。

## 浅色悬停色第二阶段：机制定位（App.xaml）
- 消融结果：20% 黑仍不可见 + **全应用**所有按钮浅色悬停都不可见 → 不是颜色深浅问题，是**浅色主题下覆盖未生效**；而 Dark 覆盖生效（用户确认深色达标）→ 不对称指向主题字典解析。
- 判定：浅色主题解析先按 **Light 键**命中框架合并字典（XamlControlsResources）的 Light 值直接返回，**不会 fallback 到我们 Default 字典**；Dark 键因我们显式提供而命中。修复：新增显式 `x:Key="Light"` 字典承载 4 个悬停键（与 Default 各一份）。
- **长期事实候选（待用户验证后转正）**：App.xaml 顶层 ThemeDictionaries 只写 `Default` 的浅色覆盖对框架合并字典已定义的系统键**不生效**（Light 先命中框架值跳过 Default fallback），浅色覆盖必须显式写 `Light` 键字典；`Dark` 键直接生效。
- 构建 → 0 警告 0 错误（38s）。待用户手工验证。

## 启动驻留后台：Light 键方案证伪，改代码直写（2 文件）
- 自启 A/B 对照（EnumWindows 按 PID 数可见 WinUI 窗口）：含 Light 字典 → visibleWindows=0（进程活、无崩溃日志、窗口不出）；还原 HEAD → =1。**实锤 `x:Key="Light"` 主题字典导致启动挂起**。
- 教训：① `Process.MainWindowTitle/MainWindowHandle` 对 WinUI 3 恒为空/0，**判窗口显示必须 EnumWindows 按 PID 数可见窗口**；② WinUI 3 的 App.xaml ThemeDictionaries 加 `Light` 键会致启动挂起（机制未深究，方案证伪即弃）。
- 新方案：删 XAML 主题字典浅色覆盖（App.xaml 还原 HEAD），改 `App.ApplyButtonHoverBrushes(bool isDark)` **代码直写资源键**（direct key 优先级最高）：浅色悬停 20%（#33000000）/按下 27%（#44000000）黑，深色保持 10%/15% 白（与原 Dark 覆盖同值）；刷新时机 = OnLaunched 创建窗口前（ISettingsService.Current，未加载即默认值）+ ApplySettings 主题切换 + OnActualThemeChanged 跟随系统反转。画刷按浅深各缓存（同 GetSettingsHoverBrush 模式）。
- ISettingsService 属性名是 `Current` 不是 `Settings`（CS1061 踩坑）。
- 自启回归：窗口显示恢复 ✓（visibleWindows=1）。构建 → 0 警告 0 错误。待用户验证浅色悬停可见性。
- 结果：**用户验证全部通过**（启动正常、浅色悬停可见、深色一致）。20% 偏重 → 回调浅色 **15%（#26000000）/ 20%（#33000000）**。构建 0 警告 0 错误。未提交改动：App.xaml.cs、MainWindow.xaml.cs（2 文件）。
- 追调：15% 仍偏深 → 浅色回调 **12%（#1F000000）/ 15%（#26000000）**。构建 0 警告 0 错误。（注意：早期经主题字典写 12% 时「不可见」的结论作废——当时实为浅色覆盖未生效，代码直写后 12% 是真实可辨的。）
- 定档：用户选 **8%（#14000000）悬停 / 12%（#1F000000）按下**。构建 0 警告 0 错误。
- 再调：用户改选 **6%（#0F000000）悬停 / 8%（#14000000）按下**（与标题栏设置按钮悬停色一致）。构建 0 警告 0 错误。
- 结果：**用户确认观感可以，浅色悬停色问题最终闭环**。

## 应用样式统一管理整理（4 文件）
- 删除分裂的三处维护，收敛为唯一来源：
  1. `App.xaml`：删主题字典中被代码直写遮蔽的死覆盖 `ButtonBackgroundPointerOver/Pressed`（浅/深各一对）；新增语义键 `PixbianButtonHoverBrush / PixbianButtonPressedBrush`（浅 6%/8% 黑、深 10%/15% 白）供**自定义模板按钮**引用。
  2. `App.xaml.cs`：`ApplyButtonHoverBrushes` 整理——系统键（Button/ToggleButton 各二）收为键数组循环，alpha 提为 4 个命名常量（浅 0x0F/0x14、深 0x1A/0x26），删画刷缓存字段（调用频率仅启动+主题切换，无需缓存）。
  3. `MainWindow.xaml/.cs`：设置按钮模板补 `CommonStates` VisualState（Normal/PointerOver/Pressed → HoverBackdrop.Background = 语义键），删手动悬停三件套（`GetSettingsHoverBrush` + 两支画刷缓存 + PointerEntered/Exited 事件 + ActualThemeChanged 中刷新块）。深色悬停 8%→10% 白，与全应用对齐。
- 键策略定论：**框架系统键浅色覆盖不生效（Light 截胡）→ 代码直写；Pixbian* 自定义语义键浅色 fallback 正常 → 走主题字典**。自定义模板按钮引用语义键即可全应用统一。
- 构建 → 0 警告 0 错误（36s）。待用户手工验证（设置按钮悬停/按下、深浅主题切换）。
- 结果：**用户验证通过，样式统一管理闭环**。今日未提交改动：App.xaml、App.xaml.cs、VideoPlayerPage.xaml/.cs、VideoPlayerViewModel.cs、MainWindow.xaml.cs（用户中途已自行提交过一版播放器改动；当前待提交为样式整理相关 4 文件）。

## 移除窗口层 loading 覆盖层（2 文件 MainWindow.xaml/.cs）
- 用户要求：图库查询期间去掉 loading 滑块遮罩，直接进骨架屏。
- 删除：XAML LoadingOverlay Border + LoadingProgressBar；cs 构造订阅（`_gallery.PropertyChanged` 中 IsQuerying 分支 + `SyncLoadingOverlay`）、代理属性 `IsGalleryQuerying`（无外部消费者，grep 证实）、`OnGalleryPropertyChanged`、`SyncLoadingOverlay` 整链。原「必须放窗口层防布局循环」约束随覆盖层删除而失效；GalleryPage 骨架屏逻辑不受影响。
- 构建 → 0 警告 0 错误（32s）。待用户手工验证（切换目录/搜索时直接显示骨架屏、无滑块遮罩）。
- 结果：**用户确认观感可以，浅色悬停色问题最终闭环**。


## 图标速查 HTML：搜索筛选失效修复（1 文件）
- 现象：浏览器中搜索框输入关键字后图标完全不筛选（用户实测"输入没反应"）。
- 根因：CSS 特异性陷阱——`.hidden { display:none }` 与 `.item`/`.grid`/`.section-title` 特异性相同（0,0,1,0）但定义更靠前，被后者 `display:flex/grid` 覆盖，元素带 hidden 类仍可见。
- 定位手段：jsdom 模拟 input 事件确认 JS 筛选逻辑本就正确（search→6、e721→1、无匹配显空态，0 报错），问题纯在 CSS 视觉层；jsdom 不渲染 CSS 故伪装成"逻辑正常"。
- 修复：新增 `.item.hidden/.grid.hidden/.section-title.hidden { display:none }` 提高特异性。
- 涉及文件：docs/Segoe-Fluent-Icons-图标速查与预览.html。

## 图标速查 HTML：搜索栏美化 + 说明块下移（1 文件）
- 搜索栏：输入框外包 `.search-box`（flex 容器，左侧放大镜图标 `&#xE721;` 走 SFI-Preview 字体，圆角 8px，focus-within 高亮描边），`#q` 改为无边框透明、`height:100%`；`.btn` 高度/圆角对齐到 38px/8px。
- 将 `<details class="usage">`（字体要点与 XAML 用法）从 `<header>` 移到 `</footer>` 之后；`.usage` 加 `max-width:1440px; margin:16px auto 0` 与页脚/工具栏对齐。
- 涉及文件：docs/Segoe-Fluent-Icons-图标速查与预览.html。

## 图标速查 HTML：移除「预览尺寸」下拉（1 文件）
- 用户要求默认固定 32px。移除搜索栏的尺寸 `<select id="size">` 及「预览尺寸」标签；`render()` 字号来源由读取下拉改为常量 `const size = 32`，并删除对应的 `size` change 事件绑定（避免删元素后 `getElementById("size")` 为 null 报错）。图标渲染尺寸在 CSS `.glyph` 与 JS 内联 style 均为 32px，保持一致。
- 涉及文件：docs/Segoe-Fluent-Icons-图标速查与预览.html。



