
用户要求"间距与间隔不要调整，将 SelectionHeaderGrid 的 TextBlock 字号缩小"。

## 物理约束（关键认知）
仅缩字号无法令两页头等高的根因：**高度差全部来自固定开销，与字号无关**。
- `HeaderGrid`（`Padding="48,24,48,12"`）：总高 = 24 + 32 + 4 + T + 12 = `72 + T`
- `SelectionHeaderGrid`（Round6 原值 `Margin="16" Padding="24,12"`）：总高 = 16 + 24 + 32 + 4 + T + 24 + 16 = `116 + T`
- 两者差 **44px**，全部是卡片 Margin(上下16×2) + Padding(24 vs 12) 的固定开销；文字字号一行最多减几像素，补不上 44px。
  → "不调间距、只缩字号"在数学上不可能零抖动。

## 执行（仅 GalleryPage.xaml）
- 回退上一轮为对齐高度改的间距：`HeaderGrid` 下 padding 24→12；`SelectionHeaderGrid` `Margin="16,0,16,0" Padding="32,24,32,24"` → 还原 `Margin="16" Padding="24,12"`。
- `SelectionCountText` 主标题 `TitleTextBlockStyle` 加 `FontSize="20"`（Subtitle 级，原约 28）。
- 页名副标题 `BodyTextBlockStyle` 加 `FontSize="12"`（Caption 级，原 14）；主行 `MinHeight=32` 保留（按钮 Height=32 仍撑高，缩主标题字号不减主行高）。
- 注释同步：说明此方案只做视觉减重、固定开销差仍导致切换位移；零抖动须改间距对齐。

## 验证
- `dotnet build ... -warnaserror -clp:WarningsOnly`：**通过**（0 Warning 0 Error）
- 改动 1 个文件，**未提交**。待用户就"零抖动 vs 保留卡片内缩"拍板。

---

# 选择页头严格零抖动（方案 A）+ 需求2「仅复选框进入」

用户最终拍板：**1）严格零抖动（方案 A）**；**2）普通模式点击图片不进选中，仅 hover 点击选择框才选中**。

## 方案 A 落地（GalleryPage.xaml，已构建通过）
- `HeaderGrid`：`Padding="48,24,48,24"`（下 padding 补 24，与选择页头对称）。
- `SelectionHeaderGrid`：`Margin="16,0,16,0" Padding="32,24,32,24"`（上下 Margin 归零 + 上下 Padding=24 → 行盒 = 24+32+4+T+24 = 84+T；左右 16 外缩 + 32 内 pad → 内容左缘 = 48 与内容区对齐；保留卡片浮层感）。
- 两者行盒严格等高、顶部起点一致 → 切换 Row0 高度不变、内容区零位移、零抖动。

## 需求2 的方案试错与结论（重要：避免重蹈）
- 我尝试用**路由事件显式授权**：`Grid.AddHandler(ButtonBase.ClickEvent, handler, true)` 捕获模板内
  `SelectionCheck` 的 Click，再移除 `OnSelectionChanged` 的自动进入分支。
  **失败**：WinUI 3 的 `Microsoft.UI.Xaml.Controls.Primitives.ButtonBase` **没有 `ClickEvent` 静态字段**
  （UWP 那套静态路由事件标识在 WinUI 3 已被移除/重命名）→ `CS0117 'ButtonBase' does not contain 'ClickEvent'`；
  该 C# 错误连锁导致所有 XAML 报 `WMC0001 Unknown type`（程序集未生成，XAML 找不到项目类型）。
  → **结论：WinUI 3 不要用 `ButtonBase.ClickEvent` 静态路由字段；要捕获子控件事件须用实例事件订阅
    （VisualTree 找子 CheckBox 挂 `Click`），但涉及容器复用、复杂度高。**
- **回退到已验证基线**（Round5 起工作、经多轮 20-35s 存活）：
  `OnSelectionChanged` 保留 `!IsSelectionMode && HasSelection → EnterSelectionMode(false)` 自动进入分支；
  `OnItemClick` 在 Extended 模式单击图片时 `SelectedItems.Clear()` 清除副作用。
  机制：**点击图片 → OnItemClick 清选择 → 不进模式**；**hover 点复选框 → IsSelected 绑定变化 →
  SelectionChanged 自动进入模式**。已满足"点击图片不进选中、仅复选框进入"的语义。
  唯一残余风险：若 `SelectionChanged` 先于 `OnItemClick` 触发，图片单击会"模式闪现即退"（最终不卡在选中），
  非停留态。用户若实测到明显闪动，再上"容器级 CheckBox 实例订阅"精确方案。

## 涉及文件
- `src/Pixbian/Views/GalleryPage.xaml`（方案 A 间距/对齐）
- `src/Pixbian/Views/GalleryPage.xaml.cs`（需求2 试错后回退至基线，无净改动）
- **未提交**（改动 < 5 文件）

---

# 真根因修复：单击图片进入选中（ItemClick 先于 SelectionChanged）

用户实测报「单击图片依然进入选中」，推翻上一轮"基线已满足"的判断。

## 真根因（时序反转）
WinUI ListViewBase 单击条目的事件顺序是 **ItemClick 先于 SelectionChanged**。Round5 的修法
（ItemClick 里 `SelectedItems.Clear()`）清的是**尚未加入的空集合**，随后控件默认逻辑照常把
点击项加入选择 → SelectionChanged 时 `HasSelection=true` → 自动进入分支命中 → 进入选择模式
且该项被勾选。逻辑反证也吻合：若时序相反（SC 先），Clear 会让模式"闪现即退"，用户看到的是
闪烁而非"进入选中"——用户报的是后者，故时序为 ItemClick 先。

## 修复（一次性标志，GalleryPage.xaml.cs 单文件 3 处）
1. 新增字段 `_suppressAutoEnterOnce`：ItemClick 打标（注释写明时序原理）。
2. `OnItemClick`（普通模式）：打标 + 保留 `Clear()`（若某时序下 ItemClick 在后则直接生效，双保险）。
3. `OnSelectionChanged` 开头消费标志：`clickedItemBody=true` 时跳过自动进入并 `grid.SelectedItems.Clear()`
   清除点击副作用；清除引发的再次 SC 为空选择、无分支命中，稳定收敛。
   点击复选框不触发 ItemClick（CheckBox 吃掉点击），标志恒 false，进入模式路径不受影响。

## 场景推演（全部闭环）
- 单击图片：ItemClick 打标 → SC 消费标志 → 拦截进入 + 清除 → 普通、无选中 ✓
- hover 点复选框：无 ItemClick → SC 正常进入模式 ✓
- 选择模式点图片：ItemClick 的 `IsSelectionMode` 分支 return（不打标），Multiple 勾选语义保留 ✓
- 残留风险：普通模式选择恒空，点击必产生选择变化 → 标志必被消费，无残留 ✓

## 验证（每项单独列出）
- `dotnet build src/Pixbian/Pixbian.csproj -warnaserror`：**通过**（0 Warning 0 Error）
- `dotnet test Pixbian.sln --no-build`：**165 全通过**（Core 120 ✓ / WebServer 33 ✓ / Imaging 12 ✓）
- 用户实测：待反馈
- 改动 1 个文件，**未提交**

---

# 复选框两档白衬底：浮现 60% 白 / 框上 hover 80% 白

## 需求
hover 图片浮现选择框时衬底 60% 白；鼠标 hover 到选择框本身时加深为 80% 白。

## 实现（2 个文件）
- `App.xaml`：新增 `PixbianItemCheckBackdropSubtle` = `#99FFFFFF`（60% 白；alpha 换算 0.6×255=153=0x99），
  Default/Dark 两主题字典同值（压图不随主题反转，与 `PixbianItemCheckBackdrop` #CCFFFFFF 同列并注释两档语义）。
- `GalleryPage.xaml`：两个容器模板的 `CheckBox.Resources` 中
  `CheckBoxCheckBackgroundFillUnchecked` → `PixbianItemCheckBackdropSubtle`（浮现常态 60% 白）；
  `UncheckedPointerOver` / `UncheckedPressed` 保持 `PixbianItemCheckBackdrop`（hover 框上 80% 白）。
  选中态走默认主题键（蓝勾块），不受影响。

## 验证
- `dotnet build -warnaserror`：**通过**（0 Warning 0 Error）
- 改动 2 个文件，**未提交**（< 5 文件阈值）

---

# 条目按压缩放反馈：按下缩小 / 松开回弹（图片与复选框双入口）

## 需求
单击图片（无论点图片主体还是复选框）都要按压效果：按下缩小、完成点击回弹。

## 关键技术点（为什么不用 XAML VisualState）
点击复选框时 `ButtonBase` 把 `PointerPressed` 标记为 Handled，**容器的 Pressed 视觉态收不到该按下**
——纯 XAML 状态机（模板 Pressed 态加 Scale 动画）只覆盖图片主体入口，覆盖不了复选框。

## 实现（GalleryPage.xaml.cs 单文件，Composition 合成层缩放）
- `SubscribeItemPressFeedback(grid)`：两个 Loaded 处各调用一次，订阅
  `PointerPressed/Released/Canceled/CaptureLost`，**`handledEventsToo: true`** 统一捕获双入口。
- `OnItemPointerPressed`：FindItemContainer 定位条目 → 记录 `_pressedItem` → 缩到 **0.97**（80ms）。
- `OnItemPointerReleased`（三事件共用）：回弹到 **1.0**（150ms）并清空标志。
- `AnimateItemScale`：`ElementCompositionPreview.GetElementVisual(item)`，
  `CenterPoint = 中心`（ActualWidth/2, ActualHeight/2），`CreateScalarKeyFrameAnimation` +
  CubicBezier(0.1,0.9,0.2,1) easeOut，`StartAnimation("Scale.X/Y")`——合成线程执行、不占 UI 线程，
  StartAnimation 自动替换前一动画（快速连点安全）。
- 新 using：System.Numerics、Microsoft.UI.Composition、Microsoft.UI.Xaml.Hosting。

## 重要 API 结论（WinUI 3 路由事件字段存留差异）
- `UIElement.PointerPressedEvent/PointerReleasedEvent/PointerCanceledEvent/PointerCaptureLostEvent`
  **静态字段存在**，`AddHandler(..., handledEventsToo: true)` 是官方支持用法 ✓
- 对比上一轮教训：被移除的是**控件级**路由字段（`ButtonBase.ClickEvent`），
  **UIElement 核心输入事件**的静态字段保留。

## 编辑器教训（replace 锚定）
- 给字段区插入声明时 old_str 只锚了 summary 注释行、new_str 却带上了字段声明 → 与下一行原有声明
  重复（CS0102 风险）。**插入字段应锚定"注释+原声明"整体或其后唯一邻接行**，插入后必须回读验证。

## 验证（每项单独列出）
- `dotnet build -warnaserror`：**通过**（0 Warning 0 Error）
- `dotnet test --no-build`：**165 全通过**（Core 120 ✓ / WebServer 33 ✓ / Imaging 12 ✓）
- 用户实测：待反馈（下压/回弹手感、双入口均生效）
- 改动 1 个文件，**未提交**

---

# 按压反馈第二轮：弹簧动画 + 按住移出不回弹修复

## 用户反馈两点
1. 弹性不够自然、太生硬（原 CubicBezier 关键帧 + 80/150ms 固定时长）
2. Bug：按住左键移到图片外，图片下压后不回弹

## 修复 1：改弹簧物理动画（CreateSpringScalarAnimation）
- 关键帧直线插值「速度突变」是生硬感根源；弹簧动画加速度平滑收敛，手感自然。
- 参数：`FinalValue` + `DampingRatio` + `Period=35ms`。
  按下 `DampingRatio=0.9`（近临界阻尼，下压干脆）；回弹 `DampingRatio=0.5`（欠阻尼产生一次柔和回弹）。
  下压深度保持 0.97。弹簧无 Duration，物理自然收敛（约 200-400ms）。
- API：`visual.Compositor.CreateSpringScalarAnimation()`，构建通过——WinUI 3 存在（WASDK/26100）。

## 修复 2：按住移出不回弹（根因与双保险）
- 根因：按住移出条目后 `PointerReleased` 的冒泡路径不可靠（指针 hover 路由随位置变化、
  ListViewBase 内部捕获等），GridView 可能根本收不到 Released。
- 修法：**照片应用式「移出取消按压」**——
  ① `PointerMoved`（handledEventsToo）差异检测：`FindItemContainer(OriginalSource) != _pressedItem`
  即回弹清空；
  ② `PointerExited` 兜底（指针离开 GridView 可视区后 Move 不再触发）。
  语义升级：移出图片即提前回弹（原行为是等 Released），符合主流图片应用交互。
- 原有 Released/Canceled/CaptureLost 三路兜底保留（点击完成在条目内的正常回弹）。

## 验证（每项单独列出）
- `dotnet build -warnaserror`：**通过**（0 Warning 0 Error）
- `dotnet test --no-build`：**165 全通过**（Core 120 ✓ / WebServer 33 ✓ / Imaging 12 ✓）
- 用户实测：待反馈
- 改动 1 个文件，**未提交**

---

# 按压反馈第三轮：弹感幅度收敛

- 发现用户已自行把回弹阻尼 0.5 → 0.35（方向记反了：阻尼越小过冲越大，弹感反而更强）。
- 修法：`ReboundSpringDamping 0.35 → 0.7`——仍欠阻尼（<1）保留一次轻柔回弹，过冲明显收敛。
- 阻尼比与弹感的关系（备忘）：0=持续振荡，<1 欠阻尼（值越小过冲越大），1=临界（无回弹）。
- 构建 -warnaserror 通过；改动 1 个文件，未提交。

---

# 按压反馈第四轮：下压加深 + 首次进选择模式卡顿缓解

## 用户反馈
1. 下压幅度要更大 → `PressedScale 0.97 → 0.94`。
2. 第一次点击复选框并选中时有明显卡顿。

## 卡顿根因分析
首次进入选择模式是一轮**布局风暴**：所有可见条目的复选框 Collapsed→Visible（模板首次参与布局）
+ 页头整行替换（HeaderGrid↔SelectionHeaderGrid）+ SelectionMode 切换的容器选择视觉刷新。
`RestoreContentFocus()` 的 Programmatic 焦点在同帧叠加**焦点遍历 + 同步布局**，放大卡顿。
弹簧动画在合成线程，与此无关。

## 修复
`EnterSelectionMode` 末尾 `RestoreContentFocus()` 改为 `DispatcherQueue.TryEnqueue(() => ...)` 延后一帧，
让布局风暴独占一帧完成、焦点遍历错峰（页面级 TryEnqueue 不需新 using）。

## 备选方案（若仍卡，未做）
复选框改「常驻布局 + Opacity/IsHitTestVisible 切换」替代 Collapsed/Visible，可消除进入模式的
布局冲击——但需新 bool→Opacity 转换器并改模板 VisualState，改动大；先验证延帧效果再定。

## 验证
- `dotnet build -warnaserror`：**通过**（0 Warning 0 Error）
- `dotnet test --no-build`：**165 全通过**（Core 120 ✓ / WebServer 33 ✓ / Imaging 12 ✓）
- 改动 1 个文件，**未提交**

---

# 卡顿根治：复选框常驻视觉树（Collapsed → Opacity/IsHitTestVisible）

延帧焦点后用户仍报卡顿 → 布局风暴主体（数百可见条目的复选框 Collapsed↔Visible 全量重测量排列）
才是瓶颈，Layout 成本无法靠错峰消除。

## 核心改造（WinUI 性能惯用法）
复选框不再 Collapsed/Visible，改**常驻视觉树 + 渲染属性切换**：
- `Opacity 0↔1`（绑 IsSelectionMode，经新转换器）——Opacity 是合成层属性，切换零 Measure/Arrange 成本；
- `IsHitTestVisible false↔true`（绑 IsSelectionMode）——防 Opacity=0 时命中穿透（框看不见却能被点）；
- PointerOver VisualState Setter 覆盖 `Opacity=1` + `IsHitTestVisible=True`（State Setter 优先于本地
  绑定值，既有已验证模式）：普通模式 hover 浮现且可点、选择模式绑定值本为 1/True 无副作用。
- 新增 `Converters/BoolToValueConverter`：bool→string 配置值按绑定目标类型（double/bool）显式转换
  ——XAML attribute 配置的 TrueValue/FalseValue 恒为字符串，靠绑定引擎隐转会静默失败；
  Page.Resources 注册 `BoolToSelectionOpacity`（1/0）与 `BoolToSelectionHitTest`（True/False）两实例。

## 效果
进入/退出选择模式从「数百条目全量重布局」降为「一次批量合成属性变更」——布局风暴消失。
页头整行替换与 SelectionMode 切换的小规模布局仍在（条目级成本已消除）。

## 备忘
- 常驻复选框参与 Grid 测量（~32px），当前条目最小档远大于此，无撑大风险；未来若支持 <40px 微缩略图需复核。
- Opacity 0 + IsHitTestVisible=false 的元素不响应指针，与 Collapsed 行为一致。

## 验证（每项单独列出）
- `dotnet build -warnaserror`：**通过**（0 Warning 0 Error）
- `dotnet test --no-build`：**165 全通过**（Core 120 ✓ / WebServer 33 ✓ / Imaging 12 ✓）
- 改动 3 个文件（新转换器 + GalleryPage.xaml 两模板 + 资源注册），**未提交**
- 用户实测：待反馈

---

# Git 提交：会话全部改动

`feat(gallery): 选择模式交互打磨与按压缩放反馈`（commit `f6901ea`，9 文件 +1378/-288），
涵盖本会话累积工作：选择工具栏卡片化与零抖动页头、两档白衬底、复选框常驻视觉树、
单击防误选、按压缩放反馈与移出取消、删除进度/结果与分页游标。
工作区已干净。

**操作教训（复用）**：含中文的 `git commit -m` 直接进 PowerShell 终端会 GBK 乱码解析失败
（与既有「终端中文命令会语法错误」一致）；正确做法是 write_to_file 写 UTF-8 提交信息文件
（放 %TEMP%，勿放工作区免被 add 误收）+ `git commit -F <file>`。

---

# 左栏重组：分组导航 + 添加文件夹/刷新分类按钮 + 删除发现功能

## 需求与决策（用户四问确认）
1. 左栏重排：收藏夹/片段 + 分隔线 + 分类组[刷新] + 分隔线 + 图库组[添加文件夹]（参考照片应用）
2. 标题栏「导入」按钮与底部「设置」导航项删除（设置入口仅剩标题栏齿轮）
3. 「发现」全链路删除（UI+VM+Core 服务+测试）
4. 分类标题不可选中（`SelectsOnInvoked=False`，包内元数据验证存在）；子项点击过滤图库；
   刷新分类=批量重新匹配；CategoryPage 移入设置页 Expander 卡片

## 关键实现
- `MediaQuery` 加 `DirectoryPath`；仓储 `QueryAsync` 与 `CountByQueryAsync` **同步**加
  `(@dir IS NULL OR directory = @dir OR directory LIKE @dirPrefix ESCAPE '\')`——两处 WHERE 必须逐字一致
  （页头统计与列表同源）；前缀经既有 `EscapeLikePattern` 转义，提炼 `BuildDirectoryFilter` 供两方法共用
- GalleryViewModel：新增 `_categoryFilter/_categoryName/_directoryPath/_directoryName`；
  `ApplyMediaFolderFilterAsync` / `ApplyCategoryFilterAsync`；`ApplyNavigationFilterAsync`（根视图语义）统一清空二者；
  PageTitle/PageIcon 优先显示过滤名（目录与分类过滤均归 Folder 图标）
- MainWindow：分组标题用元素语法 Content（Grid：* 文本 + Auto 按钮）；子项动态重建，
  Tag 前缀 `media-folder:{id}` / `category:{id}`（`long.TryParse(tag.AsSpan(prefix.Length))` 解析）；
  扫描源与分类集合的 `CollectionChanged` 驱动子项自动同步（设置页增删→左栏跟随）；
  被移除子项正被选中→回落图库根视图；`RestoreSelection` 按 Tag 恢复选中
- 添加文件夹：MainWindow 直接 `new FolderPicker(AppWindow.Id)`，仅 `AddFolderCommand` 不索引
- 刷新分类：`ApplyRulesCommand` + 期间 `IsEnabled=false` 防重入；完成后 `_activeCategoryFilter` 非 null 时重放过滤
- 启动默认选中按 Tag `FindNavItem("AllPhotos")`（收藏夹排首位后 `MenuItems[0]` 不再是图库）；
  InitializeAsync 先 `_settings.LoadCommand`（填充左栏子项）再 `_gallery.ReloadCommand`
- SettingsPage：构造注入 CategoryPage；Expander `Expanding` 时 `CategoryHost.Content ??= _categoryPage`
  懒装载（CategoryPage.Loaded 自动 LoadAsync）；CategoryPage 根 `Padding="24,16"` 移除（避免宿主双内边距）

## 验证（每项单独列出）
- `dotnet build Pixbian.sln -warnaserror`：**通过**（0 Warning 0 Error）
- `dotnet test Pixbian.sln`：**通过**
  - Core.Tests：**114/114**（原 120 − DiscoverServiceTests 8 + 新增 QueryAsync 目录过滤 2）
  - WebServer.Tests：**33/33**
  - Imaging.Tests：**12/12**
  - 新基线共 **159**（原 165）
- `read_lints`（6 个改动文件）：**0 诊断**
- 启动目测：应用已启动，左栏结构/按钮/过滤待用户确认

## 文件清单（修改 11 + 删除 5）
修改：MainWindow.xaml、MainWindow.xaml.cs、ShellViewModel.cs、GalleryViewModel.cs、
AppSettings.cs（MediaQuery）、SqliteMediaItemRepository.cs、App.xaml.cs、SettingsPage.xaml、
SettingsPage.xaml.cs、CategoryPage.xaml、SqliteMediaItemRepositoryTests.cs（+2 用例）
删除：DiscoverPage.xaml(.cs)、DiscoverViewModel.cs、DiscoverService.cs、DiscoverServiceTests.cs
**未提交**（16 个文件 > 5 阈值，已按规则询问用户是否提交）

