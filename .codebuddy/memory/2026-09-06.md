# 2026-09-06

## 图库条目收藏交互（GalleryPage）
- 需求：条目右下角收藏按钮（空心 \uEB51 白 80% / 实心 \uEB52 红 80%），双击图片切换收藏并播动画，收藏列表可见。
- 方案经用户确认：单击延迟 300ms 打开查看器（双击取消）、按钮单击即切换、动画为弹跳缩放（1→1.4 回落 + 透明度 0.4→1，约 320ms）。
- `MediaItemViewModel`：新增 `IsFavorite` 可通知属性、`FavoriteGlyph`/`FavoriteBrush`（静态 SolidColorBrush）、`SetFavorite()` 原位更新（`Item` 改 private set）。
- `GalleryViewModel.ToggleFavoriteAsync`：由「新建实例替换集合项」改为原位 `SetFavorite`（避免缩略图重载/动画中断）；收藏夹视图下取消收藏原地移除条目；删除了替换实例后的 NeedsDimensionProbe 预取尾块。
- `GalleryPage.xaml`：两个条目模板（方形/自适应）右下角插入收藏按钮（`ItemFavoriteButtonStyle`，`x:Name="FavoriteButton"`），两个 GridView 挂 `DoubleTapped`。
- `GalleryPage.xaml.cs`：`OnItemTapped` 改延迟定时器（`ScheduleOpenViewer`/`StopPendingOpen`）；`OnItemDoubleTapped`（双击抑制窗口 400ms 防框架漏出的第二 Tapped 误开查看器）；按钮 Click 300ms 去抖防双击来回切换；按钮 DoubleTapped 就地 Handled 防冒泡重复切换；`PlayFavoritePopAnimation` 代码后置现场 Storyboard（HoldEnd 复位起始值）；`FindDescendantByName` 视觉树辅助。
- 坑：页面内裸 `DispatcherQueue.GetForCurrentThread()` 会解析到 `DependencyObject.DispatcherQueue` 实例属性（CS0176），直接用 `DispatcherQueue.CreateTimer()`；`DispatcherQueueTimer` 字段类型须全限定 `Microsoft.UI.Dispatching.`（`using Windows.System` 抢先解析）；目标类型 `new(...)` 对 `Brush` 字段不会推断到 SolidColorBrush（CS1729）。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误；lint 无诊断。
- 追加调整：收藏图标两态画刷透明度由 80%（0xCC）改为 60%（0x99），用户指定。
- 涉及文件：`src/Pixbian/Views/GalleryPage.xaml`、`src/Pixbian/Views/GalleryPage.xaml.cs`、`src/Pixbian/ViewModels/MediaItemViewModel.cs`、`src/Pixbian/ViewModels/GalleryViewModel.cs`。

## 方形网格图片显示方式修正（GalleryPage）
- 需求：方形显示模式（`MediaItemTemplate`）图片不应缩放自适应，应按图片当前尺寸显示并宽高双向居中。
- 实现：`ThumbnailPresenter` 新增 `Stretch` 依赖属性（默认 `Uniform`，自适应视图不变）；`ApplyStretch()` 同步 ImageBrush 的 `Stretch` 与 `AlignmentX/AlignmentY=Center`（TileBrush 对齐默认 Left/Top，None 模式必须显式居中）；`GalleryPage.xaml` 方形模板设 `Stretch="None"`，图片超出格子的部分由 Border 边界自然裁剪。
- 验证：`dotnet build` 0 警告 0 错误。
- 涉及文件：`src/Pixbian/Controls/ThumbnailPresenter.cs`、`src/Pixbian/Views/GalleryPage.xaml`。

## 方形网格改为行填满布局（GalleryPage）
- 需求：方形模式格子不拘泥固定尺寸，每行填满内容区宽度，条目保持 1:1；图片内容显示维持上轮 `Stretch=None`（原尺寸居中，经确认）。
- 根因：`ItemsWrapGrid` 固定 `ItemWidth/ItemHeight`，行末残留空隙且无 `Spacing`，无法精确填满行宽。
- 实现：新增 `SquarePanel`（`JustifiedPanel` 的正方形简化版）——测量时按可用宽与目标边长（`GridItemWidth` = 档位 + 间距）四舍五入推导每行个数，`edge = (可用宽 − Spacing×(n−1)) / n` 均匀放大填满行宽；回写 `SetDisplaySize(edge, edge)`（VM 容差去重防反复解码）；测量行参数缓存供 Arrange 复用。`GalleryPage.xaml` 方形视图 `ItemsPanelTemplate` 换 `SquarePanel ItemSize="{Binding GridItemWidth}" Spacing="8"`，`MediaItemTemplate` Border 去 `Margin="0,0,8,8"`，删孤立 `xmlns:winui`。
- code-behind 未改：`OnContainerContentChanging` 的首帧回写 `(ThumbnailSize, ThumbnailSize)` 仍作正方形兜底，面板精确回写与档位量化下基本不触发多余重解码。
- 权衡：`SquarePanel` 不做 UI 虚拟化（与 `JustifiedPanel` 同一权衡，靠分页增量加载控制规模）。
- 验证：`dotnet build` 0 警告 0 错误。
- 涉及文件：`src/Pixbian/Controls/SquarePanel.cs`（新增）、`src/Pixbian/Views/GalleryPage.xaml`。

## 方形模式骨架屏修正（GalleryPage）
- 需求：方形模式骨架屏不对——骨架仍按图片宽高比 Uniform 贴合格子，非 1:1 图片的骨架缩成条状，与方形网格观感不符。
- 实现：`ThumbnailPresenter` 新增 `SkeletonFillsContainer` 属性（默认 false 保持自适应视图行为）；`UpdateSkeletonBounds` 为 true 时直接填满容器；回调统一为 `OnSkeletonBoundsPropertyChanged`（AspectRatio 与该属性共用触发骨架重算）。`GalleryPage.xaml` 方形模板设 `SkeletonFillsContainer="True"`。
- 涉及文件：`src/Pixbian/Controls/ThumbnailPresenter.cs`、`src/Pixbian/Views/GalleryPage.xaml`。

## 设置页「幻灯片」独立分区 + 播放顺序设置接入放映
- 需求：把「外观」下的幻灯片间隔与切换模式拆为独立【幻灯片】分区，新增「播放顺序（列表/随机）」，并让这些设置真正作用于幻灯片放映。
- 方案经用户确认（随机策略选「洗牌轮播」：一轮不重复、走完重洗、可一直循环）。
- 新增枚举 `SlideShowPlayOrder{List,Random}` 与设置项 `AppSettings.SlideShowOrder`（默认 List）；`SettingsService.Normalize` 补枚举合法性回退。
- `SettingsPage.xaml`：外观卡片只留「应用主题」；新增【幻灯片】分区（间隔 / 播放顺序 / 切换模式，播放顺序图标 E8B1），分区编号顺延（⑤ 幻灯片、⑥ 局域网访问）。
- `ImageViewerViewModel`：`ApplySettings` 接收播放顺序；随机模式走独立 tick 分支（不看 `CanGoNext`，循环播放），列表模式保持「到末尾停止」；洗牌序列 `_shuffleOrder` 以当前条目为首位，游标失效（换列表/手动翻页）或走完一轮即 `ResetShuffleOrder`；起播与设置变更时各重洗一次。
- 设置生效链路沿用既有 `MainWindow.ApplySettings → _viewer.ApplySettings`，无新增入口。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误；测试 181 全通过（Core 136 / WebServer 33 / Imaging 12，与基线一致）。
- 涉及文件：`src/Pixbian.Core/Models/AppSettings.cs`、`src/Pixbian.Core/Services/SettingsService.cs`、`src/Pixbian/ViewModels/SettingsViewModel.cs`、`src/Pixbian/Views/SettingsPage.xaml`、`src/Pixbian/Views/SettingsPage.xaml.cs`、`src/Pixbian/ViewModels/ImageViewerViewModel.cs`、`src/Pixbian/Views/MainWindow.xaml.cs`。

## 设置页分区间距：标题与卡片成组
- 需求：分区标题与下方卡片之间的空白过大。成因是 `ContentRoot` 的 `Spacing=20` 对「卡片→标题」与「标题→卡片」一视同仁，标题两侧等距 → 分组感不成立。
- 方案（用户选）：每个「标题 + 卡片」包一层 `StackPanel Spacing="8"`，标题 `Margin` 由 `0,8,0,0` 改为 `0,4,0,0`；外层 `Spacing=20` 不动。结果：组内 8、组间 24（20+4）。③ 分类管理是 Expander 自带标题，不包裹。
- 顺带修正：分区编号「⑥ 关于」与「⑥ 局域网访问」重号（上一轮改动引入）→ 改为 ⑦。
- 事故与教训：用 PowerShell 按行号批量加缩进 + 插入包裹标签时，脚本按**升序**处理区间，前一组插入 2 行后导致后续区间整体错位（误删 10 行、10 个标签插错位置）；撤销脚本索引再偏差，损坏扩大。**最终整文件重写恢复**（内容此前已全部读过）。详见 MEMORY.md 方法论。
- 恢复时补齐的行：媒体库分区注释与卡片 `</Border>`、分类管理 `</Expander>`、个性化卡片 `</Grid>`、切换模式「淡出」项、访问地址 `</HyperlinkButton>`、隐私副标题 `<TextBlock>` 开始标签。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误（XAML 已编译、x:Bind 与全部 x:Name 校验通过）；lint 0 条。
- 涉及文件：`src/Pixbian/Views/SettingsPage.xaml`。

## 个性化：查看器滚轮行为与缩放首选项
- 需求（仿 Windows 照片）：①鼠标滚轮 = 放大或缩小 / 查看上一项或下一项（Ctrl+滚轮始终缩放）；②缩放首选项 = 缩放以适应窗口 / 按实际大小查看。
- 方案经用户确认：向下滚 = 下一张；「按实际大小」每次切图都应用（与现状每张复位适应窗口一致）。
- 新增枚举 `ViewerWheelMode{Zoom,Navigate}`、`ViewerInitialZoom{FitToWindow,ActualSize}` 与设置项；`Normalize` 补回退；`SettingsViewModel` 两属性；【个性化】卡片加两行（滚轮 E962 / 照片 E8B9）。
- `ImageViewerPage`：滚轮入口按设置分派（`ZoomAtCursor` 抽取；`HandleWheelNavigation` 250ms 节流防触摸板连翻；`IsCtrlPressed` 用 `Microsoft.UI.Input.InputKeyboardSource.GetKeyStateForCurrentThread` + **`Windows.UI.Core.CoreVirtualKeyStates.Down`**——`Microsoft.UI.Core` 不存在）；「按实际大小」在 **`SourceImage` 赋值**（全图就绪）时应用，监听低清预览会算错 100%；100% 计算与双击共用 `GetActualSizeZoom()`。
- 坑：WinUI 3 无 `Microsoft.UI.Core`；CS0103/CS0234 会连锁引发 XAML 假错误（先修 CS）。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误；测试 181 全通过。
- 涉及文件：`src/Pixbian.Core/Models/AppSettings.cs`、`src/Pixbian.Core/Services/SettingsService.cs`、`src/Pixbian/ViewModels/SettingsViewModel.cs`、`src/Pixbian/Views/SettingsPage.xaml`、`src/Pixbian/Views/SettingsPage.xaml.cs`、`src/Pixbian/ViewModels/ImageViewerViewModel.cs`、`src/Pixbian/Views/ImageViewerPage.xaml.cs`。

## 拆分提交
- 用户中途已自行提交幻灯片轮（`2f76f3b`），故最终拆为：`eb9e526`（间距成组 refactor，即那次整文件重建）+ `d800199`（滚轮/缩放首选项 feat）。
- 拆分手法：备份本轮文件到 C:\Temp\pixbian-r2 → replace 撤销本轮改动 → `git checkout` 恢复仅本轮改过的 ImageViewerPage（其本轮前状态=HEAD）→ 提交 → 复制备份回来 → 再提交。
- 坑：①终端传中文 commit 信息会乱码报语法错误 → 用 `git commit -F <UTF-8 文件>`；②首提交信息与实际内容不符（内容实为间距调整）→ `git reset --soft` 重提交修正，零丢失。

## 个性化选项端到端验证 + 实际大小边界修复
- 审计确认三选项（主题 / 滚轮 / 缩放首选项）链路完整：启动 `ApplySettings(_shell.Settings)` + 变更经 `SettingsChanged` 推送。
- 修复真实缺口：「按实际大小」在查看器视口首次布局未完成（ActualWidth=0，缓存命中图片快于布局）时静默丢失 → `ImageHost.SizeChanged` 挂一次性重试（`_initialZoomPendingLayout` 标志防重复挂接）。
- 运行时验证（UIA 导航设置页 + 截屏 + 读 settings.json）：设置页两行显示与图标（E962/E8B9）正确；`viewerWheelMode: navigate`（用户已改）、`viewerInitialZoom: fitToWindow` 落盘与 UI 回显一致 → 存取链路通。
- UIA 验证注意：WinUI NavigationViewItem 可用 InvokePattern/SelectionItemPattern；按 Name 查找可能命中页头按钮（class=Button），需遍历候选；OneDrive 下产物刚落盘立即启动可能闪退，重试即可。
- 涉及文件：`src/Pixbian/Views/ImageViewerPage.xaml.cs`（修复）。

## 查看器交互调整（滚轮工具栏 / 转场复用）
- 需求 1：滚轮（缩放与翻页）不再唤出工具栏——`OnImageHostPointerWheelChanged` 移除 `ShowToolbar()`（连续操作反复弹栏遮挡图像）；拖动、双击等其他交互不受影响。
- 需求 2：确认切换模式（滑动/淡出）本就共用浏览路径——`RequestTransition` 无放映状态条件，手动翻页与放映 tick 走同一 `LoadCurrentAsync`。实测（键盘翻页 + 90ms 连续截帧）捕获到淡出转场中间态（新旧图半透明叠加）✓。
- 运行验证坑：OneDrive 同步会在产物重建后一段时间内反复锁文件 → 应用启动后 7~9 秒被杀（无事件日志、crash.log 无痕、diag.log 有 TICK）；等待 30s 再启动即稳。`mouse_event` 注入滚轮不产生 MouseMove，可隔离验证「滚轮不显示工具栏」；截屏 CopyFromScreen 截屏幕区域，目标窗口被覆盖时会截到别的应用。
- 涉及文件：`src/Pixbian/Views/ImageViewerPage.xaml.cs`。

## 缩放首选项两处修复（用户反馈）
- 「适应窗口不生效」根因：设置变更只更新 VM 属性，当前图不重置——切回适应窗口后当前图保持实际大小，翻页才恢复，观感即「不生效」。修复：页面监听 `ViewerInitialZoom` PropertyChanged，设置变更即时生效（适应窗口 = Pan 归零 + SetZoom(1.0) + ClampPan；实际大小 = 立即对当前图应用）。
- 「实际大小先适应再放大」根因：`LoadCurrentAsync` 无条件 `SetZoom(1.0)`，全图就绪后才应用实际大小。修复：ActualSize 模式装载时跳过 SetZoom(1.0)；`CurrentItem` 变化时用**索引库元数据宽高**（`MediaItem.Width/Height`）预应用 100%（口径与 `GetImageContentRect`/双击一致，未计 EXIF 旋转），全图就绪后精确校正——全程实际尺寸无跳变。
- 运行实测：直接改 settings.json 切换两模式对比截图——fitToWindow 整图高度适配居中；actualSize 仅显示 100% 局部 ✓。OneDrive 锁产物闪退再现，等待 30s 后启动正常。
- 涉及文件：`src/Pixbian/ViewModels/ImageViewerViewModel.cs`、`src/Pixbian/Views/ImageViewerPage.xaml.cs`。

## 主题切换不生效 —— 共同根因（重大）
- 根因：`MainWindow` 只订阅了 `ShellViewModel.SettingsChanged`（图库视图设置链路），**从未订阅设置页的 `SettingsViewModel.SettingsChanged`** → 设置页改的**所有**偏好（主题、幻灯片、滚轮、缩放首选项）只落盘不即时应用，重启才生效。此前「滚轮/缩放首选项不生效」的用户反馈与此同源；当时的运行验证恰好用「改配置文件 + 重启」进行，掩盖了该缺陷。
- 修复：MainWindow 构造补 `_settings.SettingsChanged += OnSettingsChanged`（两条保存链路都汇入 `ApplySettings`）。
- 教训：验证「设置即时生效」类功能必须走真实 UI 路径（改设置→观察），不能用「改配置文件+重启」替代——后者只验证持久化链路，恰好绕开广播订阅。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml.cs`。

## 深色主题使用 dark.jpg（含卡片主题化）
- 需求：深色主题背景用 dark.jpg。直接后果：卡片磨砂白 + 白字压深背景不可读，必须配套主题化。
- 实现：①csproj 把 `Assets\dark.jpg` 加入 Content（PRI 不索引，磁盘路径加载）；②`MainWindow.UpdateWallpaper(actualTheme)` 按实际生效主题换图，`ApplySettings` 与 `Content.ActualThemeChanged`（跟随系统时系统反转）两条路径驱动；③`PixbianContentCard*` 两刷子移入 ThemeDictionaries（浅色磨砂白 #CCFFFFFF/描边 #59CCCCCC，深色磨砂深灰 #CC2B2B2B/描边 #40FFFFFF），3 处引用改 ThemeResource；④LoadingOverlay 由不透明白改为随主题卡片底色。
- 坑：`ActualTheme`/`ActualThemeChanged` 在 FrameworkElement 上，`Window.Content` 声明为 UIElement 须 `is FrameworkElement` 转换。
- 实测：切深色即时生效（背景 dark.jpg + 深灰卡片 + 白字可读），浅色恢复原样。
- 涉及文件：`App.xaml`、`Pixbian.csproj`、`Views/MainWindow.xaml(.cs)`、`Views/GalleryPage.xaml`。

## 图库列表性能诊断与方案（未改代码，仅出方案）
- 现象：列表越加载越卡、资源占用随条目数增长。诊断后落盘 `docs/图库列表性能优化方案.md`（含根因清单、对标分析、执行方案、验收标准）。
- 根因（按贡献排序）：R1 `GalleryViewModel` 翻页时 `pending` 取全集合、与「头部瘦身 300」叠加成自激循环（第 N 页提交约 200N 条解码，**主因**）；R2 两视图零虚拟化（自定义 `Panel` + 外层 ScrollViewer 包裹，后者即使换虚拟化面板也失效）；R3 `CancelOffscreenThumbnails` 全集合 × `ContainerFromItem` 线性查找 = O(n²)；R4 `Diagnostics.Log` 同步 `File.AppendAllText` + 全局锁，每条缩略图写 3 行；R5 `JustifiedPanel` 每次 measure 重建订阅 HashSet 且在 measure 内发起解码；R6 位图按索引回收（300）与 `SizeLimit` 200MB 口径不一且与 R1 互激；R7 集合/订阅无上限。
- 对标结论：主参考 **Windows 照片应用**（同栈同版式同约束，且 `JustifiedPanel` 分行算法可 1:1 保留，只换底座），辅以文件资源管理器的解码优先级队列，思想借鉴 Google Photos；明确不参考 Lightroom（其性能建立在「导入即生成智能预览」的预算上）。
- 待用户拍板：① 是否立即执行 P0 四项；② 诊断日志走「彻底删除」还是「异步改造+默认关闭」（推荐后者）；③ P2 推进节奏（推荐「分两段」：先 P0+P1a+P1b 约 4 天）。
- 涉及文件：`docs/图库列表性能优化方案.md`（新增）。

## 性能优化决策记录（同日追加，未改源码）
- 用户决策：① P0 四项**暂缓**，先自行验证根因；② 诊断日志走 **A（彻底删除）**，与先前推荐的 B（异步改造）相反；③ 整体推进节奏未选。
- 已提示顺序冲突：删除埋点必须在验证**之后**——附录 A 依赖的 `THUMBSUBMIT` / `PANEL` 本身就是 `Diagnostics` 埋的点，先删即无法验证。
- 文档 `docs/图库列表性能优化方案.md` 更新：头部新增「当前状态」区块（含顺序约束与下一步动作）；十一章改为「决策记录」（已定 / 待定 / 候选）；新增十二章「验证实验操作指南（执行前必读）」（脚本参数、操作步骤、判据、R4 的 A/B 方案、验证后执行清单）；附录 A 改为「验证实验原理」。
- 验证脚本落盘 `C:\Temp\Pixbian-Diag.ps1`（纯 ASCII，参数 `-Archive` / `-Submit` / `-Panel` / `-Disk` / `-Summary` / `-Tail N`）。日志路径 `%LOCALAPPDATA%\Pixbian\Logs\diag.log`；启动用根目录 `.\Pixbian.ps1 -Configuration Release`。
- 涉及文件：`docs/图库列表性能优化方案.md`、`C:\Temp\Pixbian-Diag.ps1`（工作区外诊断脚本）。源码**未**改动。

## 深色模式适配三项（标题栏按钮 / 卡片描边 / 视频播放态）
- 需求：①深色下最小化/最大化/关闭按钮不变色；②卡片描边深色值效果不佳；③视频播放时隐藏搜索框，画面用「无圆角、无边框、顶部贴合」的卡片撑满右侧区域。
- 事实核查：`PixbianContentCardBorderBrush` 的深色值本就存在（App.xaml 的 Dark 字典），经确认用户实为「效果不满意」→ 由 25% 白改为 35% 白 `#59FFFFFF`，与浅色 `#59CCCCCC` 同 alpha 保持视觉权重对称。
- 标题栏按钮根因：`ExtendsContentIntoTitleBar=true` 后系统按钮前景色不随应用主题更新 → 新增 `UpdateCaptionButtonColors()`（按钮底色转透明 + 按 `ActualTheme` 取黑/白），挂到「构造后 / `ApplySettings` / `OnActualThemeChanged」三条路径。
- 坑：WASDK 无 `Windows.UI.Colors`（CS0234）→ 用 `Microsoft.UI.Colors`；该 CS 错误连锁引发 20 条 XAML "Unknown type" 假错误，修完 CS 全部消失（再次印证「先修 CS」）。
- 播放态结构（用户确认保留页内 CommandBar、画面撑满右侧）：**不改 `PageHost` 归属**——搬运会触发 `VideoPlayerPage.Unloaded`，而该事件释放 `MediaPlayer`，等于一进入播放就销毁播放器。改为：卡片外观初值入 `ContentCardStyle`（Style Setter，含 ThemeResource），播放态代码后置覆盖圆角/描边/边距/底色四属性，退出时 `ClearValue` 回落（初值若写在元素上，ClearValue 会连 ThemeResource 一起清掉）；底色新增 `PixbianPlayerCardBackground`（纯黑，置于主题字典之外）。
- 播放器页：三行布局改为「舞台单层 + 顶/底覆盖层 + 透明命中层」，控制条隐藏时画面真正撑满；自动隐藏用 `Microsoft.UI.Dispatching.DispatcherQueue.GetForCurrentThread().CreateTimer()`（3s），指针移入舞台或单击画面唤出，指针停在控制条 / 倍速下拉展开期间暂停计时，`Dispose` 停表复位。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误；测试 181 全通过（Core 136 / WebServer 33 / Imaging 12，与基线一致）；lint 0 条。未做运行时截屏验证（需用户手动确认观感）。
- 涉及文件：`src/Pixbian/App.xaml`、`src/Pixbian/Views/MainWindow.xaml`、`src/Pixbian/Views/MainWindow.xaml.cs`、`src/Pixbian/Views/VideoPlayerPage.xaml`、`src/Pixbian/Views/VideoPlayerPage.xaml.cs`。

## 播放态改为窗口级独立分支（方案 B，重构上一轮）
- 需求变更：上一轮「单卡片 + 代码后置切换外观」改为窗口级独立分支——`PlayerRoot`（RowSpan=2）覆盖标题栏行与内容行，播放时隐藏 `TitleBar` 与 `NavigationView`，画面顶到窗口最上沿，顶栏与系统按钮同排。
- 结构：内容区卡片外观属性写回元素内联（撤掉 `ContentCardStyle` 这层间接，播放态不再改写外观）；新增 `PlayerRoot` + 专属宿主 `VideoHost`；播放器页不再进出 `PageHost`（单例页面两个宿主并存、互不争抢父容器）。
- 三条硬约束（已写进代码注释）：①**先显示容器再装载页面**（往 Collapsed 容器塞 Content 不触发 Loaded，页面初始化被跳过）；②**卸载必须 `VideoHost.Content = null`**（只切 Collapsed 不触发 Unloaded → MediaPlayer 不释放 → 解码器不回收）；③顶栏落在非客户区，返回按钮须登记 Passthrough，且只放行按钮不放行整条顶栏（否则顶栏空白区不能拖拽窗口）。
- 联动补充：控制条自动隐藏会让放行矩形失效 → `VideoPlayerPage.ChromeVisibilityChanged` 事件 + 主窗口延一帧重算（`DispatcherQueue.TryEnqueue(Normal, …)`）；播放态系统按钮前景恒白（舞台恒暗，浅色主题下按主题取黑会黑字压黑底）；顶栏 CommandBar 右留 140px 避让系统按钮、背景由外层 Border 铺满整宽；顶栏不挂 PointerEntered/Exited（非客户区收不到）；播放态分支切换后同步 `UpdateCaptionButtonColors()`。
- 上一轮遗留的 `_playerCardBackground` 字段与构造期取值已删除（底色改由 XAML 静态引用 `PixbianPlayerCardBackground`）。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误；测试 181 全通过（Core 136 / WebServer 33 / Imaging 12）；lint 0 条。未做运行时验证。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml`、`src/Pixbian/Views/MainWindow.xaml.cs`、`src/Pixbian/Views/VideoPlayerPage.xaml`、`src/Pixbian/Views/VideoPlayerPage.xaml.cs`。

## 播放 4K 视频 CPU 高（定位 + 修复，待用户实测）
- 现象与判据（用户提供）：播放 2K–4K 时 CPU 持续 60%+；系统「电影和电视」播同一文件**明显更低** → 排除「解码本身/文件问题」，锁定**应用侧呈现路径**；控制条自动隐藏后 CPU 无变化 → 排除控制条（但命中层不随控制条隐藏，未被排除）。
- 结论：主因是覆盖在视频之上的**全屏透明命中层**（上一轮为接收单击而加）——覆盖层会打断硬件覆盖（zero-copy）路径，每帧经合成器混合，4K 下每帧数十 MB 拷贝。
- 修复：①删除铺满画面的透明命中层，`Tapped`/`PointerMoved` 改挂舞台根 `StageRoot`（经冒泡接收，MediaPlayerElement 未启用传输控件时不处理指针）；②播放态一并隐藏被完全遮挡的全窗背景图 `WallpaperImage`（2560 宽解码位图，避免继续参与每帧合成）。
- 风险与回退：若实测「单击画面唤不出控制条」，说明 MediaPlayerElement 吞了指针事件 → 回退为在画面之上保留命中层，但改为只覆盖画面区（不覆盖标题栏那 48px）并接受合成开销，或改用其他命中方案。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误。**CPU 是否下降需用户实测确认**（未做运行时测量）。
- 未做（待实测后再定）：播放期间让后台常驻任务（元数据回填 `StartBackfillResidency`、缩略图解码）让路——若实测 CPU 仍高，用 `C:\Temp\Pixbian-Diag.ps1 -Tail N` 看播放期间 diag.log 是否仍在产出即可判定。
- 涉及文件：`src/Pixbian/Views/VideoPlayerPage.xaml`、`src/Pixbian/Views/MainWindow.xaml.cs`。
- **实测更正（用户反馈「依然 CPU 高」）**：移除透明命中层**未**降低 CPU → 命中层不是主因（先前假设证伪，勿据此继续排查）。追加改动：窗口根 `RootGrid` 加不透明黑底——窗口若存在透明区域，DWM 按带 alpha 窗口处理、全窗每帧参与 alpha 合成，视频拿不到「独立翻转/硬件覆盖」直通路径；壁纸图铺满其上，常态视觉不变。
- 待用户三实验收敛：①播放时点全屏对比 CPU（全屏走硬件覆盖，若大幅下降即确认窗口合成路径是元凶）；②播放时 `powershell -File C:\Temp\Pixbian-Diag.ps1 -Tail 80` 看后台回填/缩略图是否仍在产出；③用 Release 构建（`.\Pixbian.ps1 -Configuration Release`）复测，排除 Debug 构建自身开销。
- 坑复现：构建报 MSB3026（Pixbian.exe 被运行中的进程锁住）→ 须先结束进程再构建（本轮已由用户批准后结束 PID 13720）。
- 涉及文件（追加）：`src/Pixbian/Views/MainWindow.xaml`。
- **【最终定论】播放 CPU 高 = 文件软解，非应用问题**：用户提供系统「电影和电视」播放同一 4K 文件的任务管理器截图——**Video Decode 同样 0%、CPU 94%**（我们 91%），两者持平。此前「系统播放器明显更低」系对比口径误差。根因：该文件编码不受 UHD 630 硬解支持（AV1/VP9 10bit/HEVC 4:2:2 等），或缺 HEVC 解码扩展。处置走环境侧：装「HEVC 视频扩展」或 HandBrake 转码 H.264/HEVC 8bit。应用侧三处优化（移除透明命中层、隐藏壁纸、窗口根不透明底）保留；**单击画面是否仍能唤出控制条待用户确认**（命中层已删，改为舞台根冒泡接收）。
- **结论二次修正（用户补充：PotPlayer 没这种困扰）**：系统播放器与我们同为 Media Foundation 管线（91% vs 94%）→ 两者持平只证明"同一管线"，**不代表应用侧无解**。PotPlayer 自带 FFmpeg 解码器 + DXVA/D3D11 硬解，不依赖系统 MFT，所以快。Windows 默认不提供 HEVC 解码器（需装「HEVC 视频扩展」）。
- 三条处置（代价递增）：①装/换 HEVC 扩展（仅 HEVC 有效、零代码）；②`MediaSource.CreateFromUri` 替换 `CreateFromStorageFile` 做 A/B（1 行）；③引入 FFmpegInteropX 换解码后端（只换解码，MediaPlayerElement 呈现与界面不变；代价包体 +数十 MB、LGPL 合规）。
- 前提与待确认：拿到视频编码（项目已能读 `VideoMetadata.VideoCodec`，取自 MediaClip 的 Subtype，界面未展示）；用户是否接受引入 FFmpeg 依赖（产品决策）。
## 引入 FFmpegInteropX 换解码后端（方案③ 落地）
- 决策：视频为 **AV1**、UHD 630 无硬解 → 用户确认接受引入 FFmpeg（+数十 MB 包体、LGPL）。
- 依赖核实（NuGet 官方页，非凭记忆）：FFmpegInteropX 2.1.0.81200，库 Apache-2.0，支持 WinUI 3 / 桌面，资产 net6.0-windows10.0.17763；ffmpeg 二进制 LGPL-2.1-or-later；内置 D3D11 硬解（H264/HEVC/AV1/VP9 自动探测）+ dav1d AV1 软解。
- 实现：`VideoPlayerViewModel` 新增 `CreatePlaybackItemAsync`——`FFmpegMediaSource.CreateFromStreamAsync` + `CreateMediaPlaybackItem`，失败回退 `MediaSource.CreateFromStorageFile`；`_ffmpegSource` 字段强引用（防 GC 中断播放），`Dispose` 时释放。
- 踩坑四条（均已在 csproj 注释）：①CsWinRT 报 `Platform.xml 10.0.26100.0` 缺失（本机只装 19041）→ 用 `CsWinRTWindowsMetadata` 指向 19041（`TargetPlatformVersion` 被 TFM 覆盖、无效）；②新报 `CsWinRT1028`（6 个自定义 WinRT 类未加 partial）→ 未启用 AOT，NoWarn 豁免并注明启用 AOT 时需补齐；③RID 为空导致 ffmpeg 原生库未复制（运行期静默回退）→ 加条件化 `RuntimeIdentifier=win-x64`；④RID 使产物落到 `win-x64` 子目录 → `Pixbian.ps1` 硬编码路径同步。
- 合规：`docs/THIRD-PARTY-NOTICES.md`（新增，含动态链接/可替换/源码获取说明）+ 设置页「关于」一行归属声明。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误；测试 181 全通过；输出目录已含 avcodec-62/avformat-62/avutil-60/swscale-9/FFmpegInteropX.dll 等。**CPU 是否下降需用户实测**。
- 涉及文件：`src/Pixbian/Pixbian.csproj`、`src/Pixbian/ViewModels/VideoPlayerViewModel.cs`、`Pixbian.ps1`、`src/Pixbian/Views/SettingsPage.xaml`、`docs/THIRD-PARTY-NOTICES.md`（新增）。
- **实测反馈：AV1 CPU 仍接近 100%** → 定位到默认 `VideoDecoderMode=AutomaticSystemDecoder` 会优先系统解码器，AV1 下系统解码器能播就不会用 FFmpeg（换后端等于没换）。修复：按编码判断，AV1 用 `ForceFFmpegSoftwareDecoder`（dav1d），其余保持自动（保住硬解）；并显式 `MaxDecoderThreads = Environment.ProcessorCount`（FFmpeg 默认不放开多线程）。
- 两条 API 事实（编译器实测）：配置挂在 `MediaSourceConfig.Video`（`VideoConfig`）上，`MediaSourceConfig` 本身没有这些属性；`MaxDecoderThreads` 是 `uint`，需显式转换。
- 新增一次性埋点 `VIDEODEC|codec=..|mode=..|threads=..`（回退时记 `fallback=system`），用于确认实际生效的解码后端；查法：`Get-Content "$env:LOCALAPPDATA\Pixbian\Logs\diag.log" -Tail 20 | Select-String VIDEODEC`。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误。CPU 是否下降待用户实测。
- 涉及文件（本轮）：`src/Pixbian/ViewModels/VideoPlayerViewModel.cs`。
- **验证结论（三项证据齐全，FFmpeg 后端确认生效）**：进程路径 `...\win-x64\Pixbian.exe`；已加载模块含 `avcodec-62.dll / avformat-62.dll / avutil-60.dll / FFmpegInteropX.dll`；日志 `VIDEODEC|codec=av1|mode=ForceFFmpegSoftwareDecoder|threads=6`（编码识别、强制软解、线程数均正确）。
- **重要陷阱（务必记住）**：给项目加 RID 后产物移到 `win-x64` 子目录，**旧 exe 仍留在原目录且可直接双击启动**——跑旧产物时缺 ffmpeg 原生库、会静默回退系统解码，表现就是"改动似乎完全没效果"。今后排查"改动没生效"第一件事应确认进程路径（`(Get-Process Pixbian).Path`），确认 ffmpeg 是否加载可用模块列表（`(Get-Process Pixbian).Modules` 过滤 avcodec/FFmpegInteropX）。
- 现状：dav1d 6 线程软解 4K AV1 会跑满多核（任务管理器 100% 属预期），用户反馈播放流畅；要真正降低占用只能把片源转码为 H.264 / HEVC 8bit 走硬解。

## 左栏「图库」文件夹列表默认展开
- 需求：启动后左栏图库分组直接展开，无需手动点箭头。
- 坑（关键）：NavigationView 把层级子项扁平进同一个列表，且**只在父项 `IsExpanded` 值发生变化时重算**。仅靠 XAML `IsExpanded="True"` 无效——子项是运行时（扫描源加载后）才填充的，值一直为 true 不会触发重算，表现为「箭头朝上（已展开）但子项一行都不显示」。
- 修复：`RefreshLibraryFolderItems` 重建子项前先 `IsExpanded = false`，填完再置 `true`，强制值变化。
- 排查手法：UIA 枚举 `ControlType.ListItem` 输出 Name + y 坐标，比截图更快判定子项是否真的渲染（WinUI TextBlock 不暴露 Name，但 NavigationViewItem 暴露）。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误；UIA 枚举到 `youtube / 新建文件夹 (2) / 新建文件夹` 三个子项排在「图库」之后，截图目视一致。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml`、`src/Pixbian/Views/MainWindow.xaml.cs`。

## 左栏交互拆分：点图库行只导航，仅右侧箭头切换展开（同日追加）
- 需求：点击【图库】行只显示图库内容，不得连带展开/折叠子文件夹；只有最右侧箭头才切换。
- 事实核查（编译期验证）：`Expanding`/`Collapsed` 是 **NavigationView 级**事件（args 携带 `ExpandingItemContainer`/`CollapsedItemContainer`），挂到 NavigationViewItem 上报 WMC0011；`NavigationViewItem` 无任何控制「点内容是否切换展开」的属性（成员清单已核：IsExpanded/MenuItems/MenuItemsSource/SelectsOnInvoked/HasUnrealizedChildren 等）。
- **关键事实**：①点行内容与点箭头**都会切换展开**；②点箭头**不抛** ItemInvoked，点行才抛；③切换相对 ItemInvoked 的先后顺序不固定（两条路径都要覆盖）；④**在 ItemInvoked/Expanding/Collapsed 回调内同步改 `IsExpanded` 会使进程 fail-fast 退出**（无托管异常、无 crash.log、Windows 事件日志也无记录，唯一线索是 diag.log 心跳在点击时刻中断）——改写必须 `DispatcherQueue.TryEnqueue` 延到下一个消息。
- 实现（三件套，均在 MainWindow）：`PointerPressed`（`AddHandler(..., handledEventsToo: true)`，箭头区按下被项内部标记已处理）按落点距右缘 ≤44px 判定 `_isGalleryChevronClick`；`ItemInvoked` 仅点行时置 `_isGalleryContentClick` 并按「切换前状态」预还原一次；`Expanding/Collapsed`（挂 NavigationView 级）里若 `_isGalleryContentClick` 置起则当场 `RestoreGalleryExpansion(!expanded)` 撤销。还原本身也走 TryEnqueue，且状态未变时不赋值（避免无谓重算子项）。
- 验证：UIA 枚举 + 真实鼠标点击四场景全过——启动展开、点行保持展开、点箭头折叠、折叠态点行保持折叠；`dotnet build -warnaserror` 0 警告。
- 排障注：测试脚本 UIA dump 偶发返回空但进程存活（树刷新时机），须以 `Get-Process` 存活 + 重跑判定，勿当成崩溃。
- 涉及文件：`src/Pixbian/Views/MainWindow.xaml`（NavigationView 挂 Collapsed/Expanding/ItemInvoked）、`src/Pixbian/Views/MainWindow.xaml.cs`（字段 `_isGalleryExpanded/_isGalleryExpandedBeforeToggle/_isSyncingGalleryExpansion/_isGalleryContentClick/_isGalleryChevronClick`、常量 `GalleryChevronHitWidth=44`、方法 `OnNavigationItemExpanding/Collapsed`、`OnGalleryPointerPressed`、`TrackGalleryExpansion`、`OnNavigationItemInvoked`、`RestoreGalleryExpansion`）。

## 视频封面抽帧与画质角标（同日追加）
- 需求：①时长 >2 分钟的视频封面取 20 秒处；②列表角标在时长前显示画质档位（480P/720P/1080P/2K/4K）。
- 抽帧实现：原先视频封面全走系统 `StorageFile.GetThumbnailAsync(ThumbnailMode.SingleItem)`（系统缓存快，但取帧位置不可控）。新增 `TryEncodeCoverFrameAsync`：先用 `MediaDimensionReader.ReadAsync` 判时长（短视频完全不付出抽帧成本），超门槛才建 `MediaClip` + `MediaComposition.GetThumbnailAsync(position, w, h, VideoFramePrecision.NearestFrame)`；尺寸按最长边等比换算；位置钳制到 `clip.OriginalDuration` 内；任何异常静默回退系统缩略图并留 `VIDEOFRAME|fail` 日志。抽帧慢但只首次（磁盘缓存兜底），86 条长视频实测全部 ok（含竖屏 288x512）。
- `MediaComposition.GetThumbnailAsync` 返回的 ImageStream 可直接读字节（经既有 `ReadAllBytesAsync`），与现有「byte[] → UI 线程 SetSource」管线兼容，无需改管线。
- 画质角标：档位按**短边**判定（竖屏 1080×1920 短边 1080 → 1080P）；≥2160=4K、≥1440=2K、≥1080=1080P、≥720=720P、≥480=480P，否则空串。尺寸来源与 DimensionText 一致（预取 `_probe*` 优先于 `Item.Width/Height`），SetDimensions 时补 `OnPropertyChanged(nameof(VideoBadgeText))`。
- XAML 坑：模板内 x:Bind 不能配 StaticResource Converter，无法单独 Visibility 隐藏「无档位」的文本 → 档位与时长合成单个 `VideoBadgeText`（"4K · 2:28"），两个视图模板的角标绑定由 `DurationText` 换成 `VideoBadgeText, Mode=OneWay`（预取尺寸异步到达会变值，OneTime 会停在首帧）。
- 验证：`dotnet build -warnaserror` 0 警告；实拍角标 "4K · 2:28"（>2min，抽帧路径）与 "4K · 1:11"（<2min，系统缩略图路径）均正确。
- 涉及文件：`src/Pixbian/Services/ThumbnailService.cs`、`src/Pixbian/ViewModels/MediaItemViewModel.cs`、`src/Pixbian/Views/GalleryPage.xaml`。
- 追改（同日）：抽帧门槛由 2 分钟下调为 **1 分钟**（`LongVideoCoverThreshold`），抽帧位置仍为 20s。清磁盘缓存后同批视频重跑，触发抽帧的条目由 86 增至 137，全部 ok、无 fail——1~2 分钟区间已纳入。

## 新建 Short 占位页并接管「片段」导航（同日追加）
- 需求：为「片段」单独建页面（命名 Short），先放占位内容；经确认采用「接管片段导航项 + 居中图标/标题/说明」方案。
- 新增：`src/Pixbian/Views/ShortPage.xaml`（根 Grid 占满 + 内层 StackPanel 居中，视频图标 `\uE714` + TitleTextBlockStyle "Short" + 次要说明，下边距 128 与图库空状态观感一致）、`ShortPage.xaml.cs`（仅 InitializeComponent，无业务）。
- 接入：`App.xaml.cs` 注册 `AddSingleton<ShortPage>()`；`MainWindow` 构造注入 `_shortPage`；`ApplyCurrentPage` 的 `NavigationTarget.Videos` 分支改挂 `_shortPage`；`NavigateToTarget` 删除向 `GalleryViewModel` 下发「仅视频」筛选的 case——结果会落在当前不可见的图库页上，回到图库时还得再重置。
- 已知限制（占位阶段）：顶部搜索框对 Videos 目标仍显示「在视频中搜索」，但搜索仍走图库视图模型、结果不可见；接入 Short 数据源时须一并处理搜索下发。
- 验证：`dotnet build -warnaserror` 0 警告；实跑点击「片段」截图确认占位页居中显示。
- 涉及文件：`src/Pixbian/Views/ShortPage.xaml`（新增）、`src/Pixbian/Views/ShortPage.xaml.cs`（新增）、`src/Pixbian/App.xaml.cs`、`src/Pixbian/Views/MainWindow.xaml.cs`。

## Short 页正式开发：随机连播 + 音乐库背景音乐
- 需求（经用户确认）：①随机连播图库全部视频；②≤60s 全片，>60s 随机截 40~80s 且起点不早于 20s，不足 100s 的按「20s 到片尾」的最长计；③无传输控制条；④单击画面 / 空格播放暂停；⑤右下角按钮 + 左右方向键切换下/上一个；⑥右上角「查看原视频」跳完整播放器；⑦左下角静音；⑧无音轨视频随机播放音乐库音频，且音频与视频严格联动（暂停同步、片段结束即结束、切换即换曲、随机选曲）。
- **架构决策（用户追问后定）**：音乐库**独立建表 `music_tracks`**，不复用 `media_items`。理由：音频若进 `media_items`，图库在 `Kind=null`（不过滤类型）时会把音频连同图片视频查出，且会被 `MediaMetadataBackfillService` 反复探测；要规避就得改 `MediaKind` / `MediaFileClassifier` / `BuildFilter` / 回填服务，触及图库核心链路。项目已有 `SchemaMigrations` + `PRAGMA user_version`，加表只需追加一个迁移条目（v5）。代价：不能直接用 `LibraryWatcherService` 监控，改为设置变更时重扫。
- 数据层新增：`AudioFileClassifier`（音频扩展名白名单，仅音乐库用，**禁止并入 `MediaFileClassifier`**）、`MusicTrack` 模型、`IMusicTrackRepository` + `SqliteMusicTrackRepository`（全量替换式对账）、`SchemaV5`（music_tracks 表 + directory 索引）、`AppSettings.MusicLibraryPaths`、`SettingsService.NormalizeMusicPaths`（去空白 / 转绝对路径 / 去尾分隔符 / 大小写无关去重，坏目录只丢自身不整体降级）。
- `MusicLibraryService`（接口与实现同文件，沿用 `SettingsService.cs` 既有风格）：`LoadAsync` 从库恢复到内存（启动毫秒级、不扫盘）、`ScanAsync` 递归枚举 + 全量替换、`TakeRandomTrack` 随机抽曲并避重。扫描走 `Task.Run` 让出 UI 线程（机械盘可达秒级）；用 `Interlocked` 互锁而非 `SemaphoreSlim` 字段（持可释放字段触发 CA1001，在 -warnaserror 下是构建错误）；枚举跳过重解析点并再用 `PathGuard.IsInside` 校验，双重防路径穿越。
- `ShortClipPlanner`（Core 纯函数，可单测）：三档——≤60s 整段；60~100s 自 20s 截到片尾；≥100s 长度在 40~80s 随机、起点在 [20s, 片尾−长度] 随机。随机数由调用方注入以便复现。
- `IVideoPlaybackItemFactory` / `VideoPlaybackItemFactory`（新增于 `Pixbian.Services`）：把 `VideoPlayerViewModel` 里的 FFmpeg 解码策略（AV1 强制软解 + 线程数 + 系统回退）抽出来，播放器页与短片页共用，避免两处漂移。工厂**只能放 UI 项目**——`Pixbian.Media` 刻意不引用 FFmpegInteropX（见其 csproj 注释）。`VideoPlayerViewModel` 改为注入工厂，`_ffmpegSource` 字段换成 `VideoPlaybackItem?`（切换 / 释放时一并 Dispose）。
- `ShortViewModel`：`QueryAsync(Kind=Video, SortKey=Random, 换种子)` 一次取候选——**Data 层零改动**，既有查询已支持按类型 + 随机排序；本地 Fisher-Yates 洗牌，播完一轮重洗；进度用 250ms 轮询判定片段终点（不订阅 PositionChanged，后者会与用户输入打架），时长未知时按整段播、靠 `MediaEnded` 收尾；双 `MediaPlayer`（视频 + 背景音乐）严格联动，`IsMuted` 同时作用于两者。
- `ShortPage`：舞台单层 + 三个覆盖层按钮（右上 `\uE8A7` 查看原视频 / 左下静音图标随 `IsMuted` 变 / 右下 `\uE893` 下一个）。**按钮必须就地拦截 Tapped**（`e.Handled = true`），否则点击冒泡到舞台会顺带触发播放/暂停。键盘事件订阅在**页面自身**而非根网格（键盘事件自焦点元素向上冒泡，焦点在页面时不会下传到子级网格）。主窗口经 `App.Services` 按需解析——直接注入会与「窗口持有本页」形成循环依赖。
- 设置页：新增【音乐库】分区（图标 `\uE189`），沿用行级卡片样式；分区编号顺延为 ④分类管理 / ⑤个性化 / ⑥幻灯片 / ⑦局域网访问 / ⑧关于。`SettingsViewModel` 加 `MusicFolders` / `MusicStatusText` 与 Load / Add / Remove 三个命令，增删后落盘并触发重扫。
- `MainWindow`：抽出 `OpenVideoPlayerAsync(MediaItem)` 供图库双击与短片页共用，新增 `OpenViewerAsync(MediaItem)` 重载。
- 验证：解决方案 `dotnet build -warnaserror` **0 警告 0 错误**；测试 **207 全通过**（Core 162 = 基线 136 + 新增 26 / WebServer 33 / Imaging 12）。新增单测 `ShortClipPlannerTests`（17 项，含 60s 与 100s 两个边界、起点随机性守护）、`MusicLibraryServiceTests`（9 项，真实临时目录 + 手写桩件）。
- 踩坑四条：①`Style.Resources` 在 WinUI 3 不存在（WPF 专有，报 WMC0011）→ 主题键覆盖移到页面级；②`MusicFolderRow` 无属性通知，`x:Bind Mode=OneWay` 触发 WMC1506 → 路径恒定改 `OneTime`；③`GetRequiredService` 需 `using Microsoft.Extensions.DependencyInjection`；④跨项目类型需补 `using Pixbian.Services`（CS0246 会连锁引发 XAML "Unknown type" 假错误，先修 CS 再看 XAML）。
- 涉及文件（新增 9）：`Core/Services/AudioFileClassifier.cs`、`Core/Models/MusicTrack.cs`、`Core/Services/ShortClipPlanner.cs`、`Core/Services/MusicLibraryService.cs`、`Data/Repositories/SqliteMusicTrackRepository.cs`、`Pixbian/Services/VideoPlaybackItemFactory.cs`、`Pixbian/ViewModels/ShortViewModel.cs`、`Core.Tests/ShortClipPlannerTests.cs`、`Core.Tests/MusicLibraryServiceTests.cs`。
- 涉及文件（修改 12）：`Core/Abstractions/Repositories.cs`、`Data/Sqlite/SchemaMigrations.cs`、`Core/Models/AppSettings.cs`、`Core/Services/SettingsService.cs`、`Views/ShortPage.xaml`、`Views/ShortPage.xaml.cs`、`ViewModels/VideoPlayerViewModel.cs`、`ViewModels/SettingsViewModel.cs`、`Views/SettingsPage.xaml`、`Views/SettingsPage.xaml.cs`、`Views/MainWindow.xaml.cs`、`App.xaml.cs`。

## 短片页「无背景音乐」排查与修复
- 现象：用户反馈无音轨视频没有背景音乐。
- 排查（先查数据再动代码）：`settings.json` 的 `musicLibraryPaths` 已配置 `C:\Users\Baiwanyi\Music\bgm` ✓；该目录 44 个文件（21 `.mp3` + 23 `.ogg`）✓；`sqlite3 index.db` 查 `music_tracks` **COUNT = 44**（与目录文件数一致）且 `PRAGMA user_version = 5` ✓ → **排除配置缺失、白名单不覆盖、扫描未执行**三条路径，问题落在运行时播放环节。
- 根因（高概率）：背景音乐原先走系统解码器 `MediaSource.CreateFromStorageFile`；音乐库 44 首中 23 首是 `.ogg`，系统 Media Foundation 对 **Ogg Vorbis 支持不完整**，且失败时 MediaPlayer **只内部报错、不抛同步异常** → 抽到 ogg 即静默无声，抽到 mp3 正常，表现正是「判定该配乐却没声音」。
- 修复：背景音乐统一改走 `IVideoPlaybackItemFactory`（FFmpeg），与视频解码后端保持一致；新增 `_musicPlayback` 字段持有音频的 FFmpeg 源（必须强引用防 GC），在 `StopMusic` 与 `Dispose` 中释放。
- **关键教训（通用）**：音频播放失败不会在调用处抛同步异常，只经 `MediaPlayer.MediaFailed` 上报——**不订阅就完全静默**，且 `try/catch` 包不住。任何「静默无声音」的问题，第一件事是订阅 `MediaFailed` 留痕。
- 新增诊断埋点（diag.log）：`SHORTCLIP|hasAudio=..|codec=..|channels=..|dur=..`（音轨判定与元数据）、`SHORTBGM|pick=none|reason=候选池为空` / `pick=ok|file=..` / `play=fail|..` / `mediafail|error=..`。对照判据：`hasAudio=True` = 视频被判有音轨（按设计不配乐）；`hasAudio=False` 却无 `pick=ok` = 抽曲失败；有 `pick=ok` 无声音 = 解码或输出失败。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误。**需用户实测确认 .ogg 能否出声**。
- 涉及文件：`src/Pixbian/ViewModels/ShortViewModel.cs`。
- **二次定位（用户回传日志，真正的根因）**：日志只有 `SHORTCLIP|hasAudio=False|codec=null|dur=181.7`，**完全没有 SHORTBGM** → `StartMusicAsync` 从未被调用。根因：`_player.PlaybackSession.Position = _clip.Start` 写在 try 块内、且位于 `_player.Play()` **之前**——FFmpeg 源刚挂上时会话尚未就绪，定位会抛 WinRT 异常（`COMException`/`InvalidOperationException`），被外层 catch 一并吞掉，导致 `Play()` 与背景音乐启动**同时中断**；因视频时长 181s 必走截取分支（`Start > 0`），故每条视频都命中。
- 修复：①抽出 `SeekToClipStart()` 独立 try/catch，失败仅记 `SHORTCLIP|seek=fail`，退化为整段播放；②背景音乐启动移到 try/catch/finally **之外**，与视频载入彻底解耦——视频侧异常不再连带吞掉音乐，音乐失败也不影响画面；③catch 补 `SHORTCLIP|load=fail` 留痕。
- **教训（通用）**：try 块内一旦混入「可选步骤」，其失败会连带吞掉后续全部必需步骤，且因被 catch 而完全静默。非关键步骤要么独立 try/catch，要么移出关键路径。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误。**需用户实测**；注意确认进程路径为 `...\win-x64\Pixbian.exe`（加 RID 后旧 exe 仍留在原目录可双击启动）。
- **三次定位（用户回传：进程路径已是 `bin\x64\Release\...\win-x64\Pixbian.exe`，确认新产物，但日志仍只有 SHORTCLIP、无 SHORTBGM）** → 异常发生在**写日志之前**，即 `CreateAsync(file, null)` 对纯音频抛了未列入 catch 的异常（WinRT 多为 `COMException`），经 fire-and-forget 调用静默丢失。
- 埋点设计缺陷（关键教训）：`pick=ok` 原本写在**播放成功之后**——一旦后续任一步抛异常，这一事实连同异常一起消失，排查时完全无线索。**留痕必须前置到「事实成立」的那一刻**，而不是「流程走完」之后。
- 修复：`StartMusicAsync` 抽到曲**立即**记 `SHORTBGM|pick=ok|file=..`；播放成功补 `play=ok`；catch 增列 `COMException` 并记 `hr=0x...`（WinRT 异常类型名区分度低，HRESULT 才是定位依据）。
- 判据对照（下一轮日志）：有 `pick=ok` 无 `play=ok`/`play=fail` = 异常类型仍漏网；有 `play=fail` = 看 HRESULT；连 `pick=ok` 都没有 = `TakeRandomTrack()` 前就异常。
- **四次定位（exe 时间戳 22:03:50 < 运行 22:04:25，确认最新构建，但连前置 `pick=ok` 仍无）** → `StartMusicAsync` 从未被执行。**最终根因**：`PlayCurrentAsync` 的 try 块 catch 列表缺 `COMException`——FFmpeg 源挂载 / `Source` 赋值 / `Play()` 抛出的 WinRT 异常（`COMException`）不被捕获，`finally` 执行后异常**继续向上传播**，位于 try/catch/finally 之后的 `if (!_hasAudioTrack)` 根本不执行；异常再经 fire-and-forget 调用被静默丢弃，全程零痕迹。前两轮只修了音乐内部的 catch 与留痕时机，漏掉了这条传播链。
- 最终修复（三层）：①`PlayCurrentAsync` catch 增列 `COMException` 并记 `hr=0x...`；②`_player.Play()` 后补 `SHORTCLIP|play=ok`，二分定位异常在 Play 前还是后；③新增 `StartMusicGuardedAsync` 兜底护栏——音乐自身的异常一律留痕不外泄（fire-and-forget 下的异常是绝对黑洞）。
- **教训（通用，两条）**：①fire-and-forget（`_ = XxxAsync()`）里的异常是绝对黑洞——被调方必须自己保证异常不外泄；②「异常拦截点」必须覆盖调用链上**每一层**的 WinRT 异常（`COMException`），只修内层漏外层等于没修。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误。待用户实测。
- **五次定位（步骤级埋点显形，最终根因）**：日志止于 `step=play`，`_player.Play()` **同步挂起**（不返回、不抛异常）→ 后续 `play=ok`、catch、背景音乐启动全部不执行。对照完整播放器（正常工作）：`VideoPlayerViewModel.LoadAsync` **全程无 ConfigureAwait(false)**，`Source`/`Play()` 都在 UI 线程；而 ShortViewModel 大量 `ConfigureAwait(false)` 使 await 后代码落到**线程池线程**——**MediaPlayer 跨线程调用 `Play()` 会同步挂起**。修复：移除 ShortViewModel 全部 12 处 `ConfigureAwait(false)`（本类 await 一律回 UI 线程），`MusicLibraryService`/`VideoMetadataReader` 等**库内部**的 ConfigureAwait(false) 保留（纯数据操作，外层 await 无标记自然回 UI）。
- **教训（写入 MEMORY.md 长期）**：MediaPlayer（Media Foundation 管线）的 `Source`/`Play`/`Pause` 必须在 UI 线程调用；跨线程调用表现为**同步挂起而非抛异常**——这与「跨线程 UI 抛 WrongThread」的直觉相反，是排查黑洞的典型形态。ViewModel 中操作 UI 亲和对象（MediaPlayer / ObservableProperty 赋值）的 await 链一律不加 ConfigureAwait(false)。
- **问题解决确认**：用户回传 `SHORTBGM|play=ok|file=Deep Hat - Vibe Tracks.mp3` → 背景音乐成功起播。收尾：清理诊断期步骤级埋点（`step=file/meta/ffmpeg/source/seek/play` 与 `play=ok`），保留失败判据（`load=fail`（含 unexpected 兜底）/`seek=fail`/`SHORTBGM pick=none|play=fail|mediafail|guard`）与音轨判定（`hasAudio`）——成功路径零日志、失败路径必有痕迹。构建 0 警告 0 错误。

## 短片页「文件多时切换视频卡住」优化
- 用户反馈：文件多的时候，切换到下一个视频会卡住。代码审读定位到每次切换在 **UI 线程**上的两个重量级同步开销：
  1. `_playback?.Dispose()` **同步**释放 FFmpeg 解码上下文（多线程软解的清理本身有耗时），且本方法常运行在 `MediaEnded` / 轮询 Tick 的**事件栈内**——清理开销直接压在 UI 线程；
  2. 每次切换都 `_metadataReader.ReadAsync` **重新创建 MediaClip 读盘**（机械盘单次可达数百毫秒），而候选池越大同一文件越会反复命中，纯浪费。
- 修复三项：①元数据按路径缓存 `_metadataCache`（AV1 后端判定、音轨判定、时长共用，每文件只读一次）；②旧播放项**脱离播放器后交线程池异步释放**（`finally` 统一收口：已切新源 → `Task.Run` 释放；载入失败未换源 → 挂回字段由下次切换处理），`VideoPlaybackItem.Dispose` 改为**幂等**（防异常路径与异步路径双释放交汇）；`StopMusic` 对音乐源同理；③CA2016：`Task.Run(stale.Dispose, CancellationToken.None)`——释放必须完整执行，显式拒绝传播取消令牌。
- 关键顺序：必须**先把 `player.Source` 切到新源（脱离旧源）再释放旧源**，否则播放管线仍引用被释放的解码器。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误。待用户实测；若仍卡，取卡住时段日志看 TICK 心跳是否中断（中断 = UI 线程同步阻塞，继续定位；不中断 = 另寻渲染侧原因）。
- 涉及文件：`src/Pixbian/Services/VideoPlaybackItemFactory.cs`、`src/Pixbian/ViewModels/ShortViewModel.cs`。
- **用户澄清后重新定位（「依然卡住，没有切换视频」+ 日志无任何 SHORTCLIP）**：切换动作**完全失效**而非载入慢——`PlayCurrentAsync` 卡在写日志之前且 `finally` 未执行 → **`_isLoading` 永久为 true**，之后所有切换被 guard 直接挡住。**根因是上一轮「异步释放」优化引入的回归**：旧源 `Dispose`（线程池）与新源 `CreateFromStreamAsync`（UI 线程）**并发**，FFmpeg 内部状态互相锁死，载入 await 永久挂起。教训：**FFmpeg（avcodec 全局状态）的创建与释放绝不能并发，必须严格串行**；「把慢操作挪到线程池」前必须先确认它与主路径无共享的全局状态。
- 修复两项：①FFmpeg 创建/释放恢复**严格串行**（全在 UI 线程）——旧源在 `player.Source` 切到新源、完全脱离之后同步释放（失败路径 `_playback` 赋值未发生、旧源完好，天然安全）；②载入各步骤（GetFileFromPathAsync / ReadAsync / CreateAsync / 音乐两处）全部 `WaitAsync(LoadTimeout=20s)`——超时判失败进兜底 catch 留痕，`_isLoading` 必然恢复，**切换能力永不永久锁死**（单条 IO 必须有超时的项目规则在此救场）。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误。待用户实测：若个别文件仍慢，20 秒后应自动判失败并跳过（`load=fail|unexpected|reason=TimeoutException`），切换功能照常可用。
- **计时数据排除载入慢（用户回传）**：`t=total|139ms`、音乐 `play=ok` 正常——载入链路完全健康。真正的现象：`play=ok` 之后 **TICK 空窗 6.1 秒**后自行恢复，画面**定格最后一帧**。应用代码在 play=ok 时已全部结束 → 阻塞在 **native 层**（dotnet-stack 抓栈证实：UI 线程停在 `Application.Start` 消息循环，native 阻塞对托管栈不可见——**dotnet-stack 只显托管帧，对 WinRT/媒体引擎的 native 等待无能为力**）。
- 推断（强时序指向）：`_player.Source` 切新源后，**媒体引擎对旧源的卸载是异步的**，紧随其后的同步 `Dispose` 旧 FFmpeg 源与引擎卸载竞态 → UI 线程被 native 等待拖住约 6 秒（引擎超时后放弃恢复）。音乐源 `Source=null` 后的同步释放同理。
- 修复：新增 `ScheduleStaleRelease`——旧播放项（视频+音乐）脱离播放器后**延迟 5 秒**（`StaleReleaseDelay`）再交后台线程 Dispose（幂等），给媒体引擎留足卸载时间；页面卸载路径的 `_playback` 仍同步释放（进程内立即回收）。构建 0 警告。
- 若仍卡，下一实验：短路 `SeekToClipStart`（禁 seek 整段播放）二分定位是「源切换卸载」还是「seek 首帧合成」。
- **用户关键澄清后终局定位**：「点击下一个一直正常，唯一问题是**视频自然播完不自动切换**（定格最后一帧）」。根因：轮询终点判定用严格 `session.Position >= _clip.End`，而媒体管线常态是 **Position 停在最后一帧的位置（最后帧 pts ≠ 容器时长，差数十至数百毫秒）→ 永不命中**；且 FFmpeg 源播完**也不触发 MediaEnded**。手动切换走点击路径故不受影响。此前日志里的 `next=skip|loading` 是另一独立问题（自动切换挂起，已由 MediaEnded TryEnqueue 修复）。
- 修复：终点判定加 **500ms 容差**（`Position >= End - EndTolerance`），代价是片段提前至多 500ms 结束（无感）。**方法论**：媒体管线的「播完」判定绝不能用 Position 与时长的严格比较，必须留一帧以上的容差；MediaEnded 事件在 FFmpegInteropX 源上不可靠，轮询 + 容差才是主判定。
- 验证：`dotnet build -warnaserror` 0 警告 0 错误。**用户确认自动连播已恢复正常**，Short 页核心任务完成。
- 评估（用户问）：是否加 loading 动画（音视频就绪前显示）。结论：**建议加轻量版**——居中小 ProgressRing 绑定 `_isLoading`（需改为可通知属性），仅载入瞬间可见；**不加铺满遮罩**（画面之上叠铺满层会打断硬件覆盖路径，项目铁律），Collapsed 状态不参与合成故无播放期开销；**不做**双 player 交叉淡入式的「完全就绪再切换」（成本高、内存翻倍，且实测载入仅 168ms 量级，跳变可接受）。
- 载入指示条实施（用户指定用 ProgressBar，参考 MainWindow 的 LoadingOverlay）：`ShortPage` 底部中央 240 宽横条，`IsIndeterminate` 与 `Visibility` 同源绑定 `IsLoading`——隐藏即复位停掉动画时钟（与 MainWindow.SyncLoadingOverlay 做法一致）；**刻意不加铺满遮罩底**（播放页画面之上叠铺满层会打断硬件覆盖，图库页无此约束所以 MainWindow 那个有背景）。`_isLoading` 升级为 `[ObservableProperty]`（MVVMTK0034：注解字段禁止直接引用，5 处改用生成属性 `IsLoading`）。指示条仅覆盖视频载入，音乐启动不阻塞画面故不在其覆盖范围。构建 0 警告。
