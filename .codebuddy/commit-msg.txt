chore: 清理临时计时代码、收敛强制 GC 触发条件并补充许可证

- 删除 TempTiming 临时设施及全部调用（含配套孤立 Stopwatch），诊断输出统一收敛到 AppLog
- JustifiedVirtualizingLayout 两处 catch 内异常记录改走 AppLog（catch 为防 XAML fail-fast 的唯一防线，保留）
- MainWindow 两处 Trace.WriteLine 调试输出改走 AppLog.Error
- 切换视图后的强制 GC 改为内存压力条件触发：托管堆低于 1 GB 直接跳过（blocking 式 gen2 为 STW，后台线程调用同样冻结 UI 线程）
- 新增 LICENSE（Apache-2.0），README 增补许可证章节
- 更新审计报告：P1-5/P1-6 与 P2-4 部分标记完成

验证：dotnet build -warnaserror 0 警告 0 错误；dotnet test 273 通过（Core 221 / WebServer 38 / Imaging 14）
