# Pixbian 企业级审计与对标报告

**审计日期**：2026-09-10
**范围**：`src/**`（221 个 .cs，含 4 个产品项目 + 3 个测试项目）、构建与工程配置
**方法**：静态代码走查 + 依赖漏洞核查 + 威胁建模（以「局域网共享」为最高风险域）+ 与成熟产品横向对标
**结论评级**：**B（良好，可继续使用）**

> **2026-09-10 更新**：P0 四项（对账误删防护、查看器解码上限、Web 资源耗尽防护、暴露面收敛）已实施并通过全量验证，详见第七节。**S-01（明文 HTTP）与 S-02（全网卡监听）已于同日收口**：S-02 落地监听网卡绑定收敛，S-01 按「明确安全前提 + 文档化」处理（不内置 TLS，决策记录见 7.15）。至此全部 High 级发现闭环。

| 域 | 评级 | 一句话结论 |
|---|---|---|
| 架构分层与代码规范 | **A-** | 分层严格、注释与约束意识罕见地好；巨型文件与临时代码是主要扣分项 |
| 数据安全（本地） | **B+** | 全参数化 SQL、PathGuard 纵深防御、PBKDF2，均达标；静止数据未加密、无 ACL 强化 |
| 网络安全（Web 共享） | **C+** | 认证/限流/只读设计到位，但明文 HTTP + 全网卡监听 + 无并发/超时上限是结构性短板 |
| 性能工程 | **B+** | 虚拟化解耦、两级缓存、IO 节流已达产品级；阻塞式 GC 与全图解码是两处硬伤 |
| 可靠性与数据完整性 | **C+** | 对账误删风险（High）、异常吞噬、无日志轮转 |
| 工程与供应链 | **C** | 无 LICENSE、无 CI、无 SBOM、无 UI 层测试、运行时逼近 EOL |

---

## 一、安全审计

### 1.1 发现清单

> 图例：**✅ 已闭环**（2026-09-10 P0 批次，详见第七节）· **⚠️ 部分缓解**（风险下降但未根治）· 未标记者待 P1/P2。

| # | 严重度 | 问题 | 位置 |
|---|---|---|---|
| S-01 | **High** | ✅ **明文 HTTP 传输**（已按「明确安全前提」收口，7.15）：密码（`POST /api/login` 明文 JSON）、会话 Cookie、全部媒体内容均不经 TLS；同网段可嗅探并重放。**处置**：不内置 TLS（与 Jellyfin 立场一致，推荐反向代理终止 TLS）；新增 `docs/局域网共享安全前提.md`（威胁模型 / 使用前提 / 反向代理示例 / 自检清单），设置页开关打开后首屏显式告知风险 | `WebAccessServer.cs`、`SettingsPage.xaml`、`docs/局域网共享安全前提.md` |
| S-02 | **High** | ✅ **全网卡监听且无网络位置感知**（已闭环，7.15）：绑定收敛为可选网卡（设置页「监听网卡」下拉，默认「全部网卡」并标注风险，选项来自网卡枚举而非手工输入）；非法绑定地址显式抛异常而非静默回退；仍保留「公用网络拒绝启动」闸门 | `WebAccessServer.cs`、`NetworkInterfaceProvider.cs` |
| S-03 | **High** | ✅ **索引对账可能误删**（已闭环）：枚举静默跳过不可访问子目录，对账把这些文件判为失效并删除其索引（连带收藏/分类/分组关系）。注释声称"中途失败不得触发删除"，实现未满足 | `MediaIndexingService.cs:22-32,150-168` |
| S-04 | **Medium** | ✅ **无并发连接上限 + 无读取总超时**（已闭环）：`ReceiveTimeout` 对 `NetworkStream.ReadAsync` 无效，连接可无限挂起；每连接固定 80 KB 缓冲区 → Slowloris / 资源耗尽（CWE-770/400）。已加并发上限 32 + 首字节 10s / 整连接 60s 链接令牌 | `WebAccessServer.cs:209,218-258` |
| S-05 | **Medium** | ⚠️ **内存无界增长**（已缓解）：`_requestCounts`/`_failedLogins`/`_sessions` 只增不减；登录锁定按 IP（NAT/共享出口可被同网段任何人触发 15 分钟锁定，形成 DoS）。登录时机已顺带清理过期会话/限流窗口/过期锁定；完整修复需后台定期清理 | `AuthService.cs:32-35,91` |
| S-06 | **Medium** | ✅ **大文件整读进内存**（已闭环）：`/media/{id}` 无 Range 时 `File.ReadAllBytesAsync` 全量读入；Range 分支 `new byte[length]` 无上限 → 大视频即 OOM | `WebAccessServer.cs:526,548-549` |
| S-07 | **Medium** | ✅ **解码炸弹**（已闭环，P1-2）：Web 缩略图 `Image.Load(path)` 与 `ImageEditService` 均无像素/内存上限（ImageSharp 无默认上限，CVE-2025-27598 即为此类前例） | `WebAccessServer.cs:590`、`ImageEditService.cs:163` |
| S-08 | **Medium** | ✅ **无可审计日志**（已闭环，P1-1）：登录成功/失败、越权拒绝、配置变更全部未记录（仅"服务启停"两条 Info）；不满足企业可追溯性 | `WebAccessServer.cs:37-43` |
| S-09 | **Medium** | ✅ **日志无治理**（已闭环，P1-1）：`crash.log`/`timing.log` 无轮转、无大小上限、无脱敏，`exception.ToString()` 可能含用户目录结构 | `App.xaml.cs:212-232`、`TempTiming.cs` |
| S-10 | **Medium** | ✅ **静止数据未保护**（已闭环，P2-1，DPAPI）：`index.db`（全盘文件路径 = 行为隐私）、`settings.json`（密码哈希）明文存于 `%LOCALAPPDATA%`，未做 ACL 收紧或 DPAPI 保护 | `AppPaths.cs`、`JsonSettingsService.cs` |
| S-11 | **Low** | ✅ **令牌可从 URL 读取**（已闭环）：`ExtractToken` 回退 `?token=` → 泄入浏览器历史/代理日志（CWE-598） | `WebAccessServer.cs:640` |
| S-12 | **Low** | ✅ **限流阈值与场景不匹配**（已闭环，P2-2）：60 次/分钟全局共享，首屏 60 张缩略图即 429；且限流先于鉴权、按 IP 无豁免 → 正常家庭使用误伤 | `AuthService.cs:29` |
| S-13 | **Low** | ✅ **会话管理缺失**（已全部闭环：登出端点、会话列表/踢出、逐设备踢出、UA 绑定、令牌轮换、密码强度策略；IP 不绑定为有意取舍——家庭 DHCP 短租约会误伤正常设备，见 7.6） | `AuthService.cs`、`SettingsViewModel.cs:890` |
| S-14 | **Low** | ✅ **`PathGuard.IsInside` 语义与文档不符**（已闭环，见 7.6：候选路径统一补尾分隔符，根目录自身按文档语义判定为内部；`\\?\` 长路径前缀与 8.3 短名的拒绝式行为已用测试固化） | `PathGuard.cs:35-44` |
| S-15 | **Low** | ✅ **`SHFileOperation` 为微软已弃用 API**（已闭环，P2-4：迁移至 `Microsoft.VisualBasic.FileIO.FileSystem`，shell 错误对话框 + 取消异常归为失败） | `RecycleBinHelper.cs` |
| S-16 | **Low** | ✅ **`Process.Start` 手写引号**（已闭环：路径先去除结尾分隔符再引号包裹，消除 `\` 转义收尾引号导致的 explorer 解析失败） | `MainWindow.xaml.cs:798`、`GalleryPage.xaml.cs:1932` |
| S-17 | **Info** | **供应链**：无 `LICENSE`（默认「保留所有权利」，第三方不可合法分发/使用）、无 CI 工作流、无 SBOM、无 Dependabot；`README` 已提示 ImageSharp 商用授权门槛 | 仓库根 |

### 1.2 已核实为「做得好」的控制（不必返工）

- **SQL 注入防御彻底**：全参数化；`LIKE` 通配符 `% _ \` 转义；`IN` 子句参数名由序号生成；排序键用参数化 `CASE` 而非拼接字段名；`user_version` 拼接为 long 强类型常量。
```591:600:src/Pixbian.Data/Repositories/SqliteMediaItemRepository.cs
    private static string BuildFilter(
        SqliteParameterCollection parameters,
        MediaQuery query,
        string? searchPattern,
        (string Directory, string Prefix)? directoryFilter)
    {
        List<string> conditions = ["deleted_utc IS NULL"];
```
- **ReDoS 四道防线**齐备（构造期 1s 超时 / 超时标记危险规则 / 整批 30s 总时限 / 文本截断 4096），并刻意不用 `RegexOptions.Compiled` 避免程序集不可卸载——属于超出平均水平的设计判断。
- **密码学正确**：PBKDF2-SHA256 / 100k / 16B 随机盐 / `FixedTimeEquals` 恒定时间比较 / 256 位 CSPRNG 令牌；哈希长度与盐长度校验防篡改绕过。
- **响应安全头齐全**（`nosniff`、`X-Frame-Options: DENY`、`Referrer-Policy`、CSP `self` 基线），头部值统一过滤 CR/LF 阻断响应拆分。
- **只读服务 + 主键寻址**：`/media/{id}`、`/thumb/{id}` 拒收路径参数，落盘前再经 `PathGuard` 二次校验（纵深防御）；对外 JSON 不含绝对路径。
- **索引跳过重解析点**（防目录环与越权读取）。

### 1.3 加固建议（按性价比排序）

1. ✅ **TLS 化或明确代理解耦**（已按后者落地，7.15）：文档中强制「仅经反向代理暴露」并在 UI 明示风险（对标 Jellyfin Hardening Guide）；内置自签证书 + 指纹配对经评估不做。
2. ✅ **绑定收敛**（已落地，7.15）：改为可选网卡（设置页下拉，选项来自枚举）；启动时读取 Windows 网络类别（`NetworkListManager`），「公用」网络下拒绝监听。
3. **连接治理**：`SemaphoreSlim` 限制并发连接（如 32）、`CancellationTokenSource` 级 10s 首字节超时、24h 硬超时；缓冲区改池化。
4. **流式响应**：`/media` 无 Range 与小 Range 改为 `FileStream` + 分块写（`CopyToAsync`），禁止整文件入内存。
5. **解码上限**：ImageSharp 设 `Configuration.Default.MemoryAllocator` 上限 + 先读文件头校验像素数（>50 MP 拒绝或降级）。
6. **审计日志**：`LoggerMessage` 覆盖登录成败、401/429/416、启停、配置变更；脱敏（IP 保留 /24、不记路径全名）；按大小轮转（如 5 MB × 3）。
7. **对账保护**：枚举过程记录"不可访问目录数"，>0 时跳过删除并告警。
8. **补齐 LICENSE + CI（含 `-warnaserror` 与 `dotnet test`）+ SBOM**；规划 .NET 8（2026-11 EOL）迁移。

---

## 二、性能审计

### 2.1 发现清单

| # | 严重度 | 问题 | 位置 |
|---|---|---|---|
| P-01 | **Medium** | ⏳ **强制全量 GC（STW）**（已降级为 P1-6，见第七节遗留）：切目录时在后台线程执行 `GC.Collect(2, Aggressive, blocking:true)` ×2 + `WaitForPendingFinalizers`。注意：虽在后台线程调用，但 `blocking:true` 的 gen2 收集是 **Stop-The-World**，UI 线程作为托管线程同样被冻结——代码注释「移到后台后 UI 仅在标记阶段短暂参与」的认知有误；本质仍是"位图内存压力未根治"的症状而非解药 | `GalleryViewModel.cs:951-961` |
| P-02 | **High** | ✅ **查看器全图无解码上限**（已闭环，P0-2）：`SetSourceAsync` 直解原图，位图内存约「宽 × 高 × 4」；12000×9000 单张即约 432 MB，与转场留存帧叠加足以 OOM 闪退（此类崩溃常无托管堆栈）。**更正**：`PreviousImage` 已由页面回调 `CompleteTransition` 正确回收，三张位图长期驻留的判断不成立 | `ImageViewerViewModel.cs:364-368` |
| P-03 | **Medium** | ✅ **SQL 搜索与排序退化**（排序/分页已闭环，P1-3 键集分页 151×；LIKE 子串搜索为 SQLite 固有限制，FTS5 经评估放弃）：`file_name LIKE '%q%'` 前置通配符全表扫；`ORDER BY CASE WHEN @sortKey...` 表达式排序无法用索引；非随机排序仍用 `LIMIT/OFFSET` 深翻 | `SqliteMediaItemRepository.cs:252-260,638` |
| P-04 | **Medium** | ✅ **磁盘缓存驱逐 O(n log n)**（已闭环，P1-4）：每次超容都对全表 `OrderBy(LastAccess).ToList()` 并持锁；LRU 内存表百万条目约 150 MB（注释已自认） | `ThumbnailDiskCache.cs:373-376` |
| P-05 | **Medium** | ✅ **数据集合无上限增长**（已闭环，7.11）：翻页余量条目滞留调度器待解队列随翻页无界增长；位图内存已由字节限额 LRU 权威管理（条目侧无位图账本问题）；VM 元数据量级经评估可接受（10 万条约数十 MB，实例替换占位的风险收益比不佳，决策记录见 7.11） | `ThumbnailLoadScheduler.cs` |
| P-06 | **Medium** | ⚠️ **Web 端加载原图**（Web 部分已闭环，P2-3；数据层虚拟化待办）：查看器与网格视频均直连 `/media/{id}`（无 512 垫场、无分级），每格一个 `<video preload=metadata>`，移动端 DOM 与带宽压力大 | `WebAssets.cs:230-241,307-310` |
| P-07 | **Low** | ✅ **调度器每拍全量排序**（已闭环，7.11）：`CommitBatch` 先把窗口外条目移出待解队列，排序规模收敛到 O(窗口 log 窗口)/拍 | `ThumbnailLoadScheduler.cs` |
| P-08 | **Low** | **后台回填收敛慢**：并发 2 × 200 条 × 单次 25 批，10 万条库需多次触发；缺"自动续跑 + 可见进度" | `MediaMetadataBackfillService.cs:24-34` |

### 2.2 性能资产（已属先进，建议保持）

- **解码量与集合规模解耦**：视口 ±1 屏窗口 + 距中心优先级 + 每拍 ≤4 条，命中"O(视口)"而非 O(集合)。
- **统一解码档位 512**：缓存条目数从「档位 × 文件」降为「文件」，切档位全量命中——典型的工程取舍且论证充分。
- **两级缓存 + 指纹失效**：磁盘 LRU 2 GB，条目头带 mtime+size，原子替换，任何故障静默降级。
- **信号量 + 20 s 超时自愈**：防止被杀软/坏道挂死的单条 IO 占死管线。
- **后台任务节流比例**：批 2.5 s 让出 1.5 s（约 1/3），并设批次数上限。
- **随机排序走固定 `random_rank` 游标**：避免 `ORDER BY RANDOM()` 全表排序与深翻重复/遗漏。

### 2.3 优化建议

1. 以**位图内存账本**替代强制 GC：按视口显式 `Release`，用内存指标驱动回收，去掉 `GC.Collect`。注意"移到后台线程"并不能规避 STW 停顿（见 P-01 更正）。
2. 查看器引入**分级解码**：当前已加 8192 最长边上限兜底（见第七节 P0-2）；完整方案是按视口设置 `DecodePixelWidth/Height`，并在放大到 1:1 时按需重载原图（对标 Windows 照片）。`PreviousImage` 无需处理——已由 `CompleteTransition` 回收。
3. 搜索改 **FTS5 虚表**或至少「前缀索引 + 扫描限流」；排序改**键集分页**（`(sort_key, id) > (?, ?)`）替代 OFFSET；为 `random_rank`、`kind` 建覆盖索引。
4. 磁盘缓存驱逐改**小顶堆 + 分批裁剪**（每次最多驱逐 N 条），避免长锁与全量排序。
5. Web 端网格用 `/thumb/{id}` 替 `/media/{id}`，查看器先 512 垫场再原图；视频 tile 改为点击后创建 `<video>`。

---

## 三、可靠性与数据完整性

| # | 严重度 | 问题 |
|---|---|---|
| R-01 | **High** | ✅ 对账误删（同 S-03，已闭环）：移动硬盘离线、权限变更、路径过长等导致子目录不可访问 → 该子树条目被判失效删除，用户收藏/分类一并丢失（不可逆）。**修法**：枚举时统计不可访问跳过次数，>0 则跳过删除并提示；或对账前校验"发现数 < 既有数 × 阈值(如 0.5)"时中止。 |
| R-02 | **Medium** | ⚠️ **异常吞噬与可观测性**（主要部分已闭环，P1-1：全局 `UnhandledException`/`UnobservedTaskException` 落盘 + 空 catch 补告警）：剩余为个别 fire-and-forget 路径的日志细化。 |
| R-03 | **Medium** | ✅ **UI 层零自动化测试**（主体已闭环，见 7.8：新增 `Pixbian.UI.Tests` 39 用例，覆盖缩略图调度器、等高视图选择服务、条目 VM 纯展示逻辑与图库 VM 首批集合语义；分页深度用例见 7.9，全仓用例总数 347；GalleryViewModel 深度用例的删除聚合部分随巨型文件拆分继续补充） | `src/Pixbian.UI.Tests/**` |
| R-04 | **Low** | ✅ **sync-over-async**（已闭环）：`WebAccessServer.StartAsync` 改为纯 async/await；`photoCountTask.Result` 位于 `Task.WhenAll` 之后取值（安全），保留。 |
| R-05 | **Low** | ✅ **临时设施未按注释清除**（已闭环，P2-4）：`TempTiming` 与全部调用、`Trace.WriteLine` 调试输出已清理；`【临时实验】Mica 停用` 为行为决策记录，保留。 |

---

## 四、架构与可维护性

**优点**：Core 零平台依赖 + 抽象注入、单向依赖、模型集中、文档级头部注释（职责/复用/约束三段式）在同类项目中属上乘；`Directory.Build.props` 集中版本、分析器开启、`NU1900` 精准豁免（未屏蔽真实漏洞告警）都做得对。

**扣分项**：

- ~~**巨型文件**~~ ✅ **已全部拆分收口**（2026-09-10）：`GalleryViewModel.cs`（7.10）、`GalleryPage.xaml.cs` 99 KB（7.12）、`MainWindow.xaml.cs` 69 KB（7.13）、`SettingsPage.xaml.cs` 27 KB（7.14）——四个文件均按职责拆为 partial，单文件至多承载一类语义。
- **无 CI**：仓库无工作流配置，`-warnaserror` 与 `dotnet test` 依赖人工执行。
- **知识沉淀优秀但封闭**：大量"实测结论"注释极有价值，建议抽取为 `docs/` 的《避坑清单》并纳入 CR 检查表。

---

## 五、成熟案例对标

| 维度 | **Pixbian** | **Windows 照片** | **digiKam** | **Immich** | **Jellyfin** | **Lightroom** |
|---|---|---|---|---|---|---|
| 元数据/索引 | SQLite 自管 + 两阶段回填 | 系统索引器 + Windows Search | SQLite/MySQL 全量元数据 + 校验和 | Postgres + 向量索引 | SQLite/EF | Catalog + XMP sidecar |
| 缩略图 | 统一 512 档 + 内存/磁盘两级 LRU | 系统缩略图缓存 | 多级持久化 + 数据库管理 | 多尺寸预生成队列 | 服务端按需生成 | Smart Preview（DNG 代理） |
| 大图查看 | 512 垫场 → **原图全解**（无上限） | 分块渐进 + 缩放分级 | 分级加载 | 多尺寸 + 渐进 JPEG | — | 分级金字塔预览 |
| 远程/共享安全 | 明文 HTTP + 单密码 + IP 限流 + 监听网卡收敛 + Hardening 文档（TLS 建议走反向代理） | 无（走 OneDrive 云） | 无内置 | 反向代理 + OAuth/OIDC + 多用户 + 审计 | API Key + 用户体系 + Hardening 文档 + 可选 HTTPS | 云同步账号体系 |
| 可观测性 | 崩溃日志（无轮转） | ETW / 遥测 | 日志级别 + 诊断页 | 结构化日志 + 健康检查 | 完整日志 + 审计 | 诊断日志 |
| 测试 | 全仓 347 用例（Core 230 / Web 59 / Imaging 14 / **UI 44**），含调度器与选择服务语义锁定 | 微软内部 | CTest 覆盖 | E2E + 单元 | 单元 + 集成 | 内部 |
| 发布工程 | 手动 + 一键脚本 | MSIX | CI 多平台制品 | Docker + CI + 版本化迁移 | CI + 多平台 | 商业 |

**三条最关键的对标差距**：

1. **共享服务的"产品级硬化"**：Immich/Jellyfin 默认假定"不可信网络"，因此强制 TLS 终止、用户/RBAC、审计日志、配额。Pixbian 的威胁模型停留在"可信家庭局域网"，一旦用户在公司/酒店网络开启即破防 —— 需在 UI 与文档中把"可信网络"前提显式化。
2. **大图与缓存的分级策略**：Windows 照片与 Lightroom 用「金字塔/代理预览」把内存与视口解耦。Pixbian 目前"统一 512 + 原图全解"是两端跳变，缺中间档（1024/2048），这正是 P-01/P-02 的根因。
3. **工程流水线**：digiKam/Immich/Jellyfin 均有 CI + 版本化迁移 + 制品与 SBOM。Pixbian 依赖人工，且 UI 层无测试网，随着巨型文件累积，变更风险会非线性上升。

---

## 六、修改方案（按优先级，实施级）

> 约定：
> - **验收方式**一律为可观察结果（新增/修改的测试、可复现的手动步骤、日志证据），不写"已完成"式描述。
> - 涉及文件 >2 个的批次，实施前先确认方案（项目规范）。
> - 每批次完成后单独 `git commit`（Conventional Commits，中文内容）；改动文件 >5 时先确认是否提交。

### P0 —— 本迭代必须闭环 ✅ 四项全部完成（2026-09-10）

> P0-1 / P0-2 / P0-3 / P0-4 均已实施并通过全量验证，实施细节见第七节。以下各条保留原方案描述，仅追加状态标记，便于复核范围是否一致。

#### P0-1 修复索引对账误删（S-03 / R-01）✅ 已实施

> **状态**：2026-09-10 完成，改动与验证见 [7.1](#71-改动清单) / [7.2](#72-验证结果)。

| 项 | 内容 |
|---|---|
| 目标 | 目录不可访问时**绝不删除**任何索引条目，避免用户收藏/分类/分组关系不可逆丢失 |
| 涉及文件 | `src/Pixbian.Core/Services/MediaIndexingService.cs`、新增 `src/Pixbian.Core.Tests/MediaIndexingServiceTests.cs` 用例 |
| 实施要点 | 1) 把 `EnumerateMediaFiles` 改为返回「文件流 + 不可访问目录计数」（用 `EnumerationOptions` 无法直接取错误，改为遍历中捕获 `UnauthorizedAccessException`/`DirectoryNotFoundException` 并计数）；2) `ScanAsync` 中若计数 > 0，跳过 `ReconcileAsync`，并在 `IndexingReport` 增加 `SkippedInaccessibleCount` 字段；3) 增设兜底闸门：发现数 < 既有数 × 0.5 时中止对账并上报；4) 界面层把该计数显示为告警 |
| 验收 | 新增 3 个用例：① 正常目录对账仍删除失效条目（回归保护）；② 存在不可访问子目录时**零删除**且报告计数 = 1；③ 发现数阈值闸门触发时不删除。`dotnet test` 全绿 + 手动：把库目录子文件夹权限改为拒绝访问后执行索引，条目数不减少 |
| 风险 | 枚举改造可能影响扫描性能；回滚：仅删除新增统计逻辑，恢复原对账 |
| 工作量 | 1.5 人日 |

#### P0-2 查看器解码上限（P-02）✅ 已实施

| 项 | 内容 |
|---|---|
| 目标 | 消除超大图一次性全分辨率解码导致的 OOM 闪退 |
| 涉及文件 | `src/Pixbian/ViewModels/ImageViewerViewModel.cs` |
| 实施要点 | 新增 `ApplyDecodeLimit`（`FullDecodeMaxEdge = 8192`）：条目已回填尺寸且最长边超限时，按等比设置 `DecodePixelWidth/Height` 降采样解码；尺寸未知时保持原分辨率。手机与单反照片普遍 ≤6000 最长边，日常放大清晰度零回退，仅全景拼接 / 高精扫描被降级 |
| 验收 | ✅ `dotnet build -warnaserror` 0 警告 0 错误；待手工：① 打开 12000×9000 级全景图不闪退、内存峰值明显下降；② 打开 6000×4000 常规照片，放大到 100% 清晰度与改造前一致 |
| 风险 | 极端图放大到 1:1 细节减少；完整方案（按视口分级 + 放大时重载原图）列入 P1，届时不可直接取消本限制 |
| 工作量 | 0.5 人日（已实施） |

> **原方案中的 GC 部分移出 P0**：经复核，`GC.Collect(2, Aggressive, blocking:true)` 已在后台线程 + 延迟 2 秒执行，注释认为可规避停顿，但 STW 仍会冻结 UI。该项降级为 **P1-6**（见第七节遗留项），与「按视口释放位图」一起做，避免在未建立内存账本前贸然删除导致 GC 风暴回归。

#### P0-3 Web 服务资源耗尽防护（S-04 / S-06）✅ 已实施

> **状态**：2026-09-10 完成，改动与验证见 [7.1](#71-改动清单) / [7.2](#72-验证结果)。

| 项 | 内容 |
|---|---|
| 目标 | 单个/少量恶意客户端无法打挂宿主进程；大视频不再整读进内存 |
| 涉及文件 | `src/Pixbian.WebServer/WebAccessServer.cs`、新增 `src/Pixbian.WebServer.Tests/` 用例 |
| 实施要点 | 1) `SemaphoreSlim` 限制并发连接（建议 32，超出排队等待，队列满则直接关闭）；2) 每连接独立 `CancellationTokenSource` + 10 s 首字节超时、60 s 总超时（替换无效的 `ReceiveTimeout`）；3) 缓冲区改 `ArrayPool<byte>.Shared` 租用；4) `/media` 无 Range 与 Range 长度 > 8 MB 时改 `FileStream` + `CopyToAsync` 分块写，Range 长度上限常量化；5) 响应路径统一 `try/catch` 兜底，异常转 500 并记日志 |
| 验收 | 新增用例：① 并发 100 连接时服务仍响应 `/api/health`；② 单连接不发数据 10 s 后被断开；③ 请求 2 GB 文件无 Range 时进程内存增量 < 50 MB（用 `/media` 对大文件 + 内存采样断言或手动任务管理器观察） |
| 风险 | 分块写需正确维护 `Content-Length`/`Content-Range`；回滚：保留旧的整读分支由开关控制 |
| 工作量 | 2 人日 |

#### P0-4 Web 服务暴露面收敛（S-02 / S-11）✅ 已实施（S-02 部分）

> **状态**：2026-09-10 完成。S-11 完全闭环；S-02 仅落地「公用网络拒绝启动」，网卡绑定收敛与 TLS 仍待 P1。改动见 [7.1](#71-改动清单)。

| 项 | 内容 |
|---|---|
| 目标 | 杜绝"在不可信网络一键开启即全库公开" |
| 涉及文件 | `src/Pixbian.WebServer/WebAccessServer.cs`、`src/Pixbian/ViewModels/SettingsViewModel.cs`、`src/Pixbian/Views/SettingsPage.xaml(.cs)` |
| 实施要点 | 1) 启动时读取 Windows 网络类别（`NetworkListManager` COM），类别为「公用」时拒绝启动并给出明确提示（可由用户显式勾选"仍要启动"覆盖，覆盖值不持久化）；2) 移除 `ExtractToken` 的 `?token=` 回退，仅认 Cookie；3) 设置页增加风险提示文案与「当前网络类别」显示；4) 绑定地址改为可配置（默认 `IPAddress.Any` 保持兼容，后续迭代提供网卡选择） |
| 验收 | 手动：① 网络设为「公用」后开启共享 → 提示拒绝且端口未监听（`netstat` 验证）；② 携带 `?token=xxx` 请求 `/api/items` 返回 401，携带 Cookie 正常；③ 私有点选覆盖后可正常启动 |
| 风险 | `NetworkListManager` 在无网卡/虚拟机环境可能取不到类别 → 取不到时按「公用」保守处理；回滚：移除类别检查 |
| 工作量 | 1.5 人日 |

### P1 —— 下个迭代（目标 4–6 周）

#### P1-1 可观测性：审计日志 + 日志治理（S-08 / S-09 / R-02）✅ 已实施

> **状态**：2026-09-10 完成。新增 `AppLog`（轮转 5 MB × 3 归档 + IP /24 与文件名脱敏 + 日志注入清洗）、`FileLogger` 桥接、`App` 全局异常双通道（`AppDomain.UnhandledException` + `UnobservedTaskException` → `SetObserved`）、Web 层审计事件（登录成败 / 锁定 / 401 / 429 / 405 / 404）。测试 12 个新用例。

| 项 | 内容 |
|---|---|
| 目标 | 安全事件可追溯；日志不再无限增长；后台异常有落点 |
| 涉及文件 | `src/Pixbian.WebServer/WebAccessServer.cs`、`src/Pixbian.WebServer/Security/AuthService.cs`、`src/Pixbian/App.xaml.cs`、新增 `src/Pixbian.Core/Utilities/AppLog.cs`（或复用既有日志目录约定） |
| 实施要点 | 1) 用 `LoggerMessage` 源生成器补齐事件：登录成功/失败/锁定、401/429/416/405、启停与配置变更；2) 脱敏：IP 记 /24，路径只记文件名不记全路径；3) 新增简单轮转写入器（单文件 >5 MB 即 `.1`→`.2` 滚动，保留 3 份），`crash.log` 与 `timing.log` 统一接入；4) `App` 构造中订阅 `AppDomain.CurrentDomain.UnhandledException` 与 `TaskScheduler.UnobservedTaskException`，统一走同一写入器；5) 空 `catch` 处补 Warning 级日志（统计刷新、缓存初始化、音乐库加载） |
| 验收 | 手动：① 连续 6 次错误密码 → 日志出现 5 条失败 + 1 条锁定，15 分钟内正确密码也失败；② 制造一次后台未观察异常 → crash.log 有记录；③ 写满 5 MB 后文件滚动且旧文件保留 3 份；④ 日志中不出现用户完整目录路径 |
| 风险 | 日志 IO 进入热路径需异步/节流（复用 `ThumbnailDiskCache` 的节流思路）；回滚：降级为空实现 |
| 工作量 | 2 人日 |

#### P1-2 解码炸弹防护（S-07）✅ 已实施

> **状态**：2026-09-10 完成。Web 缩略图与 `ImageEditService` 均先 `Image.Identify`（只读头）校验，像素数 > 50 MP 直接拒绝（阈值可测试注入）；新增 4 个用例（超限 404 / 正常 200 / 编辑拒绝且无输出 / 边界内正常）。

| 项 | 内容 |
|---|---|
| 目标 | 单个超大/畸形图片无法耗尽内存 |
| 涉及文件 | `src/Pixbian.WebServer/WebAccessServer.cs`（`EncodeThumbnail`）、`src/Pixbian.Imaging/Services/ImageEditService.cs` |
| 实施要点 | 1) 解码前用 `Image.Identify` 读头，像素数 > 50 MP 或边长 > 20000 直接拒绝（Web 侧返回 404/占位，编辑侧抛 `NotSupportedException` 并由 UI 提示）；2) 全局设置 `Configuration.Default.MemoryAllocator` 上限；3) 编辑服务增加 `CancellationToken` 检查点（当前仅传入 `Task.Run`） |
| 验收 | 新增用例：构造 30000×30000 的稀疏 PNG（或用小程序生成）→ Web 缩略图返回占位而非 OOM；编辑服务抛出明确异常而非崩溃 |
| 工作量 | 1 人日 |

#### P1-3 查询性能：搜索与分页（P-03）✅ 已实施（键集分页部分；FTS5 见决策记录）

> **状态**：2026-09-10 完成。`MediaQuery` 新增 `KeysetCursor`（LastId + 按排序键类型分字段），`QueryAsync` 非随机排序走「排序值 + id」双键比较（无 Schema 变更、零语义变化、旧 OFFSET 路径保留兼容）；`GalleryViewModel` 以已加载末条构造游标，删除收缩集合后天然避免 OFFSET 错位。**10 万条实测**：OFFSET 第 251 页 **151 ms** vs 键集第 252 页 **1 ms**（**151×**，且 OFFSET 随页深线性恶化、键集恒定）。
>
> **FTS5 决策记录**：实施时发现原方案缺陷——FTS5 分词器按空白/标点切词，中文整句成单 token；可子串匹配的 `trigram` 分词器要求查询 ≥3 字符，而中文搜索词以 2 字居多，FTS5 无法加速且语义劣于现有 `LIKE '%词%'`。**结论：放弃 FTS5，保留 LIKE 子串语义**；子串搜索无法用索引是 SQLite 的固有限制，如未来需要可评估混合路径（≥3 字符查询走 trigram，其余回退 LIKE）。

| 项 | 内容 |
|---|---|
| 目标 | 10 万级库下搜索与翻页不退化 |
| 涉及文件 | `src/Pixbian.Data/Repositories/SqliteMediaItemRepository.cs`、`src/Pixbian.Data/Sqlite/SchemaMigrations.cs`（新增迁移） |
| 实施要点 | 1) 新增 FTS5 虚表 `media_fts(file_name)` + 触发器同步，搜索改 `JOIN media_fts`；短期可先做「前缀匹配（`term%`）+ 全量扫描限流」过渡；2) 非随机排序改键集分页：`WHERE (sort_col, id) > (@lastValue, @lastId)`，随机序继续走 `random_rank` 游标；3) 为 `kind`、`random_rank`、`directory` 建覆盖索引（写入增量可接受，索引阶段已批处理） |
| 验收 | 造 10 万条数据（脚本生成）：① 搜索关键词 P95 < 300 ms（改造前基线先测）；② 翻到第 500 页耗时与第 1 页同量级；③ 现有 `MediaQueryTests` 全部通过（语义不变） |
| 风险 | FTS5 需 SQLite 版本支持（Microsoft.Data.Sqlite 内置支持）；迁移脚本一经发布不得修改 |
| 工作量 | 3 人日 |

#### P1-4 磁盘缓存驱逐优化（P-04）✅ 已实施

> **状态**：2026-09-10 完成。`Evict` 改为「锁外排序候选 + 锁内短临界区记账」，消除十万级条目下的长锁；容量计数跨线程读取改 `Interlocked.Read` 防撕裂；候选过期由 `TryRemove` 失败兜底；驱逐改为超容后的分批收敛。LRU 语义由既有 12 个用例回归验证。

| 项 | 内容 |
|---|---|
| 目标 | 消除驱逐时的长锁与全量排序 |
| 涉及文件 | `src/Pixbian.Core/Services/ThumbnailDiskCache.cs`、新增用例 |
| 实施要点 | 1) 维护「按 LastAccess 的小顶堆 + 字典索引」或退而求其次：把全量 `OrderBy` 改为 `Take(目标条数)` 的局部选择（分批驱逐，每批 ≤ 256 条）；2) 驱逐移出 `_evictionGate` 长临界区（只锁记账）；3) 定期（如每 10 分钟）清理 `_entries` 中磁盘已不存在的条目 |
| 验收 | 新增用例：缓存 5 万条目后连续写入 2000 条，测量单次 `StoreAsync` P99 < 20 ms（改造前基线先测）；LRU 语义不变（最旧条目先被驱逐） |
| 工作量 | 1.5 人日 |

#### P1-5 工程流水线：CI + LICENSE + SBOM（S-17）✅ 已实施

> **状态**：2026-09-10 完成。`.github/workflows/ci.yml`（windows-latest：构建 `-warnaserror` → 全量测试 → 包清单 SBOM artifact；首跑结果待观察 PRI 任务）；`LICENSE` 采用 **Apache-2.0**（与 ImageSharp/MetadataExtractor 生态一致、含专利条款；未正式分发前可更换），README 增补许可证章节。CycloneDX 完整 SBOM 留待分发策略确认。

| 项 | 内容 |
|---|---|
| 目标 | 变更有门禁，分发合规 |
| 涉及文件 | 新增 `.github/workflows/ci.yml`、`LICENSE`、更新 `README.md` |
| 实施要点 | 1) CI：`dotnet build -c Release -warnaserror`（注意保留 `NU1900` 豁免）+ `dotnet test -c Debug` + `dotnet format --verify-no-changes`；2) 选定 LICENSE（若采用 ImageSharp/MetadataExtractor 等 Apache-2.0 组件，建议 MIT 或 Apache-2.0，并在 `THIRD-PARTY-NOTICES.md` 注明 ImageSharp 商用条款）；3) 发布流程产出 SBOM（`dotnet list package --include-transitive` 或 CycloneDX） |
| 验收 | 推送分支后 CI 全绿；故意引入一个 warning 验证 `-warnaserror` 生效 |
| 工作量 | 1 人日 |

### P2 —— 季度规划

#### P2-1 静止数据保护（S-10）✅ 已实施（DPAPI 部分）

> **状态**：2026-09-10 完成。新增 `IHashProtector` 抽象与 `DpapiHashProtector`（DPAPI CurrentUser + 格式前缀 `dpapi:v1:`）；`settings.json` 中 Web 密码哈希落盘前加密，旧版明文哈希原样加载、下次保存自动升级；解密失败（跨机器复制 / 被篡改）安全降级为空（等价重置密码）并告警。**ACL 部分经评估跳过**：`%LOCALAPPDATA%` 默认 ACL 已限定当前用户、SYSTEM 与 Administrators，显式收紧无实际增量。新增 5 个迁移路径用例。
对 `settings.json` 中的密码哈希改用 DPAPI（`ProtectedData.Protect`，`CurrentUser` 作用域）存储并保留明文哈希的兼容读取路径；对 `%LOCALAPPDATA%\Pixbian` 目录显式设置仅当前用户 ACL。验收：以另一 Windows 用户（管理员）登录后无法读取数据库与设置文件；老版本设置文件仍可正确读取并自动升级。工作量 2 人日。

#### P2-2 会话与密码策略（S-13 / S-12）✅ 已完成

> **状态**：2026-09-10 完成：`POST /api/logout` 吊销当前会话并下发过期 Cookie（幂等）；前端 header 增登出按钮；限流分桶——一般请求 300/min、登录 10/min；登录时机顺带清理过期限流窗口与已过期的锁定记录；密码强度策略（≥8 字符、非纯数字、弱口令黑名单包含式匹配，校验先于落盘）；**会话管理**——会话记录签发 IP/时间，设置页局域网区块展示活跃会话（脱敏 IP + 登录时间）并提供「踢出全部设备」按钮，`GetActiveSessions` 不回传任何令牌。剩余项（逐设备踢出、UA 绑定、令牌轮换）已于同日 7.6 闭环；IP 绑定经评估**不做**（家庭局域网 DHCP 短租约会把正常设备踢下线，取舍记录见 7.6）。
新增 `/api/logout` 端点与会话记录结构（签发时间、IP、UA、最后访问）；设置页展示活跃设备并支持逐一吊销；密码设置加最小长度（8）与弱口令黑名单；限流阈值按端点分级（静态资源/列表 300/min，登录 10/min），并对已认证会话豁免全局计数。验收：吊销后该 Cookie 立即 401；已登录状态连续浏览 200 张缩略图不触发 429。工作量 2 人日。

#### P2-3 数据层虚拟化与 Web 端改造（P-05 / P-06）✅ 已完成（虚拟化以调度器收口形式闭环，见 7.11）

> **状态**：2026-09-10 完成 Web 端改造：网格视频 tile 不再内嵌 `<video preload=metadata>`（数十格的元数据请求会打满移动端带宽与 DOM），改为占位样式 + 点击后播放；查看器图片改「320px 缩略图垫场 → 原图就绪替换」（带 currentId 防错配守卫，与桌面端查看器同一策略）。**数据层虚拟化**：经复核实测评估，位图内存已由字节限额 LRU 与调度器视口机制覆盖，真实缺口（调度器待解队列无界增长）以队列收口闭环（7.11），VM 元数据实例替换占位经风险收益评估不做（决策记录见 7.11）。
图库集合改为「滚动窗口 + 占位条目」（保留已加载区间，超窗条目降级为轻量占位），或至少提供"释放视口外位图 + 保留元数据"的定期瘦身；Web 端网格改 `/thumb/{id}`、视频 tile 点击后再创建 `<video>`、查看器先 512 垫场。验收：滚动至 2 万条时进程内存增长 < 300 MB；手机浏览器首屏流量下降 > 70%。工作量 3–4 人日。

#### P2-4 技术债与运行时规划（S-15 / R-05 / A-01）⏳ 部分完成

> **状态**：2026-09-10 完成临时代码清理（TempTiming / Trace.WriteLine，见 7.5）与 **S-15**：`RecycleBinHelper` 由已弃用的 `SHFileOperation` 迁移到 `Microsoft.VisualBasic.FileIO.FileSystem`（.NET 共享框架自带、微软维护的官方继任路径，支持长路径与取消语义）。**巨型文件拆分**：✅ 已全部完成——`GalleryViewModel`（7.10）、`GalleryPage`（7.12）、`MainWindow`（7.13）、`SettingsPage`（7.14）。

**.NET 8 EOL 迁移评估（2026-09）**：

| 项 | 现状 | 结论 |
|---|---|---|
| 支持期 | .NET 8 LTS 支持至 **2026-11-10**（距今约 2 个月），到期后无安全补丁 | 必须规划，但非立即中断 |
| 目标 | .NET 10（LTS，2025-11 发布，支持至 2028-11） | 直接 8 → 10，跳过 9（STS 已近尾） |
| 主要风险 | ① WASDK/WinUI 对 net10 的支持需以实际包版本验证；② CsWinRT / XAML 编译器（`CsWinRTWindowsMetadata` 显式指向 10.0.19041）在 net10 下的行为；③ FFmpegInteropX 2.1 对 net10 的兼容性 | 均可通过试点构建快速验证 |
| 建议行动 | ① 建分支将 TFM 升 `net10.0-windows10.0.26100.0`、SDK 锁 10.0.x，跑通构建与全量测试；② 通过后合入并在 CI 增加双 TFM 矩阵过渡；③ `LangVersion` 可顺势升 13（顺带消除 MVVMTK0045 豁免） | ✅ 试点已完成（见 7.7），较原计划提前一个月 |
`RecycleBinHelper` 迁移到 `IFileOperation` 并细分错误码（区分取消/拒绝/占用）；删除 `TempTiming` 全部 12 处调用与 `【临时实验】` 代码、`Trace.WriteLine`；`GalleryPage.xaml.cs` / `MainWindow.xaml.cs` 按职责拆分为 partial 或协作组件（先补 UI 测试再拆）；评估 .NET 8（2026-11 EOL）迁移到 .NET 10 LTS 的路径与 WASDK 版本配套。验收：`dotnet build -warnaserror` 无警告；拆分后行为不变（人工回归 + 新增 VM 层测试）。工作量 5 人日（可分期）。

### 优先级决策矩阵

| 批次 | 主要收益 | 用户可感知 | 不可逆风险 | 依赖 |
|---|---|---|---|---|
| ✅ P0-1 对账误删 | 数据安全 | 高（曾丢失者） | **修复前持续存在** | 无 |
| ✅ P0-2 解码上限 | 稳定性 | 高（大图闪退） | 无 | 无 |
| ✅ P0-3 资源耗尽 | 安全/稳定 | 中 | 无 | 无 |
| ✅ P0-4 暴露面 | 隐私 | 中 | 无 | 无 |
| P1-1 可观测性 | 运维 | 低 | 无 | 无 |
| P1-2 解码炸弹 | 安全 | 低 | 无 | 无 |
| P1-3 查询性能 | 性能 | 中（大库） | 迁移需谨慎 | 数据量 |
| P1-4 缓存驱逐 | 性能 | 低 | 无 | 无 |
| P1-5 流水线 | 工程 | 低 | 无 | 无 |
| ✅ P2-1 静止数据 / P2-2 会话与密码 / P2-4 清理与 S-15 | 隐私/工程 | 中 | 无 | 部分依赖 P1 |
| ✅ P2-4 技术债与拆分 | 长期健康 | — | — | ✅ 运行时迁移已合入（7.7）；✅ UI 测试已就绪（7.8）；✅ GalleryViewModel（7.10）/ GalleryPage（7.12）/ MainWindow（7.13）/ SettingsPage（7.14）全部拆分；✅ 数据层虚拟化已闭环（7.11） |

---

## 七、P0 实施记录（2026-09-10）

### 7.1 改动清单

| 批次 | 文件 | 改动 |
|---|---|---|
| **P0-1** 对账误删 | `Core/Services/MediaFileEnumerator.cs`（新增） | `IMediaFileEnumerator` + `FileSystemMediaFileEnumerator` + `InaccessibleDirectoryCounter`；显式栈遍历、单层枚举、`IgnoreInaccessible=false` 以便计数 |
| | `Core/Services/MediaIndexingService.cs` | 枚举改注入；`inaccessibleDirectories.Count > 0` 时跳过 `ReconcileAsync`；移除原 `EnumerationOptions` 字段 |
| | `Core/Models/IndexingReport.cs` | 新增 `InaccessibleDirectoryCount`、`ReconcileSkipped`（带默认值，兼容既有构造） |
| | `Core.Tests/MediaIndexingServiceTests.cs` | 新增 3 用例（含桩枚举器 `StubMediaFileEnumerator`） |
| **P0-2** 解码上限 | `ViewModels/ImageViewerViewModel.cs` | 新增 `ApplyDecodeLimit`（8192 最长边）并在全图解码前调用 |
| **P0-3** 资源耗尽 | `WebServer/Http/BoundedStream.cs`（新增） | 限长只读流，供 Range 响应按块输出 |
| | `WebServer/Http/HttpTypes.cs` | `HttpResponse` 新增 `BodyStream`、`ContentLength`；`ToBytes` 支持流式 |
| | `WebServer/WebAccessServer.cs` | 并发连接上限 32（超出即关闭，不排队）；首字节 10 s / 整连接 60 s 链接令牌（替代无效的 `ReceiveTimeout`）；`/media` 整文件与 Range 全部改流式；连接处理异常兜底 |
| | `WebServer.Tests/WebAccessServerTests.cs` | 新增 3 用例：流式整文件（64 KB 逐字节比对）、Range 严格截断（16 字节）、慢连接占满并发后 12 s 内恢复服务 |
| **P0-4** 暴露面 | `WebServer/WebAccessServer.cs` | `ExtractToken` 移除 `?token=` 回退，仅认 HttpOnly Cookie |
| | `Services/NetworkCategoryDetector.cs`（新增） | COM `NetworkListManager` 判定公用网络；失败按可信返回 |
| | `ViewModels/SettingsViewModel.cs` | 启用共享前检测，公用网络直接拒绝并提示切换 |

### 7.2 验证结果

| 项目 | 结果 |
|---|---|
| `dotnet build Pixbian.sln -c Release -warnaserror` | **0 警告 0 错误** |
| `dotnet test Pixbian.sln -c Debug` | **255 通过 / 0 失败**（Core 207、WebServer 36、Imaging 12） |
| P0-1 反向验证 | 临时把 `skippedReconcile` 改回 `false`（旧行为）后，2 个新用例失败（`Expected:2/Actual:1`、`ReconcileSkipped` 断言失败），证明用例真实有效、非桩未生效导致的假通过；恢复后全绿 |
| 既有行为回归 | 隐藏文件跳过、子目录递归、格式过滤、Range 语义、鉴权 401、安全头等既有用例全部保持通过 |

### 7.3 需用户手工验证（AI 不得代验）

1. **对账防护**：把某个已索引库目录的子目录设为「拒绝访问」（非管理员），执行索引 → 应提示跳过清理且条目数不减少；恢复访问后再索引 → 失效条目被清理。
2. **大图解码**：打开 12000×9000 级全景图不闪退；6000×4000 常规照片放大到 100% 清晰度无变化。
3. **网络类别闸门**：在「公用」网络下开启局域网共享应被拒绝；切为「专用」后正常启动。**注意**：`NetworkCategoryDetector` 依赖 COM `NetworkListManager` 后绑定，需在真实环境确认取到类别（失败会静默按可信处理，功能降级但不崩）。
4. **Web 播放**：用手机浏览器播放一个大视频并拖动进度条（验证 Range 流式输出未破坏播放）。

### 7.4 P0 遗留与后续

- ~~P0-1：库目录级别的告警文案尚未接到界面~~（✅ 已于 7.6 接入设置页）。
- P0-2：完整分级解码（按视口 + 放大重载原图）列入 P1。
- P0-3：缓冲区未改池化（每连接 80 KB，受 32 连接上限约束，风险已可控）。
- 强制 GC 处理降级为 **P1-6**：先建位图内存账本，再删除 `GC.Collect`。

### 7.5 P1 实施记录（2026-09-10 同日续）

| 批次 | 状态 | 改动与验证 |
|---|---|---|
| **P1-1** 可观测性 | ✅ | 新增 `Core/Utilities/AppLog.cs`（轮转 5 MB × 3、IP /24 与文件名脱敏、消息体 CRLF 清洗防日志注入）、`Pixbian/Services/FileLogger.cs`（`ILogger` 桥接）；`App` 订阅 `AppDomain.UnhandledException` 与 `UnobservedTaskException`（`SetObserved`），崩溃日志统一走 AppLog；`AuthService` 记录登录成败 / 锁定，`WebAccessServer` 记录 401 / 429 / 405 / 404。新增 12 测试用例 |
| **P1-2** 解码炸弹 | ✅ | `WebAccessServer.EncodeThumbnail` 与 `ImageEditService.ProcessAsync` 先 `Image.Identify` 读头校验，> 50 MP 拒绝（阈值属性化便于测试注入）；新增 4 用例 |
| **P1-4** 缓存驱逐 | ✅ | `Evict` 重构：锁外排序候选 + 锁内短临界区记账；`Interlocked.Read` 防容量计数撕裂；候选过期由 `TryRemove` 兜底；12 个既有用例回归通过 |
| **P1-5** 流水线 | ✅ | `.github/workflows/ci.yml` + `LICENSE`（Apache-2.0，与 ImageSharp/MetadataExtractor 生态一致、含专利条款）+ README 许可证章节；CycloneDX 完整 SBOM 留待分发策略确认 |
| **P1-3** 查询性能 | ✅ | 键集分页（`KeysetCursor` 双键比较，无 Schema 变更）；10 万条实测 OFFSET 151 ms vs 键集 1 ms（151×）；FTS5 经评估放弃（中文 2 字词受 trigram ≥3 字符限制，语义会劣化），详见 P1-3 决策记录。新增 2 用例（三种排序键逐页拼接与 OFFSET 全量一致、翻页不重不漏）+ 临时基准（已按约定删除） |
| **P1-6** 强制 GC | ✅ | 切换视图后的 `GC.Collect(2, Aggressive, blocking:true)`（STW）改为**内存压力条件触发**：托管堆 < 1 GB（`MemoryCompactionThresholdBytes`）直接跳过，多数切换零停顿；超阈值路径保留。完整位图账本留 P2 |

#### P2-4 部分完成：临时代码清理 ✅

> 2026-09-10：删除 `TempTiming` 类与全部 12 处调用（含配套 Stopwatch 孤立代码）；`JustifiedVirtualizingLayout` 两处 catch 内的异常记录改走 `AppLog`（catch 是防 XAML fail-fast 的唯一防线，保留，仅换日志通道）；`MainWindow` 两处 `Trace.WriteLine` 调试输出改走 `AppLog.Error`。`IFileOperation` 迁移与巨型文件拆分、.NET 运行时迁移评估仍待后续迭代。

**验证**：`build -warnaserror` 0 警告 0 错误；测试 **271 通过 / 0 失败**（Core 219、WebServer 38、Imaging 14）。
**期间修复**：CS0104 —— `UnhandledExceptionEventArgs` 与 XAML 同名类型二义，以 `using UnhandledExceptionEventArgs = System.UnhandledExceptionEventArgs;` 消歧（与既有踩坑记录一致：XAML 假错误先查 CS）。

### 7.6 P2 续作记录（2026-09-10 同日三批）

| 任务 | 状态 | 改动与验证 |
|---|---|---|
| **S-16** explorer 引号 | ✅ | 提交 `bc3a769`：路径先 `TrimEnd` 分隔符再引号包裹（`"D:\Lib\"` 的 `\` 会转义收尾引号），`MainWindow` 与 `GalleryPage` 两处同步；顺带消除 `StartAsync` 的 sync-over-async |
| **S-14** PathGuard 语义 | ✅ | `IsInside`/`TryResolveInside` 的候选路径规范化后统一补尾分隔符再比较：根目录自身按文档语义判定为内部，文件归属判定不受影响；`\\?\` 长路径前缀与 8.3 短名「不展开即拒绝」的安全默认新增 4 个固化用例（含「将来不得顺手修好」约束） |
| **P0-1 遗留** 对账告警接入界面 | ✅ | `SettingsViewModel.ScanFolderAsync`：`InaccessibleDirectoryCount > 0` 时在设置页状态文本显示「N 个子目录无法访问，已跳过失效条目清理」告警，用户不再把「条目未清理」误判为扫描失灵 |
| **S-13 剩余** 逐设备踢出 | ✅ | 会话新增公开 ID（32 位随机 hex，非令牌不可认证）；设置页活跃会话改逐行展示，每行「脱敏 IP + 登录时间 + 踢出」按钮，`AuthService.RevokeById` 按 ID 吊销且不影响其他会话 |
| **S-13 剩余** UA 绑定 | ✅ | 会话签发时记录 User-Agent，`ValidateWithRotation` 校验一致性；签发时无 UA 则跳过绑定（兼容无 UA 客户端）。**IP 绑定经评估不做**：家庭 DHCP 短租约会把正常设备踢下线，误伤大于收益 |
| **S-13 剩余** 令牌轮换 | ✅ | 剩余寿命不足一半时签发新令牌替换旧令牌（新令牌随响应 Set-Cookie 下发，旧令牌立即失效），收紧失窃令牌的可用窗口；`WebAccessServer` 统一认证点执行校验 + 轮换 |

**验证**：`build -warnaserror` 0 警告 0 错误；测试 **303 通过 / 0 失败**（Core 230、WebServer 59、Imaging 14）；新增 11 个用例（PathGuard 4 + AuthService 会话 7，覆盖 UA 绑定、轮换时间条件（注入短寿命驱动）、逐设备吊销隔离、空令牌拒绝）。
**需用户手工验证**：① 设置页对某库目录子目录拒绝访问后执行索引 → 状态文本出现对账保护告警；② 设置页局域网区块逐行踢出单个设备 → 该设备下次刷新即要求重新登录；③ 手机端登录后浏览超过 4 小时（8 小时会话过半）仍能正常访问（轮换 Cookie 已自动下发）。

---

### 7.7 .NET 10 迁移试点记录（2026-09-10）

> 分支 `feature/net10-migration`；SDK 10.0.401；较原计划（2026-10 内）提前完成。改动：`global.json`（8.0.424→10.0.401）、`Directory.Build.props`（LangVersion 12→13）、全部 9 个 csproj TFM（`net8.0`→`net10.0`）、CI 切 10.0.x。依赖包版本未动（`Microsoft.Extensions.*` 8.x 与 `Sqlite 8.0.11` 在 net10 下兼容，升级另行评估）。

**试点结论：TFM 升级本身零源码破坏，唯一硬伤是 XAML 编译器的工作目录断裂（已修复）**：

| # | 问题 | 处置 |
|---|---|---|
| 1 | **XAML 编译器 CWD 断裂（核心风险，已解决）**：net10 SDK 的 MSBuild 下，`Exec` 启动 `XamlCompiler.exe` 的工作目录是**构建启动目录**而非项目目录（net8 行为相反或恰好兼容）；而 XAML 页面以项目相对路径传入编译器（按 CWD 解析）→ 从仓库根构建 sln 时全部页面解析失败（`WMC0001 Unknown type` 连锁 + `WMC9999` 内部 NRE，极易误判为 WASDK 不支持 net10）。排查路径：手动重放编译器 → 以项目目录为 CWD 重放即成功 → 定位 CWD 断裂。**修复**：`XamlCompilerExePath` 指向构建期生成的包装脚本（先 `cd` 到项目目录再启动真实编译器），页面解析与启动目录彻底解耦 | `Pixbian.csproj`（`GenerateXamlCompilerWrapper` target） |
| 2 | **CA1859**（net10 分析器新增）：私有字段/参数以 `IReadOnlyList<>` 声明但所有赋值/调用点均为 `List<>` → 建议具体化 | 5 处照改（`MusicLibraryService._trackPaths`、`SlideShowViewModel._playlist`、`GalleryViewModel` 三个私有方法参数），无抽象损失 |
| 3 | **CA1873**（net10 对 `LoggerMessage` 调用点的新检查）：参数为方法调用时建议 `IsEnabled` 守卫 | 精准豁免（`Directory.Build.props`，沿用 NU1900 先例）：AppLog 文件日志恒启用，参数均为轻量脱敏函数，守卫化零收益 |

**验证**：从仓库根 `dotnet build Pixbian.sln -c Release -warnaserror` **0 警告 0 错误**；测试 **303 通过 / 0 失败**（Core 230 / WebServer 59 / Imaging 14）；CI 已切 .NET 10.0.x。
**需用户手工验证**：✅ 已确认——应用启动与全链路回归无误（2026-09-10 用户反馈）。
**遗留**：① 双 TFM 矩阵（合入 main 前评估是否保留 net8 过渡）——✅ 已评估：不保留，见 7.16；② `LangVersion 13` 已启用，`[ObservableProperty]` 迁移为 partial property 后可移除 MVVMTK0045 豁免——✅ 已评估：前置条件未满足（生成器仅支持 preview 语言版本），暂不迁移，见 7.16；③ 本机需重启后确认 SDK 环境完全生效（安装器提示）。

### 7.8 UI 测试基建记录（2026-09-10）

> R-03 的主体闭环：UI 层测试从 0 → 39 用例，全仓基线 **342 通过**（Core 230 / WebServer 59 / Imaging 14 / UI 39）。这是巨型文件拆分与数据层虚拟化（两者的「先补 UI 测试」前置）的行为安全网。

| 阶段 | 内容 |
|---|---|
| **阶段 1** | 新增 `src/Pixbian.UI.Tests`（net10.0-windows，引用 WinExe 主项目）。36 用例锁定三个核心单元的语义：`GallerySelectionService`（游离条目交集陷阱、范围/全选对齐、锚点失效退化——等高视图选择的历史真实缺陷防线）；`ThumbnailLoadScheduler`（视口窗口收编/离窗清理、距中心优先级、每拍 ≤4 批次上限、失败不重试）；`MediaItemViewModel`（AspectRatio 预取 > 索引 > 方图兜底与钳制——LayoutCycle 事故防线、通知去重、加载状态机离线分支） |
| **阶段 2** | 新增 `IUiDispatcher`/`IUiDispatcherTimer` 抽象与 `UiDispatcherAdapter`（投递语义含「队列关闭转异常」），GalleryViewModel / 调度器改经抽象注入，DI 零改动、`InternalsVisibleTo` 移除；新增 GalleryViewModel 离线测试（内存仓储桩 + 同步内联假队列）：首页加载、类型筛选查询条件传递、收藏切换双线同步与取消收藏解除分组 |

**测试基建的 net10 特有事实**：`dotnet build sln` 会构建**全部**解决方案平台（net8 仅构建默认平台）——新增项目后其 AnyCPU 映射会以全局属性穿透 ProjectReference（项目内 AnyCPU→x64 重定向无法覆盖全局属性），令 SelfContained/Win2D 拒绝构建主项目；修复为 sln 中相关项目的全部平台映射指向 x64。

**遗留**：① ✅ GalleryViewModel 深度用例已补充（2026-09-10，见 7.9）；② SettingsViewModel 等其余 VM 测试排期；③ 视觉回归仍由人工验收承担（WinAppDriver 已弃用不引入）。

---

### 7.9 分页深度用例补充记录（2026-09-10）

> 7.8 遗留①闭环：新增 `Pixbian.UI.Tests/ViewModels/GalleryViewModelPaginationTests.cs` 5 用例，全仓基线 **342 → 347**（Core 230 / WebServer 59 / Imaging 14 / **UI 44**）。

| 用例 | 锁定的语义 |
|---|---|
| 多页翻页不重不漏且页满收口 | 450 条 → 3 页拼接，无重复 id；页未满即 `HasMore=false` 且翻页命令禁用 |
| 默认排序翻页携带键集游标 | 首页 `Keyset=null`；翻页游标 = 已加载末条 `Id` + `ModifiedUtc` 双键 |
| 随机排序游标自末条之后推进 | 洗牌必携带 `RandomCursor`；翻页游标 = 上一页末条 `RandomRank + 1` |
| 收藏视图收缩后游标落在新末条 | 取消收藏致集合收缩（200 → 199）后，翻页游标落在收缩后的新末条上，不回退为集合长度（OFFSET 错位缺陷防线） |
| 删除守卫直接返回零 | 空集合与删除进行中的并发守卫不启动删除状态机 |

**范围说明**：删除聚合完整链路（回收站 → 集合移除 → 索引清理 → 统计收缩）依赖 `RecycleBinHelper` 可注入才能离线测试——✅ 已于同日 7.10 随 GalleryViewModel 拆分补齐（`GalleryViewModelDeleteTests` 4 用例）。测试桩为支持 `Skip/Take/Keyset/RandomCursor` 语义的分页内存仓储，断言「页与页拼接 = 全量不重不漏」。

---

### 7.10 GalleryViewModel 拆分记录（2026-09-10）

> P2-4 巨型文件拆分第一项闭环：`GalleryViewModel.cs`（58 KB / 约 1360 行）按职责拆为三个 partial 文件，行为零变更；同时完成删除链路的可测试化（`RecycleBinHelper` 抽象注入），补齐 7.9 注明的删除聚合测试。全仓基线 **347 → 351**。

| 文件 | 职责 |
|---|---|
| `GalleryViewModel.cs`（主文件） | 筛选/排序/搜索状态、集合与替换语义、键集分页加载、页头统计、收藏与分组、重命名 |
| `GalleryViewModel.Delete.cs`（新增） | 回收站删除循环、进度/结果通知条状态机、取消；经注入的 `IRecycleBinService` 执行回收站操作 |
| `GalleryViewModel.Thumbnails.cs`（新增） | 调度器（视口窗口/批次节奏）、容量淘汰回写与视口内延后释放、档位切换、尺寸预取 |

**关键改动**：新增 `IRecycleBinService` 接口 + `RecycleBinService` 默认实现（`RecycleBinHelper.cs` 内，静态类保留给 ShortViewModel / SettingsViewModel 待后续拆分迁移）；构造注入并在 DI 注册。顺带清理主文件遗留的孤立 `Stopwatch` 别名与 `Microsoft.UI.Xaml.Media` using。

**新增测试**：`GalleryViewModelDeleteTests` 4 用例——① 全部成功：集合移除 + 索引按实际路径清理 + 统计收缩 + 进度终值；② 部分失败：仅成功条目出集合、通知含失败计数；③ 全部失败：索引与集合原状、通知含失败原因；④ 选中项被删除时 `SelectedItem` 置空。

**验证**：`build -warnaserror` 0 警告 0 错误；全仓测试 **351 通过 / 0 失败**（Core 230 / WebServer 59 / Imaging 14 / UI 48）。

---

### 7.11 数据层虚拟化：调度器队列收口记录（2026-09-10）

> P2-3 数据层虚拟化闭环。实施前复核发现原「滚动窗口 + 占位条目」方案的两个事实前提已被既有机制覆盖，据实调整为调度器队列收口（2 文件改动）。全仓基线 **351 → 353**。

**方案调整的依据**：

| 原方案设想 | 复核结论 |
|---|---|
| 瘦身释放视口外条目位图 | 位图内存已由内存缓存的字节限额 LRU 权威管理（P1 批次落地），条目侧置空不释放缓存本体，且曾实测头部瘦身与调度器叠加成「释放→重解」自激——该部分无增量收益 |
| 占位条目降级（实例替换） | 每条 VM 含 MediaItem 元数据约 0.5 KB，10 万条全量常驻约数十 MB；实例替换会牵动选择服务（游离条目交集陷阱）、页面持有的条目引用与收藏动画语义，风险收益比不佳 |
| 真实缺口 | 翻页余量条目经 `Enqueue` 滞留调度器待解队列：窗口外条目永不提交也永不清除，快速翻页下随集合无界增长（P-05 实体），并使 `CommitBatch` 每拍对全量队列排序（P-07） |

**实施**（`ThumbnailLoadScheduler.cs`）：`CommitBatch` 在窗口建立时先把窗口外条目移出待解队列再排序——收编（`UpdateWindow` 对窗口内无位图条目统一收编）本就是窗口解码的唯一权威入口，移出与停留语义等价，滚动到位后自然重新纳入；队列规模与每拍排序代价恒为 O(窗口)，与集合规模解耦。

**新增测试 2 个**：① 窗口外余量条目不立即提交、滚动到位后由收编提交；② 大批（50 条）窗口外余量多拍持续解码不发生（旧行为会以每拍 4 条渐进解码全部条目，与「解码量 = O(视口)」目标相悖）。

**验证**：`build -warnaserror` 0 警告 0 错误；全仓测试 **353 通过 / 0 失败**（Core 230 / WebServer 59 / Imaging 14 / UI 50）。

**需用户手工验证**：大库（数万条以上）连续快速翻页后持续滚动，缩略图按视口渐进出现、无成批卡顿；任务管理器观察长时间浏览内存不随翻页线性增长（位图由缓存限额兜底）。

---

### 7.12 GalleryPage 拆分记录（2026-09-10）

> P2-4 巨型文件拆分第二项闭环：`GalleryPage.xaml.cs`（99 KB / 约 2410 行）按职责拆为 1 主文件 + 5 个 partial，**代码逐段原样搬运、行为零变更**。全仓基线保持 353。

| 文件 | 职责 |
|---|---|
| `GalleryPage.xaml.cs`（主文件） | 字段与页面级状态、构造与事件订阅、公共属性、VM 属性联动、等高元素重建与回顶、容器复用、通用 `FindDescendant` |
| `GalleryPage.ItemInteraction.cs`（新） | 等高悬停视觉、元素准备（按需解码）、命中解析（Repeater 索引反查）、单击/双击/收藏手势、查看器延时打开、收藏弹跳动画 |
| `GalleryPage.Selection.cs`（新） | 选择模式进出与选择集合双通道聚合（GridView 多选 + 等高选择服务）、收藏分组菜单、全选/计数、键盘快捷键、焦点还原 |
| `GalleryPage.GridView.cs`（新） | ItemsWrapGrid 动态边长（防震荡门控）、视口窗口化与在途取消、触底翻页、按压弹簧反馈 |
| `GalleryPage.ContextMenu.cs`（新） | 右键菜单构建/挂解/信息填充、打开/复制/重命名/删除/资源管理器定位、对话框 |
| `GalleryPage.Toolbars.cs`（新） | 排序/筛选/显示与大小菜单、两个「更多」菜单自适应补齐、幻灯片入口、删除通知条按钮 |

**拆分注意点（复用价值）**：WinUI 3 的 `FocusManager` 在 `Microsoft.UI.Xaml.Input`，而 `FocusManagerGotFocusEventArgs` 在 `Microsoft.UI.Input`；`RightTappedRoutedEventArgs` 同属 `Xaml.Input`——partial 拆分后每个文件的 using 必须按实际引用裁剪，C# 错误会连锁引发 XAML 假错误，先修 CS（既有规律第三次验证）。

**验证**：`build -warnaserror` 0 警告 0 错误；全仓测试 **353 通过 / 0 失败**（Core 230 / WebServer 59 / Imaging 14 / UI 50）。

**需用户手工验证**（页面层无自动化测试，行为回归由人工承担）：图库页悬停复选框浮现、单击打开/双击收藏、选择模式进出与全选、右键菜单五项操作、方形/等高视图切换与滚动翻页、F5/Ctrl+A/Delete 等快捷键、明暗主题下右键菜单删除项红色。

---

### 7.13 MainWindow 拆分记录（2026-09-10）

> P2-4 巨型文件拆分第三项闭环：`MainWindow.xaml.cs`（69 KB / 约 1655 行）按职责拆为 1 主文件 + 4 个 partial，代码逐段原样搬运、行为零变更。全仓基线保持 353。

| 文件 | 职责 |
|---|---|
| `MainWindow.xaml.cs`（主文件） | 页面实例与视图模型持有、构造装配与事件订阅、绑定属性、启动初始化 |
| `MainWindow.Navigation.cs`（新） | NavigationView 分组展开交互（点行只导航/点箭头才折叠）、导航项分派、页面装载、搜索下发、设置页跳转 |
| `MainWindow.NavigationItems.cs`（新） | 左栏动态子项重建与选中恢复、扫描源右键菜单与全部操作、Ctrl+I 添加媒体文件夹 |
| `MainWindow.Viewer.cs`（新） | 查看器/放映窗口的创建与复用、查看器移交放映、视频播放态装载与卸载、全屏与窗口级 chrome 切换 |
| `MainWindow.Window.cs`（新） | 根布局装配、DPI 跟进、标题栏 Passthrough 注册、系统标题栏按钮配色、设置回流分发 |

**顺带修正**：`AddMediaFolderAsync` 中遗留的一处 `Trace.WriteLine` 调试输出改为 `AppLog.Error`（R-05 清理的漏网点，原为 fire-and-forget 异常黑洞）。

**验证**：`build -warnaserror` 0 警告 0 错误；全仓测试 **353 通过 / 0 失败**（Core 230 / WebServer 59 / Imaging 14 / UI 50）。

**需用户手工验证**：左栏「图库」点行只导航、点箭头才折叠；扫描源右键菜单六项操作与索引期间禁用；Ctrl+I 添加文件夹；双击图片/视频分别进查看器与播放器；查看器内移交放映；Esc 分级退出全屏；标题栏搜索框与设置按钮可点（Passthrough）；主题切换后系统按钮配色与 logo 跟随；查看器打开时导航栏收起。

---

### 7.14 SettingsPage 拆分记录（2026-09-10）

> P2-4 巨型文件拆分最后一项闭环：`SettingsPage.xaml.cs`（27 KB / 570 行）按职责拆为 1 主文件 + 4 个 partial，代码逐段原样搬运、行为零变更。全仓基线保持 353。

| 文件 | 职责 |
|---|---|
| `SettingsPage.xaml.cs`（主文件） | 视图模型与分类页持有、构造与选择器索引初始化、共享绑定属性（Owner 等）、页面加载初始同步、内容区限宽居中、开关状态文字同步、INPC |
| `SettingsPage.Preferences.cs`（新） | 主题 / 查看器（滚轮、缩放）/ 幻灯片（顺序、转场、间隔、片段档位、背景音乐与音量、四个开关）的回填与落盘 |
| `SettingsPage.Library.cs`（新） | 扫描源与音乐目录的增删、手动索引、媒体库「位置」行 Expander 头部对齐 |
| `SettingsPage.WebSharing.cs`（新） | 共享开关、端口与密码落盘、活跃会话踢出、访问地址经系统浏览器打开 |
| `SettingsPage.FavoriteGroups.cs`（新） | 分组选中与改名模式互斥、新增 / 改名 / 删除与选中态清理 |

**验证**：`build -warnaserror` 0 警告 0 错误；全仓测试 **353 通过 / 0 失败**（Core 230 / WebServer 59 / Imaging 14 / UI 50）。

**需用户手工验证**：设置页各下拉与开关切换后即时生效且重启保留；扫描源与音乐目录增删；共享开关与端口密码失焦应用、活跃会话踢出；收藏分组新增/改名/删除与列表选中态；分类组内容正常装载；窗口放大时内容块居中限宽。

---

### 7.15 S-01 / S-02 收口记录（2026-09-10）

> 报告中最后两项 High 级发现闭环。全仓基线 **353 → 356**（WebServer 59 → 62）。

**S-02 监听绑定收敛**（代码加固）：

| 改动 | 说明 |
|---|---|
| `AppSettings.WebSharingBindAddress` | 新增设置项；空串 = 全部网卡（默认，兼容既有行为），随设置文件持久化 |
| `NetworkInterfaceProvider`（新） | 枚举本机 Up 且非回环的 IPv4 地址，首项为「全部网卡」哨兵；枚举失败仅返回哨兵项（不阻断设置页） |
| `WebAccessServer` | 构造新增 `bindAddress`（末位可选参数，既有调用零改动）；`TcpListener(bind, port)`；`ActiveUrls` 绑定具体网卡时只列该地址 |
| 非法地址处置 | **显式抛 `ArgumentException`**，不静默回退到全网卡——安全相关的降级必须是显式失败 |
| 设置页 | 局域网卡片新增「监听网卡」下拉（`DisplayMemberPath` + `SelectedIndex` TwoWay，符合「SelectedValue 绑值类型是雷」的既有约束）；切换即落盘并重建服务；回填期间以 `_isSyncingWebBind` 抑制重入 |
| 闸门保持 | 「公用」网络拒绝启动的逻辑不变（含绑定具体网卡时） |

**S-01 明文 HTTP**（风险显式化，不内置 TLS）：

| 改动 | 说明 |
|---|---|
| `docs/局域网共享安全前提.md`（新） | 威胁模型、内置防护清单、使用前提、Caddy/nginx 反向代理示例、已知未做项（含「IP 绑定有意不做」）、开启前自检清单 |
| 设置页 | 开关打开后的首屏显式风险提示文案（明文 HTTP + 仅可信专用网络 + 反向代理建议） |
| README | 「功能一览」「安全设计」同步，并在局域网条目链向安全前提文档 |
| **决策记录** | 不内置 TLS：自签证书在移动浏览器仍有证书警告，需为握手/超时/Range 流式全链路适配，收益低于反向代理方案；与 Jellyfin 官方 Hardening 立场一致（推荐代理终止 TLS，内置 HTTPS 为可选项）。故 S-01 以「明确前提 + 文档化 + UI 提示」收口，不再列为待办 |

**新增测试 3 个**：① 绑定指定地址后 `ActiveUrls` 收敛为该地址且服务可用（`/api/health` 200）；② 非法绑定地址构造即抛 `ArgumentException`；③ 未指定绑定地址时保持全部网卡监听行为（回归保护）。

**验证**：`build -warnaserror` 0 警告 0 错误；全仓测试 **356 通过 / 0 失败**（Core 230 / WebServer 62 / Imaging 14 / UI 50）。

**需用户手工验证**：① 设置页「监听网卡」切换到具体网卡后，`netstat -ano | findstr 8756` 只在该地址监听，其它网卡 IP 不可达；② 选「全部网卡」恢复既有行为；③ 拔掉网卡 / 换 Wi-Fi 后重新进入设置页，失效地址自动回落「全部网卡」；④ 开关打开即见明文 HTTP 风险提示文案。

---

### 7.16 两项「另行排期」项评估结论（2026-09-10）

> 7.7 遗留的两项工程整洁项评估完成，**结论均为「维持现状」**，无代码改动（本轮试点改动已全部回滚，工作区与 `84ba4e2` 一致）。全仓基线保持 356。

**① 双 TFM 矩阵（net8 过渡）→ 不保留**

net10 迁移（7.7）已合入 main 并经用户真机验证（应用启动与全链路回归无误），此后 7.10–7.15 全部批次均在 net10 单 TFM 下构建与测试（构建 0 警告、全仓 356 通过）。保留 net8 双 TFM 需维护两套包版本与两条构建路径，而 net8 于 2026-11 EOL 在即，**不保留过渡 TFM**；CI 继续单 TFM（10.0.x）。

**② `[ObservableProperty]` → partial property 迁移（消除 MVVMTK0045 豁免）→ 前置条件未满足，暂不迁移**

实测结论（本轮验证）：

| 语言版本 | 结果 |
|---|---|
| `LangVersion 13.0`（现状） | 全部 partial property 声明报 **CS9248**（分部属性缺实现）——生成器不处理 |
| `LangVersion 14.0` | 同样报 CS9248 |
| `LangVersion preview` | ✅ 构建成功（0 警告 0 错误），确认生成器仅在 preview 下为 partial property 生成实现 |

- **根因**：CommunityToolkit.Mvvm 8.4.0 的 partial property 实现依赖 `field` 关键字，该关键字在 8.4 发布（2024-12）时仍属 preview；生成器以语言版本判定是否生成。
- **不采用 preview 的理由**：CI 使用浮动 `10.0.x` SDK，preview 语言特性语义可能随 SDK 升级变化 → 破坏可复现构建；而收益仅是消除一条**非 AOT 场景下本就无害**的分析器建议（7.14 前提：本应用未启用 AOT 发布）。升级 Toolkit 9.x 属预发布依赖，项目规范禁止。
- **处置**：保留字段版写法与 `NoWarn;MVVMTK0045` 豁免，并把实测结论写入 `Pixbian.csproj` 豁免注释（含「待 Toolkit 正式支持 C# 14 `field` 后再迁移」的前置条件），避免后续重复踩坑。

### 7.17 仓库脚本布局收拢记录（2026-09-10）

> 目标：仓库根目录只保留解决方案与文档入口，全部开发辅助脚本收拢到 `scripts\`，消除「根目录脚本 + `build\` + `tools\` 三处散落」的布局债。

**收拢结果**

| 旧位置 | 新位置 | 处置 |
|---|---|---|
| `Pixbian.ps1`（根目录） | `scripts\Build-Pixbian.ps1` | 移入；脚本内仓库根改由 `Split-Path -Parent $PSScriptRoot` 推出（`$PSScriptRoot` 已从仓库根变为 `scripts\`） |
| `tools\register-local.ps1` | `scripts\Register-Pixbian.ps1` | 移入 |
| `build\Install-Toolchain.ps1` | `scripts\Install-Toolchain.ps1` | 移入 |
| `tools\gen-icon.ps1` | `scripts\New-AppIcon.ps1` | 移入 |
| `tools\_probe.ps1` | — | 删除（一次性探针脚本，已无用） |

命名统一为「动词-对象」PascalCase（PowerShell 生态惯例，目录内按动词聚集排序）：
`Pixbian-build.ps1` → `Build-Pixbian.ps1`（构建并启动）、`Register.ps1` → `Register-Pixbian.ps1`（注册稀疏包）、
`gen-icon.ps1` → `New-AppIcon.ps1`（生成 ICO）；`Install-Toolchain.ps1` 命名本就符合，未动。

根目录现仅剩 `Directory.Build.props`、`global.json`、`LICENSE`、`Pixbian.sln`、`README.md` 与 `docs\` / `scripts\` / `Packaging\` / `src\`。

**引用同步**（6 处）

- `README.md`：常用命令与项目结构树
- `docs\可行性开发方案.md`：构建验证命令与物理分层树
- `docs\图库列表性能优化方案.md`：埋点章节的启动脚本路径
- `src\Pixbian\Services\IdentityPackageService.cs`：`RegisterScriptPath` 常量（`tools\register-local.ps1` → `scripts\Register-Pixbian.ps1`）与类注释；该常量直接渲染进设置页「未注册」提示，路径必须与实际脚本一致

**验证**

| 项 | 结果 |
|---|---|
| 全仓引用残留（`register-local` / `Pixbian.ps1` / `tools\register*`） | 匹配 **0** |
| 脚本语法（`[Parser]::ParseFile`） | 两个脚本均 **0 错误**；`$root` 推导后 `src\Pixbian\Pixbian.csproj` 与 `Packaging\identity\AppxManifest.xml` 均存在 |
| 脚本编码 | `scripts\` 下统一 **UTF-8 with BOM**（PS 5.1 对无 BOM 文件按 ANSI 解析，中文注释与引号会错乱并报语法错误——本次新脚本首写为无 BOM，已实测复现并修正） |
| 构建 / 测试 | `dotnet build Pixbian.sln -c Release -warnaserror`（CI 等价命令）**0 警告 0 错误**；`dotnet test -c Debug` **356 通过 / 0 失败**（Imaging 14 / WebServer 62 / Core 230 / UI 50），与 7.16 基线一致 |

> 附带修复（CI 阻塞点）：本轮首次按 CI 原样执行 `-warnaserror` 构建，复现出 7.15 批次引入的 **CA1861 错误**（`WebAccessServerTests` 的 `Assert.Equal(new[] { … }, …)` 内联数组），它会让 `Build (warnings as errors)` 步骤直接失败；已把该数组提为静态字段 `LoopbackOnlyUrl`，修复后 CI 等价命令 0 警告 0 错误。

**需用户手工验证**：`.\scripts\Build-Pixbian.ps1` 与 `.\scripts\Register-Pixbian.ps1` 在真机按新路径可正常执行（注册脚本会改动系统包与证书，AI 不得代跑）。

---

## 八、一句话总结

> 领域层与数据层的**安全基本功（参数化、路径守卫、密码学、ReDoS 防护）明显优于同类个人项目**，性能工程已有产品级方法论沉淀；曾经的短板集中在三处：**Web 共享仍按"可信局域网"建模而非零信任**、**大图内存策略靠强制 GC 兜底**、**工程流水线与 UI 测试缺位**。截至 2026-09-10 已分别以「安全前提显式化 + 监听绑定收敛」（7.15）、「内存压力条件触发 + 解码上限」（P0-2 / P1-6）、「CI + LICENSE + UI 测试基建（388 用例）」（7.7 / 7.8）三项收口，仅剩 TLS 与 RBAC 属产品定位内的有意取舍。
