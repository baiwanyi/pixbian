# 2026-09-08

## 幻灯片 Ken Burns 幅度上调（用户反馈）
- 合成帧架构下边缘恒在视口外，幅度只受重采样爬行制约（照片边缘有模糊背景衬托，爬行基本不可见）→ 幅度可放开：`KenBurnsZoomScale` 1.08 → **1.15**、`KenBurnsPanScale` 1.04 → **1.06**、`KenBurnsPanRatio` 1.5% → **2.5%**（余量 3%，约束 `ratio ≤ (panScale-1)/2` 不变）。时长保持 10s 固定。构建 0 警告 0 错误，已启动（PID 20896）。未提交（单文件微调，随下次一并提交）。

## 幻灯片放映工具栏重构（对标 Windows 照片应用）
- 需求：浮动 UI 统一显隐（进入立即显示 / 3s 淡出 / 点击画面 toggle / 悬停暂停计时）、左上退出、左右翻页窄条（屏幕两侧垂直居中）、底部工具栏 6 按钮（选项 / 重放 / 播放暂停 / 播放顺序三态 / 声音 / 全屏切换）、顶部渐变遮罩（80px，`#8C000000`→透明，不拦截点击）、移除原关闭按钮与位置文本、选项 Flyout 四开关（动画 / 虚化 / 完整视频 / 音乐播放）与设置页写回同一份设置。
- 实现：
  - `FullscreenWindowBase` 增加全屏↔窗口双形态：`ToggleFullscreen()` + `IsFullscreen`；样式位必须对称增删（窗口态恢复 `WS_CAPTION|WS_THICKFRAME` 才能拖动调整），presenter 存字段；窗口态 1280×800 钳制工作区居中 + 取消置顶 + 恢复默认圆角，全屏态反向。`SlideShowWindow` 无需改动（基类能力直用）。
  - 显隐收口：遮罩 / 退出 / 左翻 / 右翻 / 工具栏五个浮动元素全收进 `OverlayLayer` 单容器，动画与 `IsHitTestVisible` 只作用于容器；`HideOverlay` 前查 `OptionsFlyout.IsOpen` 防浮层悬空。
  - 设置写回：放映页注入 `SettingsViewModel`（DI 单例），开关 Toggled → 属性 setter 落盘广播 → MainWindow.ApplySettings → 放映 VM 即时生效；`SlideShowOrder`/`SlideShowSilentPlayback` setter 原本就有。`ApplySettings` 补 `PlayOrder`/`IsSilentPlayback` 的 OnPropertyChanged 驱动图标。
  - 重放：VM 新增 `RestartCommand`（`Stop` → `sequencer.Reset(count,0)` → `Start` → `LoadCurrentAsync`），随机序经 Start 内重洗从首条开始。
  - 图标：播放 E768/E769、顺序 EA37(列表)/E8B1(随机)/E8EE(循环)、声音 E767/E74F、全屏 E740/E73F；点击处理器图标先行切换给即时反馈，广播回流校准。
  - 坑：Flyout 开关程序化赋 `IsOn` 会触发 Toggled → `_isSyncingOptions` 防重入；勿引不存在的 `StaticResource`（XAML 能编译但启动崩）。
- 构建修一处 CA1822（`SetCornerPreference` 改 static）。构建 0 警告，测试 234 全过（Core 189 / WebServer 33 / Imaging 12）。涉及 4 文件未提交。

## 放映工具栏四问题修复（用户验收反馈）
- ① 窗口态标题栏按钮背景色块 → `SetFullscreen(false)` 改 `ExtendsContentIntoTitleBar=true` + `AppWindow.TitleBar.Button{,Inactive}{Background,Foreground}Color` 显式透明/白（按钮前景不随主题，必须四组全设）；全屏态恢复 false。
- ② 工具栏移顶部：`ToolbarRoot` 改 `VerticalAlignment=Top`、Margin 0,10,0,0；左右翻页保持两侧垂直居中。
- ③ 「工具栏按钮变黑」元凶：ToggleButton 未选中态的悬停/按下走 `ToggleButtonBackgroundPointerOver/Pressed` 键，浅色主题默认半透明黑，深底上即黑块 → 工具栏/退出/翻页按钮全量补 Button Focused 与 ToggleButton 全态键覆盖。
- ④ **放映内选项设置不生效的真因：`MainWindow.ApplySettings` 只推了查看器与短片页，从未推放映视图模型**（原链路只在 OpenSlideShowAsync 打开时推一次）→ 补 `_slideShowWindow?.Page.ViewModel.ApplySettings(settings)`。
- 窗口态拖动区：顶部延伸后默认全宽拖区会吞顶部按钮点击 → 基类给安全拖区（`GetDpiForWindow` 算物理高度、避开右上系统按钮 150 逻辑 px），页面 `RootLayer.SizeChanged` → `Owner.UpdateTitleBarDragRegions` 精确划两段（退出按钮右侧→工具栏左侧、工具栏右侧→系统按钮左侧，`AppWindow.TitleBar.SetDragRectangles`）。
- 坑：`SystemButtonReserve` private 被子类访问 → CS0122 连锁引发全项目 XAML "Unknown type" 假错误（WMC1509/WMC0110），修复 CS 后全部消失——再次验证「先修 CS」判据。构建 0 警告、测试 234 全过，涉及 5 文件未提交。

## 放映浮动 UI 三项微调（用户反馈）
- 删左上角退出按钮（仅 Esc 退出）；窗口态拖动区左段改从窗口左缘起。
- 工具栏距顶 16px（布局位），进场动画 = 渐入 + `ToolbarShift.Y` 从 -6 滑到 0（视觉即 top 10→16）；消失仅渐出。
- 左右翻页按钮进场 = 渐入 + X 从 ∓6（贴边）滑到当前位置；滑入距离常量 `OverlayEnterOffset=6`。
- 关键点：进场动画 HoldEnd 钉住终值 → 每轮 Show 前 Stop `_overlayShowStoryboard` 解除钉住再 Begin（显式 from 动画即初值，无需手动复位变换）。构建 0 警告。3 文件未提交。

## 分类菜单形态对齐图库（用户预期修正）
- 现状误判澄清：点分类子项本就进 GalleryPage 过滤（OnNavigationSelectionChanged → SelectCategory → ShowPage(_galleryPage)），未实现的是第 1/2 点。
- 根因：RefreshCategoryItems 只 Clear+Add，缺图库 RefreshLibraryFolderItems 的「先收起再展开」重放 → NavigationView 只在父项 IsExpanded 值变化时重算扁平子项，动态加入的分类子项不显示。
- 修复：① MainWindow.xaml 分类行删行内 Grid + RefreshCategoriesButton，恢复 Content="分类" + IsExpanded="True"，展开/收起用控件自带 chevron（与图库一致，不移植「点行撤销切换」逻辑——分类行 SelectsOnInvoked=False 无导航目标，点行切换展开即自然交互）；② cs 删 OnRefreshCategoriesClick（批量重匹配入口保留在 CategoryPage）；③ RefreshCategoryItems 补 wasExpanded 收起/展开重放。
- 构建 0 警告 0 错误。涉及 2 文件未提交。

## 分类设置页按原型重构（三行式 + 编辑能力）
- 原型：设置页「分类」区块 = 分类管理（展开列分类：开关+名称+✏+🗑）/ 规则管理（展开列规则：开关+名称+正则+所属分类+✏+🗑）/ 重新匹配固定行；前两行互斥手风琴。用户拍板：分类开关=仅停止参与匹配（导航/归属不动）。
- 新能力落地：
  - Category 加 IsEnabled；Schema v6（categories 加 is_enabled DEFAULT 1，CurrentVersion=6）；ICategoryRepository 加 UpdateAsync/SetEnabledAsync；GetEnabledAsync 改 SQL join categories 过滤禁用分类（c.is_enabled=1）。
  - CategoryViewModel 重写：新建/编辑共用表单草稿（EditingRuleId/EditingCategoryId 分流 Save*Async）、分类重名校验（排除自身）、RuleDisplay(Rule, CategoryName) 展示包装解决 DataTemplate 内 x:Bind 无法引用页面属性的约束。
  - CategoryPage 重排三行式；新建/编辑共用 ContentDialog（表单面板常驻页面 Collapsed、弹出移交 Content，x:Bind 保持有效）；Primary 按钮经 VM.PropertyChanged 动态启停；Expander 手风琴互斥（_isSyncingSectionExpansion 防回声）；header 内按钮 Tapped handled 防 Expander 误 toggle。
  - SettingsPage 外层 header 文案「分类管理」→「分类」。
- 实测：VM 里禁用分类时「没有可参与的规则（规则或其分类未启用）」提示文案；删除分类文案明确级联删规则。
- 坑：CS0246/CS0103 连锁引发全项目 XAML 假错误再次出现（TappedRoutedEventArgs 缺 using、Expander 缺 x:Name）——先修 CS 即全消。
- 测试 237 全过（Core 192 含新增 3 用例：禁用分类过滤/改名/默认启用；WebServer 33；Imaging 12）。构建 0 警告。涉及 9 文件未提交。

## 分类区块视觉对齐「媒体库」（用户验收反馈）
- 用户给的参照是设置页「媒体库」区块：单卡片多行、行间 1px 分隔线、Expander 深度定制（Resources 覆盖 Border/Header/Content 六键透明 + Padding=0 + Loaded 经 FindDescendantByName 归零模板 ExpanderHeader 的 ToggleButton 内边距）、Header 用 SettingsRowStyle 三列（图标 20px SettingsRowIconStyle + 标题/动态说明副标题 + 右侧按钮）、说明文字承载动态计数。
- 改造：CategoryPage 重写为同构单卡片三行（分类管理/规则管理 Expander + 重新匹配固定行）；VM 拆 CategoryCountText/RuleCountText 两行副标题，StatusText 专用于操作反馈；列表容器 Padding=0/MinHeight 24、条目行 Padding 24,12 + 顶部分隔线，全对齐媒体库。
- 样式迁移：SettingsRowStyle/SettingsRowIconStyle/SettingsRowIconSpacerStyle 从 SettingsPage.Resources 提升到 App.xaml「③ 共享样式」——嵌入设置页的页面构造时不在宿主视觉树内，Page 级 StaticResource 有启动崩风险，跨页共享样式一律放 App 级。
- 构建 0 警告。累计 11 文件未提交。

## 去掉分类组外层 Expander（用户验收反馈）
- 设置页「分类」区块改为与「媒体库」组同构：组标题 TextBlock「分类」+ 直接装分类页卡片，不再有外层 Expander；懒装载（OnCategoryExpanderExpanding）随之删除，CategoryHost.Content 在设置页构造时直接赋值（数据加载仍由分类页自身 Loaded 驱动）。构建 0 警告。涉及 2 文件（SettingsPage.xaml/.cs）。

## 「新建分类/新建规则」点击无效修复
- 根因：对话框 Content 用的 CategoryFormPanel/RuleFormPanel 常驻页面根 Grid 视觉树，XAML 元素不能同时有两个父级 → ShowAsync 抛「already the child of another element」→ App 层 UnhandledException 处理器（写 crash.log + e.Handled）吞掉 → 表现即「点击无效」无任何反馈。
- 修复：根 Grid 命名 RootGrid；弹出前 `Children.Remove(panel)`、finally 里 `Children.Add(panel)` 归还；x:Bind 保持有效（同一 namescope）。
- 排查提醒：WinUI「点击无效」且无崩溃 → 先查 crash.log（App 吞了未处理异常）。构建 0 警告。

## 对话框内容空白修复（用户验收反馈）
- 第二层坑：表单面板 XAML 本地值 Visibility="Collapsed" 会随元素带进 ContentDialog.Content——ContentDialog 不会自动展开 Content，对话框标题/按钮正常但内容区空白。
- 修复：ShowCategoryDialogAsync / ShowRuleDialogAsync 摘除面板后置 Visible，finally 归还时复回 Collapsed。ContentDialog 主题随 XamlRoot ActualTheme 自动浅/深切换，表单无硬编码色无需适配。构建 0 警告。

## 「新建规则」不弹出且污染后续对话框（crash.log 实锤）
- 根因：「归入分类」ComboBox `SelectedValue` 用 x:Bind TwoWay 绑 VM 的 long 属性——分类列表为空/重建时 Selector 把 SelectedValue 置 null，TwoWay 回写拆箱 null → NRE（crash.log：UpdateTwoWay_8_SelectedValue），异常打断弹层 → 对话框不弹且后续 ContentDialog 全部失效。
- 修复：SelectedValue 改 OneWay（VM→UI），UI→VM 由 SelectionChanged 手动回写（`SelectedValue is long` null 安全归零）。SelectedIndex 绑 int 无拆箱风险可保留 TwoWay。
- 方法论：对话框/弹窗类「点击无效」直接查 `%LOCALAPPDATA%\Pixbian\Logs\crash.log`，App.UnhandledException 会把真实异常全量落盘。

## 复用面板的对话框双坑（crash.log 实锤）
- 坑 A「有几率不弹出」：ContentDialog 关闭后**不会清空对 Content 的引用**，面板可能仍挂在旧对话框 ContentPresenter（直到 dialog 被 GC）→ 下次 set_Content 概率性抛「already the child of another element」→ 修复：ShowAsync 完成后 finally 里显式 `dialog.Content = null` 再归还面板。
- 坑 B「文字不显示/白底」：弹层主题不跟随 root.RequestedTheme 自动继承，深色应用下对话框可能浅色底 + 混合主题求值 → Header/CheckBox/TextBlock 白字白底不可见。修复：`dialog.RequestedTheme = ActualTheme` 显式对齐。
- 两条均属「复用页面元素作对话框 Content」模式的必要配套。构建 0 警告。

## 分类条目行按钮样式统一（用户验收反馈）
- 编辑/删除按钮去掉 `Background="Transparent"` 本地值（本地值压掉 hover 反馈——项目已知坑），改 `Style="{StaticResource PixbianIconButtonBaseStyle}"`，悬停背景由 App 级 ButtonBackgroundPointerOver 主题键提供（浅色 8% 黑 / 深色 10% 白）。
- 删除按钮 FontIcon 加 `Foreground="{StaticResource PixbianDeleteForeground}"`（#FF99A4 柔和红，全应用「删除」语义色）。构建 0 警告。

## 删除色主题化 + 浅色悬停浓度（用户验收反馈）
- PixbianDeleteForeground 从 ④ 常量区迁入 ② ThemeDictionaries（浅色 #C42B1C / 深色 #FF99A4）；全部 XAML 引用 StaticResource → ThemeResource（Style Setter 内 ThemeResource 随主题重求值 ✓）；ShortPage 项级 StaticResource 别名在两主题分色后会钉死主题 → 改项级 ThemeDictionaries 直写两色。
- 代码后置取键：ThemeDictionaries 键不能顶层索引直取，需 `(ResourceDictionary)Resources.ThemeDictionaries[themeKey]["键"]`；GalleryPage 静态构建方法加 ElementTheme 参数、MainWindow 用 Content 元素 ActualTheme。
- 浅色悬停不可见根因：App 级 ButtonBackgroundPointerOver Default=8% 黑在白卡片对比度过低；该键是全应用默认不能动 → 两个 ListView.Resources 用 ThemeDictionaries 仅覆盖 Default 的悬停/按下键（Dark 缺省回落 App 级白色值）。首版写死 #1F000000 用户嫌过深 → 改引用系统 subtle 标准键 SubtleFillColorTertiaryBrush（悬停，约 5% 黑）/ SecondaryBrush（按下），与 Expander 箭头、ComboBox 下拉等内嵌按钮一致。
- 构建 0 警告。涉及 7 文件未提交（累计 14）。

## 放映工具栏补充修复
- 翻页按钮悬停图标变黑：漏覆盖 `ButtonForegroundPointerOver/Pressed/Focused`（浅色主题默认黑前景）→ 补白色；悬停背景改纯黑系连续加深（`#66000000`→`#73000000`→`#80262626`），消除色系跳变。
- 四按钮 ToolTip 随状态动态同步（`ToolTipService.SetToolTip`）：播放/暂停、静音/声音、退出/进入全屏显示「点击将执行的动作」，播放顺序显示当前模式名（顺序播放/随机播放/循环播放）；同步收口在各 Sync 方法（图标与标签同处更新）。声音按钮后按用户要求改为显示当前状态（有声 →「声音」、静音 →「静音」）。构建 0 警告。

## 图库列表性能优化方案 v2（审计 + 重写落盘）
- 对 v1 方案（docs/图库列表性能优化方案.md）做完整审计：R1–R7 逐条对照源码全部属实；发现 v1 内部矛盾 2 处（7.1 方案 B 残留 vs 决策 A；7.4.1 `ElementRealizationOptions.RecycleElement` 枚举不存在）、验证判据错误 1 处（THUMBSUBMIT 实际序列 200→300→500→700→900，非 v1 所写 200→400→600）。
- **新发现关键事实**：非虚拟化面板容器生成后永不回收重建，`ContainerContentChanging` 仅 realize 时触发一次 → 被头部瘦身置空的条目唯一重解触发点就是 R1 全量扫描；v1 的 P0-1 单独实施会让历史条目永久骨架屏。
- v2 方案（docs/图库列表性能优化方案-v2.md，另存保留 v1）：P0 重构为五步强耦合组合拳（行偏移表提前暴露 + pending=added + _evicted 恢复 + 视口区间驱动取消 + 订阅脏标记 + 删埋点）；P1a 补滚动条震荡迟滞防护；P1b 明确 DispatcherQueueTimer 驱动（禁用 CompositionTarget.Rendering）+ LRU 驱逐回调回 UI 线程；P2 修正枚举用法。工作量合计 9.5~14 人日。用户已确认按 v2 实施。
- 涉及文件：仅新增文档 1 个（docs/图库列表性能优化方案-v2.md），未动代码。

## 阶段 0 验证完成（R1/R2 确认）
- 流程：归档旧日志（diag-20260908-163507.log）→ Release 构建 0 警告（46s）→ 用户手动复现（图库 5 次触底翻页至 1000 条）→ 关闭应用 → 跑 C:\Temp\Pixbian-Diag.ps1。
- **R1 确认**：THUMBSUBMIT 序列 200 → 300 → 504 → 700 → 906（翻页 3~7），与 v2 修正判据 200/300/500/700/900 几乎逐位吻合（+4/+6 为少量杂音）——v2 判据精确命中，自激放大实锤（第 5 页单次提交 906 条）。
- **R2 确认**：PANEL 采样 max children=1000 且从不回落（5 页翻满后全部容器常驻）；measures/500ms=3，布局频率本身不高——瓶颈在常驻容器规模而非测量频率。
- 综合：LOADTOTAL first=3299ms → last=4176ms（+26%）；磁盘命中 70.4%（首过新目录属正常）；无解码失败。
- 下一步：按 v2 方案 P0 五步整组实施（删埋点 → 行偏移表 → pending=added+_evicted → 视口驱动 → 脏标记），涉及约 6 文件。
