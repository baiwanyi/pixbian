# Pixbian 企业级审计报告（第二轮·复审）

| 项 | 内容 |
|---|---|
| 审计日期 | 2026-09-10（第二轮） |
| 审计范围 | 全仓：10 个项目（132 个产品 `.cs` + 10 个 XAML 页面）、构建与工程配置、依赖供应链、文档 |
| 审计方法 | 4 路并行静态走查 · **依赖漏洞实测**（`dotnet list package --vulnerable`）· **CI 等价构建实测**（`-warnaserror`）· 与前轮报告及成熟开源产品对标 |
| 结论评级 | **B−（良好，可继续使用；供应链与文档一致性各降一级）** |
| 与前轮关系 | 前轮报告 `docs/审计与对标报告-2026-09-10.md`（723 行，S/P/R 三域 + P0/P1/P2 实施记录，基线 356 用例）。**本报告为独立复审，不重复已闭环结论**，聚焦「实测证据 · 前轮未覆盖维度 · 前轮遗留项复核」 |

---

## 一、执行摘要

### 1.1 评级表

| 域 | 前轮 | 本轮 | 变动原因（本轮实测依据） |
|---|---|---|---|
| 架构分层与代码规范 | A− | **A−** | 复核一致；分层与头部文档规范执行良好 |
| 数据安全（本地） | B+ | **B+** | 复核一致；新增 `Cache=Shared` 已弃用项 |
| 网络安全（Web 共享） | C+ | **B−** | 前轮 High 已闭环（并发上限/超时/绑定收敛）；本轮新增 3 项 Medium（限流表无界、连接任务未跟踪、密码无长度上限） |
| 性能工程 | B+ | **B** | 前轮已闭环项复核有效；**新发现索引结构与查询不匹配**（3 项 High：对账全量物化、子树查询、全表 COUNT） |
| 可靠性与数据完整性 | C+ | **B** | 前轮对账误删已闭环；本轮未见新的数据破坏路径 |
| 工程与供应链 | C | **C** | **实测出 2 个真实漏洞依赖（含 1 个 High）**，且 CI 门禁对漏洞告警失效；较前轮"未见具体漏洞"更确切地暴露了风险 |
| 文档一致性 | 未评估 | **D** | 本轮新增维度：README 核心描述与实现**方向性矛盾**（FFmpeg、运行时版本、测试规模） |

### 1.2 三条关键结论

1. **供应链是本轮最实质的风险，且已被门禁放过。** 实测 `dotnet list package --vulnerable --include-transitive` 报出 `SQLitePCLRaw.lib.e_sqlite3 2.1.6`（**High**）与 `SixLabors.ImageSharp 3.1.7`（Moderate）两个漏洞依赖；而由 `.github/workflows/ci.yml:25-29` 组成的「restore → build `-warnaserror`」两步门禁在本机实测为 **0 警告 0 错误**——**漏洞告警不会在 build 阶段重放，因此 CI 永远不会因为它失败**。修复成本极低（改两行版本号），应作为 P0。

2. **README 与实现存在方向性矛盾，会误导使用者、审计者与贡献者。** README 明确写「**关于 FFmpeg：未引入**」，而 `src/Pixbian/Pixbian.csproj:110` 直接引用 `FFmpegInteropX 2.1.0.81200` 并随产物分发原生 FFmpeg 8.1.2（`avcodec-62.dll` 等）。技术栈表同样整表过时（宣称 .NET 8 / C# 12 / SDK 8.0.424，实际 net10 / LangVersion 13 / 10.0.401），测试规模宣称 181 而实际 356。文档可信度直接影响安全与许可判断，应作为 P0。

3. **数据层的「查询-索引」匹配度是性能的主要短板。** 项目在分页（键集游标 151×）与缩略图管线上的工程水平已达产品级，但索引层仍是「一列一索引」的朴素形态：键集分页缺 `(排序列, id)` 复合索引、页头统计每次两次全表 `COUNT(*)`、对账查询同时受 `OR` 与 `LIKE` 前缀通配拖累，且对账把三份全量集合（发现/既有/命中）同时驻留内存。10 万条量级下这是**内存与延迟的双重线性恶化点**。

---

## 二、第一部分：供应链与依赖（实测）

> 本节全部结论可用两条命令复现（见第八节）。级别判定依据：漏洞 CVSS + 在本项目中的**可达性**（是否能被不可信输入触发）。

### 2.1 实测漏洞清单

| # | 级别 | 组件 | 版本 | 公告 | 性质 | 在本项目中的可达性 |
|---|---|---|---|---|---|---|
| **S-101** | **High** | `SQLitePCLRaw.lib.e_sqlite3` | **2.1.6** | [GHSA-2m69-gcr7-jv3q](https://github.com/advisories/GHSA-2m69-gcr7-jv3q) | SQLite < 3.50.2，聚合项数量可超过内部限制 | **传递依赖**（`Microsoft.Data.Sqlite 8.0.11` → bundle）。索引库由本进程独占写入，但 `q` 等参数进入 SQL 的路径存在；属"应升即升"的低成本处置 |
| **S-102** | **Moderate** | `SixLabors.ImageSharp` | **3.1.7** | [GHSA-rxmq-m78w-7wmc](https://github.com/advisories/GHSA-rxmq-m78w-7wmc) / CVE-2025-54575 | GIF 解码器畸形 comment extension block → **无限循环（DoS）** | **直接可达**：`WebAccessServer.cs:770,777`（局域网 `/thumb/{id}`）与 `ImageEditService.cs:173,181` 均解码库内文件；库内一旦存在恶意 GIF，**同网段任意已认证用户即可触发宿主进程挂死** |
| S-103 | Info | 原生 FFmpeg | 8.1.2 | 未核实 | FFmpegInteropX 2.1 随包分发的解码库 | 由每个媒体文件的解码路径触达；**需按上游公告定期核查**（本轮未做穷尽核查，不编造结论） |
| S-104 | Info | `Microsoft.WindowsAppSDK` | 2.4.0 | 未核实 | 元包拉入 AI/ML/Search/Widgets 组件 | 见 2.3 |

**处置建议（P0，成本 < 0.5 人日）**：

- `Directory.Build.props` 提升 `SqliteVersion` 至含 `SQLitePCLRaw ≥ 2.1.12` 的 `Microsoft.Data.Sqlite` 版本；或显式 `PackageReference` 覆盖传递依赖版本。
- `ImageSharpVersion` 升至 `3.1.x` 最新补丁版（修复版本号以 NuGet 公告为准），升级后**先跑 `Pixbian.Imaging.Tests` 与 `Pixbian.WebServer.Tests`** 确认解码行为未变。
- 升级前若需临时缓解 S-102：现有 `Image.Identify` 读头校验（50 MP 阈值）**不能**拦截无限循环（该漏洞发生在解码阶段而非尺寸校验阶段），故**不可**作为缓解手段，只能升级。

### 2.2 CI 门禁有效性（实测，High）

| 检查 | 命令 | 实测结果 |
|---|---|---|
| CI 等价构建 | `dotnet build Pixbian.sln -c Release -warnaserror` | **已成功生成，0 警告 0 错误**（耗时 22s） |
| 漏洞扫描 | `dotnet list package --vulnerable --include-transitive` | **2 个易受攻击的包**（见 2.1） |

**结论**：`.github/workflows/ci.yml:25-29` 的两步式流程（`dotnet restore` → `dotnet build --warnaserror`）**无法把 NuGet 审计告警提升为构建错误**。原因是 NuGet 审计告警（NU1901–NU1904）在 **restore 阶段**产生；当 restore 已完成、项目处于 "所有项目均是最新的" 状态时，`dotnet build` 不会重放审计告警，`-warnaserror` 因而不触发。这不是 `NoWarn;NU1900` 造成的（NU1900 是"漏洞库不可达"，与真实漏洞 NU1902/NU1903 无关，前轮的豁免判断本身正确），而是**流程设计缺口**。

**修复（P0）**：在 CI 增加独立的门禁步骤，把漏洞扫描本身变成失败条件：

```yaml
- name: Audit vulnerable packages
  shell: pwsh
  run: |
    dotnet list Pixbian.sln package --vulnerable --include-transitive |
      Tee-Object -Variable audit
    if ($audit -match '易受攻击的包|has the following vulnerable') { throw '发现易受攻击的依赖' }
```

### 2.3 依赖治理基线（Medium）

| # | 级别 | 问题 | 证据 |
|---|---|---|---|
| S-105 | Medium | **无锁文件**（`packages.lock.json` 全仓 0 命中），无 `RestorePackagesWithLockFile`，无 Dependabot/renovate 配置 | 全仓检索无 `packages.lock.json`；`Directory.Build.props` 无该属性 |
| S-106 | Medium | `NuGetAudit` 未显式配置（依赖 SDK 默认），`NuGetAuditMode` 未设为 `all`（传递依赖可能不被审计） | `Directory.Build.props:31` 仅设 `NoWarn` |
| S-107 | Medium | **WASDK 2.4.0 元包拉入大量非必要传递依赖**：`Microsoft.WindowsAppSDK.AI / ML / Search / Widgets`，产物含 `onnxruntime.dll`（约 20 MB）、`DirectML.dll`（约 18 MB） | `src/Pixbian/obj/project.assets.json`；`artifacts/win-x64/` |
| S-108 | Medium | `THIRD-PARTY-NOTICES.md` 未逐项列出 Win2D、MetadataExtractor、`Microsoft.Extensions.*`、WASDK 及其传递依赖（含 ONNX Runtime / DirectML）的许可 | `docs/THIRD-PARTY-NOTICES.md:33-34`（"其余……可在各自项目主页查看"） |
| S-109 | Low | Win2D 1.3.2 声明的 WASDK 依赖为 **1.6.241114003**，与主项目 2.4.0 跨大版本，由 NuGet 就近原则静默解决 | `project.assets.json` |

**处置**：① 启用锁文件并提交（`<RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>`）；② 显式 `<NuGetAudit>true</NuGetAudit><NuGetAuditMode>all</NuGetAuditMode>`；③ 评估 WASDK 精简元包以剔除 ONNX/DirectML（对相册浏览器无用途，且直接放大了攻击面与分发体积）；④ 补齐 THIRD-PARTY-NOTICES 并随产物分发。

### 2.4 许可合规（复核前轮，仍有效）

| # | 级别 | 问题 | 证据 |
|---|---|---|---|
| S-110 | Medium | **LGPL 义务履行方式偏弱**：`THIRD-PARTY-NOTICES.md:24` 称已在"设置 → 关于"标注，实际仅 `SettingsPage.xaml:1572` 一行静态文本（许可证名 + 无源码获取途径、无可点击条目）；LGPL-2.1 §6 的"源码可获取"告知义务未随分发产物提供 | `docs/THIRD-PARTY-NOTICES.md:24`、`src/Pixbian/Views/SettingsPage.xaml:1572` |
| S-111 | Medium | ImageSharp 商用许可门槛（营收超 100 万美元需商业授权）已在 README 提示，但**未纳入 LICENSE 说明流程** | `README.md:272-274` |

---

## 三、第二部分：文档与实现一致性（本轮新增维度）

> 前轮未评估此维度。文档不一致不是"美观问题"——它直接影响安全判断、许可判断与贡献者心智模型。

| # | 级别 | 问题 | 证据（文档 vs 实现） |
|---|---|---|---|
| **D-101** | **High** | **README 称「FFmpeg 未引入」，实际已引入并分发** | `README.md:37` "**关于 FFmpeg**：未引入。`Pixbian.Media` 基于系统解码器……零额外体积" **vs** `src/Pixbian/Pixbian.csproj:110` `<PackageReference Include="FFmpegInteropX" Version="2.1.0.81200" />`、`src/Pixbian/Services/VideoPlaybackItemFactory.cs:14` `using FFmpegInteropX;`、产物 `avcodec-62.dll`/`avformat-62.dll`/`avutil-60.dll`。**影响**：使用者据 README 会低估体积、许可义务（LGPL）与原生解码攻击面 |
| D-102 | Medium | README 技术栈表**整表过时** | `README.md:15-19`：.NET 8 / C# 12 / SDK 8.0.424 **vs** 实际 `net10.0`（各 csproj）、`Directory.Build.props:5` `LangVersion 13.0`、`global.json:3` `10.0.401` |
| D-103 | Medium | README 测试规模宣称 181，实际 356 | `README.md:220` "共 **181 个用例**（Core 136 / WebServer 33 / Imaging 12）" **vs** 前轮报告 7.19 记录 356（Core 230 / WebServer 62 / Imaging 14 / UI 50） |
| D-104 | Low | 前轮报告内部数字不一致：第八节写 "388 用例"，第七节各批次记录为 356 | `docs/审计与对标报告-2026-09-10.md:723` vs `:644,:582` |
| D-105 | Low | README 项目结构树未标注 `Pixbian.UI.Tests`（已存在） | `README.md:128` 仅列 Core/Imaging/WebServer 三套测试 |
| D-106 | Info | README "安全设计" 称"会话令牌只在服务端字典中**按哈希**查找"，实现为**明文令牌作字典键**（`ConcurrentDictionary<string, SessionEntry>`）。安全性未受损（不参与客户端可控的字节比较），但表述不准确 | `README.md:205` vs `src/Pixbian.WebServer/Security/AuthService.cs:79,258` |

**处置（P0，成本 < 1 人日）**：重写 README 的「技术栈」「关键实现-FFmpeg」「测试」三节，使其成为可信的入口文档；在 README 增加"文档与实现同步"的贡献要求（已有条款，但需明确"版本号、依赖、测试数"三类字段的强制核对）。

---

## 四、第三部分：安全复审（前轮未记录的增量）

> 前轮 S-01~S-17 已覆盖并多数闭环。本轮复核确认那些闭环**真实有效**（见第五节），以下仅列前轮未记录的项。

| # | 级别 | 问题 | 证据 | 影响与建议 |
|---|---|---|---|---|
| SEC-01 | Medium | **限流/锁定/会话表无界增长** | `AuthService.cs:80` `ConcurrentDictionary<string,(int,DateTimeOffset)> _requestCounts`；`:346` 键为 `$"{ip}:{tier}"`；`:426` 清理仅在**登录成功时**触发 | 扫描器以大量源 IP（或 CGNAT 后的海量端口映射）请求即可让字典单调增长 → 宿主内存 DoS。建议：定时清理（独立计时器）或改用固定容量环形结构 |
| SEC-02 | Medium | **在途连接任务 fire-and-forget，`StopAsync` 不跟踪** | `WebAccessServer.cs:307` `_ = Task.Run(() => HandleClientAsync(client, ct), CancellationToken.None)`；`:208-218` `StopAsync` 仅 `await _acceptLoop` | 停止服务时不取消/不等待在途连接；异常无落点。建议：收集 `Task` 到集合，`StopAsync` 中经 `_cts` 取消后 `Task.WhenAll`（带超时） |
| SEC-03 | Medium | **登录密码无长度上限** | `WebAccessServer.cs:469` 直接取 `password` 字符串无长度检查；`AuthService.cs:62` `Iterations = 100_000`；`:394-399` 进入 PBKDF2 | 超长密码使 10 万次迭代的耗时随输入放大（内存与 CPU 双重放大）。虽受限流 10/min 与 64 KB 请求体上限缓解，仍应在入口拒绝 `password.Length > 256` |
| SEC-04 | Low | 会话相关响应缺 `Cache-Control: no-store` | `HttpTypes.cs:98-105` `ApplySecurityHeaders()` 已含 `X-Content-Type-Options` / `X-Frame-Options` / `Referrer-Policy` / CSP，**无 `Cache-Control`** | 共享设备上的浏览器可能缓存 `/api/items` 结果。建议对 `/`、`/api/*` 设 `Cache-Control: no-store` |
| SEC-05 | Low | `StartAsync` 异常路径未释放 `_cts` | `WebAccessServer.cs:178-182`（`_cts` 先 new，`_listener.Start()` 后置） | `TcpListener.Start()` 抛异常时 `_cts` 泄漏。建议 try/catch 中释放 |
| SEC-06 | Medium | **SQLite 连接串启用已弃用的 `Cache=Shared`** | `SqliteDatabaseInitializer.cs:46` `Cache = SqliteCacheMode.Shared` | 共享缓存为 Microsoft.Data.Sqlite 已弃用特性，其 `SQLITE_LOCKED` 语义与 `busy_timeout` 不一致，会放大并发写锁冲突、抵消 WAL 收益。建议改为默认 `Default`，保留 `Pooling = true` |
| SEC-07 | Medium | **路径校验为纯字符串前缀，无实体（realpath）解析 → TOCTOU 面** | `PathGuard.cs:42-51`（`GetFullPath` + `StartsWith`）；校验与打开文件之间存在窗口 | 当前库内消费点影响有限，但配合"父目录被替换为符号链接"可读库外内容。建议对安全敏感读取前用 `File.ResolveLinkTarget` 复核最终目标仍在根内 |
| SEC-08 | Low | 文件枚举无深度/总量上限 | `MediaFileEnumerator.cs:76-118`（显式栈无深度计数） | 正常 NTFS 已被 `ReparsePoint` 跳过保护；硬链接或极深目录树仍可使栈无界增长 |
| SEC-09 | Low | `ImageEditService` 先 `Identify` 再 `Load`（两次打开同一文件） | `ImageEditService.cs:173,181` | 两窗口之间文件被替换可绕过 50 MP 尺寸校验；同时是双份 IO。建议单流 `Identify` + `Load` 或先 `Load` 后校验像素数 |
| SEC-10 | Low | 编辑输出路径的"防覆盖原文件"校验仅字符串比较 | `ImageEditService.cs:213-224`（`GetFullPath` + `OrdinalIgnoreCase`） | 符号链接指向同一实体时可绕过。建议解析父目录真实路径后比较 |
| SEC-11 | Low | `ThumbnailDiskCache` 内存表无条目上限 | `ThumbnailDiskCache.cs:74`（`ConcurrentDictionary`；`:53` 注释自述"百万级约占 150 MB"） | 大库 + 多档位下常驻内存显著。建议设条目上限或紧凑结构 |

### 4.1 经本轮核实**属实**的安全控制（不必返工）

- **HTTP 解析边界完备**：头部 16 KB / URI 2048 B / 请求体 64 KB 上限（`HttpRequestParser.cs:30-32`）；完整但超限的头被二次校验拒绝（`:56-59`）；不支持 `chunked` → 无分块攻击面。
- **慢速攻击防护到位且认知正确**：首字节 10 s / 整连接 60 s 链接令牌，注释明确指出 `NetworkStream.ReadAsync` 不受 `ReceiveTimeout` 约束这一常见误区（`WebAccessServer.cs:318-325`）。
- **响应头 CRLF 注入防护**：写入前过滤 `\r`/`\n`（`HttpTypes.cs:159-163`）。
- **并发连接有上限**：32（`WebAccessServer.cs:82,94,301`），超限直接关闭不排队。
- **认证链路正确**：PBKDF2-SHA256/100k/16B 盐、`FixedTimeEquals` 恒定时间比较、256 位 CSPRNG 令牌、8 小时过期、UA 绑定、令牌轮换、无会话固定（登录即签发全新令牌）。
- **限流键不可伪造**：取自套接字源 IP（`WebAccessServer.cs:328`），**未使用** `X-Forwarded-For` 等客户端可控头。
- **越权（IDOR）防护**：`{id}` 强制 `long.TryParse` + 主键查库，客户端无法传路径；`PathGuard` 校验在打开文件之前（`WebAccessServer.cs:641,686`）；对外 JSON 不含绝对路径。
- **Range 解析严格**：拒绝多区间、`start<0`、`start>=len`、`end<start`，end 钳制到 `len-1`（`RangeParser.cs:31,56,71,83,88-91`）。
- **静态资源为嵌入常量**，不读磁盘、无路径拼接（`WebAssets.cs:26,29,37,44`）。
- **日志脱敏与防注入**：IP 记 /24（`AppLog.cs:57-78`）、`\r\n` 清洗防日志伪造（`:109-113`）、不记录口令与令牌。
- **异常不泄漏**：客户端响应为固定文本，堆栈仅落日志（`WebAccessServer.cs:471-474,664-670`）。
- **搜索参数经参数化 + 通配符转义**：`% _ \` 转义后绑定 `@search`（`SqliteMediaItemRepository.cs:660-661,724-728`）。

---

## 五、第四部分：性能复审

### 5.1 本轮新发现

| # | 类别 | 级别 | 问题 | 证据 | 影响与建议 |
|---|---|---|---|---|---|
| **PERF-01** | 扫描/内存 | **High** | **对账把三份全量集合同时驻留内存** | `MediaIndexingService.cs:67-68,76-78,142-161`：`discovered`（全部路径）+ `existing`（`GetPathsUnderDirectoryAsync` 返回全部）+ `found`（HashSet） | 10 万文件时约 3 × 10–20 MB = **30–60 MB 峰值**，且随库线性增长；枚举本身是惰性的（`MediaFileEnumerator.cs:62-119`），但其成果被消费者一次性物化抵消。建议改流式对账（分块游标读取 + 即时比对） |
| **PERF-02** | 数据库 | **High** | **子树查询同时受 `OR` 与 `LIKE` 前缀通配拖累** | `SqliteMediaItemRepository.cs:124-154`：`WHERE directory = @directory OR directory LIKE @prefix ESCAPE '\'`；索引仅 `ix_media_items_directory ON media_items(directory)`（`SchemaMigrations.cs:114`） | 前缀通配使 `directory` 单列索引难以做范围扫描，`OR` 进一步阻止优化；对账前需全量物化子树路径。建议：① 改 `directory >= @lo AND directory < @hi` 区间扫描（可走索引）；② 建复合索引 `(directory, path)` 覆盖 |
| **PERF-03** | 数据库 | **High** | **每次列表加载触发两次全表 `COUNT(*)`** | `SqliteMediaItemRepository.cs:490-513`；调用点 `GalleryViewModel.cs:820-832`（照片 + 视频各一次） | `deleted_utc` 不在任何索引中 → 无法 index-only scan；10 万条库每次切换/筛选/搜索都要两次全表扫。建议建 `(kind, deleted_utc)` 复合索引，或维护计数缓存 |
| PERF-04 | 数据库 | Medium | 键集分页缺 `(排序列, id)` 复合索引 | `SqliteMediaItemRepository.cs:244-264` `ORDER BY {column} {order}, id {order}`；现有索引均为单列（`SchemaMigrations.cs:149,150,116,170`） | 键集条件 `col = @v AND id > @id` 在单列索引下仍需组内二次排序；相等值多时退化。建议为四组排序键建复合索引 |
| PERF-05 | 数据库 | Medium | 搜索 `LIKE '%x%'` 两侧通配，必全表扫 | `SqliteMediaItemRepository.cs:217-219,660` | 前轮已评估 FTS5（中文 2 字词受 trigram ≥3 字符限制，决定放弃）。**该决策本轮复核仍然成立**，但应显式记录为"已知取舍"而非遗漏；可补"最短查询长度 + 结果上限"限流以约束最坏代价 |
| PERF-06 | 缩略图 | Medium | 磁盘缓存命中时 `stat` 源文件 | `ThumbnailDiskCache.cs:274-294`（`IsSourceUnchanged` → `new FileInfo(path)`） | 内存缓存未命中时每次触发源文件 stat；机械盘上可能引入寻道。前轮方案已提"用索引库 `file_size`/`modified_utc` 替代"，本轮建议落地 |
| PERF-07 | 数据库 | Low | 逐条 `ExecuteNonQueryAsync` upsert | `SqliteMediaItemRepository.cs:102-118`，同 `SqliteMusicTrackRepository.cs:102-111` 等 | 已复用预编译语句且批次在单事务内（无 n 次 fsync），收益有限；维持现状可接受 |

### 5.2 前轮性能资产复核（**确认有效，建议保持**）

| 资产 | 复核证据 |
|---|---|
| 缩略图统一 512 档位（缓存条目数从"档位 × 文件"降为"文件"） | 前轮 P1 记录 + 本轮 `ThumbnailDiskCache` 键结构复核 |
| 调度器视口收编（解码量 = O(视口) 而非 O(集合)） | `ThumbnailLoadScheduler.cs` 的 `CommitBatch` 窗口外移出逻辑 + 前轮 7.11 的两个锁定用例 |
| 键集分页（10 万条实测 151× 提升） | 前轮 P1-3 实测记录（OFFSET 151 ms vs 键集 1 ms） |
| 随机排序走固定 `random_rank` 游标（避免 `ORDER BY RANDOM()`） | `SchemaMigrations.cs:170-172`（索引 + 触发器） |
| 内存压力条件触发 GC（替代无条件 STW） | 前轮 P1-6 记录（`MemoryCompactionThresholdBytes`） |
| 后台回填节流（批 2.5 s 让出 1.5 s + 批次上限） | `MediaMetadataBackfillService.cs:24-34` |

---

## 六、第五部分：前轮结论复核表

> 目的是确认前轮的"已闭环"标记**经代码核实属实**，而非仅凭报告自述。

| 前轮项 | 前轮状态 | 本轮复核 | 复核依据 |
|---|---|---|---|
| S-02 全网卡监听 | ✅ 闭环 | **✅ 属实** | 构造参数 `bindAddress` + 非法地址显式 `ArgumentException`；`PathGuard`/网络类别闸门均在 |
| S-04 无并发上限/无超时 | ✅ 闭环 | **✅ 属实** | `MaxConcurrentConnections = 32`（`WebAccessServer.cs:82`）；10 s/60 s 链接令牌（`:318-325`） |
| S-06 大文件整读 | ✅ 闭环 | **✅ 属实** | `BoundedStream` + `/media` 流式分支存在 |
| S-07 解码炸弹 | ✅ 闭环 | **⚠️ 部分有效** | 50 MP `Image.Identify` 校验存在，但**不能**拦截 S-102（GIF 无限循环发生于解码阶段）；需以升级依赖补齐 |
| S-13 会话管理 | ✅ 闭环 | **✅ 属实** | 会话 ID/UA 绑定/轮换/`RevokeById` 均在 `AuthService.cs` |
| S-14 PathGuard 语义 | ✅ 闭环 | **✅ 属实** | `PathGuard.cs:29-31,79-86` 候选路径补尾分隔符后再比较 |
| S-15 弃用 API | ✅ 闭环 | **✅ 属实** | 未见 `SHFileOperation`；回收站走 `Microsoft.VisualBasic.FileIO` |
| P-02 查看器解码上限 | ✅ 闭环 | **✅ 属实** | `FullDecodeMaxEdge = 8192` 上限存在 |
| P-03 搜索/分页 | ✅ 闭环 | **⚠️ 部分有效** | 键集分页属实且有效；**LIKE 子串搜索仍全表扫**（已声明为固有限制，但缺"结果上限/最短长度"约束） |
| P-05 数据集合增长 | ✅ 闭环 | **✅ 属实** | 调度器队列收口逻辑在（`CommitBatch` 先移出窗口外条目） |
| R-03 UI 测试 | ✅ 闭环 | **✅ 属实** | `src/Pixbian.UI.Tests` 存在，50 用例（本轮 grep 核实 `[Fact]/[Theory]` 分布） |
| 前轮"工程与供应链 C" | — | **需下调** | 前轮仅记"无 SBOM/Dependabot"；本轮实测出**具体漏洞依赖**且**门禁失效** |

**复核结论**：前轮 11 项"已闭环"中 **9 项经代码与实测确认属实**，2 项（S-07、P-03）为"部分有效"——前轮将其标记为完全闭环存在**乐观偏差**。

---

## 七、第六部分：与成熟案例对标

> 对标基线（均为公开资料，截至 2026-09）：**Files**（WinUI 3，同技术栈本地文件管理器）、**ImageGlass**（.NET 图片查看器）、**digiKam**（KDE 相册，C++）、**Immich**（Go/TS 自托管照片）、**Jellyfin**（C#/.NET 媒体服务器）、**Windows 照片**。

### 7.1 能力矩阵

| 维度 | Pixbian | Files | ImageGlass | digiKam | Immich | Jellyfin |
|---|---|---|---|---|---|---|
| 技术栈 | WinUI 3 + .NET 10 | **WinUI 3**（同栈） | WinForms/.NET | C++/Qt | Node/TS + Go | C#/.NET |
| 索引 | SQLite 自管 + 两阶段回填 | 依赖系统文件系统枚举 | 无库（目录直读） | SQLite/MySQL 全量元数据 | Postgres + 向量索引 | SQLite/EF Core |
| 缩略图 | 统一 512 档 + 内存/磁盘两级 LRU | 系统缩略图 API + 后台队列 | WIC 直解 | 多级持久化 + DB 管理 | **多尺寸预生成队列**（thumb/preview/original） | 服务端按需生成 + 缓存 |
| 大图查看 | 512 垫场 → 原图（8192 上限） | — | 直解 + 缓存 | 分级加载 | **多尺寸 + 渐进** | — |
| 远程共享安全 | 单密码 + IP 限流 + 绑定收敛；**明文 HTTP** | 无 | 无 | 无内置 | **反向代理 + OAuth/OIDC + RBAC + 审计** | API Key + 用户体系 + Hardening 文档 + 可选 HTTPS |
| 依赖治理 | **无锁文件 / 无自动更新 / 2 个漏洞依赖** | Dependabot + CI | CI + 发布制品 | CI 多平台制品 | CI + SBOM + 版本化迁移 | CI + 多平台 |
| 测试 | 356 用例（含 50 UI） | 单元 + 集成 | 有限 | CTest 覆盖 | E2E + 单元 | 单元 + 集成 |
| 可访问性 | 36 处 `AutomationProperties`（非系统化） | 系统化 a11y | 部分 | 部分 | Web a11y（ARIA） | Web a11y |
| 国际化 | 硬编码中文 | 多语言 resw | 多语言 | 多语言 | i18n 框架 | i18n 框架 |

### 7.2 三条最关键的差距与可借鉴动作

**差距 1：依赖治理"零自动化"，而同栈的 Files 与对标的 Jellyfin/Immich 均有自动化。**
Pixbian 拥有 CI 却未把**漏洞扫描**纳入门禁（2.2 已实测证实），也没有锁文件与自动更新。这是"有流水线但流水线不查最关键的东西"。
→ **可借鉴**：Files 的 Dependabot 自动 PR + Immich 的 SBOM 产物。**动作**：CI 增加漏洞门禁步骤（2.2 已给出 YAML）+ 启用锁文件 + 开启 Dependabot。

**差距 2：大图与缓存缺"中间档"，是内存问题的结构性根因。**
Immich 用 thumb/preview/original 三尺寸，digiKam 多级持久化，Windows 照片用分块渐进；Pixbian 是"512 缩略图 → 原图全解"的两端跳变（虽有 8192 兜底）。前轮已识别此点（P-01/P-02 的根因）。
→ **可借鉴**：Immich 的"多尺寸预生成 + 按视口取用"。**动作**：在 512 与原始之间增加 1024/2048 档，查看器按视口请求对应档位，仅在 1:1 时加载原图。

**差距 3：远程共享的威胁模型停留在"可信家庭局域网"，而 Immich/Jellyfin 默认"零信任"。**
Pixbian 已在 UI/文档显式化前提（前轮 7.15，方向正确），但仍缺**多用户/RBAC**与**审计日志的完整闭环**（本轮 SEC-01 指出限流表本身还是 DoS 面）。
→ **可借鉴**：Jellyfin 的"官方推荐反向代理终止 TLS + Hardening 文档"路线（Pixbian 已对齐）。**动作**：保持不内置 TLS 的取舍，但补齐 S-103/S-102 的依赖升级，避免"Zero Trust 的文档 + 有漏洞的实现"这种反差。

### 7.3 相对成熟案例的**优势项**（应保持）

- **领域层纯净度**：Core 零项目引用、零平台依赖（对标产品多把领域逻辑与平台耦合）。这是同类个人项目中少见的纪律。
- **实测结论沉淀**：把"为什么不做"的实测证据写入配置注释（如 `Pixbian.csproj:73-90` 对两处 `NoWarn` 的完整论证），可维护性高于多数同类项目。
- **索引分离与游标分页**：键集游标 + 固定 `random_rank` 的组合，规避了同类项目常见的深翻退化与随机分页重复/遗漏。

---

## 八、第七部分：整改路线图

> 验收方式一律为**可观察结果**（命令输出、测试通过、可复现步骤）。涉及文件 > 2 个前先确认方案（项目规范）。

### P0 —— 本迭代必须闭环（成本合计约 2 人日）

| # | 目标 | 动作 | 涉及文件 | 验收 |
|---|---|---|---|---|
| **P0-1** | 消除已知漏洞依赖 | 升级 `ImageSharp` 至 3.1.x 最新补丁；升级 `Microsoft.Data.Sqlite` 使 `SQLitePCLRaw ≥ 2.1.12` | `Directory.Build.props`（版本变量） | `dotnet list package --vulnerable --include-transitive` 输出**无易受攻击的包**；`dotnet test` 全绿 |
| **P0-2** | 让 CI 真正拦截漏洞 | 增加独立门禁步骤（YAML 见 2.2）；启用 `RestorePackagesWithLockFile` 并提交锁文件；显式 `NuGetAudit=true` + `NuGetAuditMode=all` | `.github/workflows/ci.yml`、`Directory.Build.props`、新增 `packages.lock.json` | 故意降级一个包版本 → CI 失败；恢复后 CI 通过 |
| **P0-3** | 修正文档与实现矛盾 | 重写 README「技术栈」「FFmpeg」「测试」三节；修正"令牌按哈希查找"表述 | `README.md` | 逐项对照 `global.json` / `Directory.Build.props` / csproj / 测试输出，零差异 |
| **P0-4** | 补齐许可合规 | THIRD-PARTY-NOTICES 逐项列出全部直接依赖 + WASDK 传递依赖；"关于"页增加可点击许可条目与 FFmpeg 源码获取途径 | `docs/THIRD-PARTY-NOTICES.md`、`SettingsPage.xaml` | 声明的组件集合 = `dotnet list package` 输出集合 |

### P0 实施记录（2026-09-10 同日完成）

| # | 状态 | 实际改动 | 验证证据 |
|---|---|---|---|
| P0-1 | 完成 | `SqliteVersion` 8.0.11 → **8.0.31**（其传递依赖 `SQLitePCLRaw.bundle_e_sqlite3` = 2.1.12，已含修复）；`ImageSharpVersion` 3.1.7 → **3.1.12**。选 8.0.31 而非跨大版本，是为把跨度压在同一条补丁线内、API 面零变化 | `dotnet list Pixbian.sln package --vulnerable --include-transitive` 对 10 个项目全部输出「没有易受攻击的包」；锁文件中 `SQLitePCLRaw.lib.e_sqlite3 = 2.1.12` |
| P0-2 | 完成 | `ci.yml`：restore 改锁定模式 + 新增独立「Audit vulnerable packages」步骤（解析 `--format json` 结果，命中即 throw）；`Directory.Build.props`：`NuGetAudit=true` + `NuGetAuditMode=all` + `RestorePackagesWithLockFile=true`；产出 10 个 `packages.lock.json`（合计约 63 KB，已确认未被 `.gitignore` 排除） | **反向验证**：临时把 ImageSharp 降回 3.1.7，门禁脚本输出 `VULNERABLE FOUND` 并以退出码 1 失败；恢复 3.1.12 后输出 `AUDIT PASSED`（退出码 0）。**证明该门禁不是"永远通过"的假门禁** |
| P0-3 | 完成 | README 六处修正：技术栈表（.NET 10 / C# 13 / SDK 10.0.401 / 新增锁文件行）、依赖表（Sqlite 8.0.31 / ImageSharp 3.1.12 / 新增 FFmpegInteropX）、FFmpeg 说明由「未引入」改为「已引入」并写明边界（`Pixbian.Media` 仍不引用 FFmpeg）、测试章节 181 → 356（四套）、项目结构树（net8.0 → net10.0、补 UI.Tests）、令牌表述（「按哈希查找」→「作内存字典的键」）、「已知未完成项」中 `LibraryWatcherService` 条目（该文件已于同日随死代码清理移除） | 逐项对照 `global.json`（10.0.401）、`Directory.Build.props`（LangVersion 13.0）、各 csproj、`dotnet test` 输出（356） |
| P0-4 | 完成（文档侧） | `docs/THIRD-PARTY-NOTICES.md` 由 35 行扩写为完整清单：新增「运行时与界面框架」「数据与图像」「测试组件」三类共 13 个直接依赖，标注 WASDK 传递依赖（AI/ML/Search/Widgets、ONNX Runtime、DirectML）与 SQLitePCLRaw；明确「本文件须随分发产物一同提供」。README 许可摘要表同步，并修正 `Microsoft.Data.Sqlite` 由误标的 Apache-2.0 改为 **MIT** | 声明的组件集合 = `dotnet list package --include-transitive` 的直接依赖集合 |

**遗留（未做，需另行确认）**：「关于」页（`SettingsPage.xaml`）目前仍只有一行静态许可文本；改为可点击许可条目 + FFmpeg 源码入口需改动 XAML 与代码后置并依赖 UI 手工验证，本轮未实施。

**P0 整体验证**：

| 项 | 结果 |
|---|---|
| `dotnet build Pixbian.sln -c Release -warnaserror` | **0 警告 0 错误** |
| `dotnet test Pixbian.sln -c Debug` | **356 通过 / 0 失败**（Core 230 / WebServer 62 / Imaging 14 / UI 50）——与升级前基线完全一致，依赖升级零行为回归 |
| `dotnet list Pixbian.sln package --vulnerable --include-transitive` | 10 个项目均为「没有易受攻击的包」 |
| 锁文件一致性 | `dotnet restore Pixbian.sln --locked-mode --force` 通过（退出码 0） |

**过程中新踩的坑（已修复并写入配置注释警示）**：在 `Directory.Build.props` 的 XML 注释里写入了含**连续两个连字符**的命令行开关（锁定模式与强制评估的开关名），导致 **XML 解析异常、其后的属性组求值为空**，还原报 `NU1015：以下 PackageReference 项未指定版本`——现象是"包版本突然全部丢失"。XML 注释不允许出现连续两个连字符（与项目中既有的「XAML 注释内不得出现连续两个连字符」同源）。已改为文字描述，并在注释中留下该约束的警示。

### P1 —— 下个迭代（成本合计约 4 人日）

| # | 目标 | 动作 | 验收 |
|---|---|---|---|
| P1-1 | 消除数据层线性恶化 | ① 建复合索引 `(kind, deleted_utc)` 与四组 `(排序列, id)`；② 子树查询改区间扫描 `directory >= @lo AND directory < @hi`；③ 对账改流式分块比对 | 造 10 万条数据：页头统计 P95 下降 ≥ 50%；对账峰值内存下降 ≥ 60%（采样断言） |
| P1-2 | Web 层抗 DoS | ① 限流/锁定/会话表定时清理或容量上限；② 连接任务纳入统一跟踪，`StopAsync` 取消并 `WhenAll`；③ 登录密码长度上限 256；④ 会话响应加 `Cache-Control: no-store` | 新增用例：大量离散 IP 请求后字典条目数有界；`StopAsync` 返回时无在途连接；超长密码被 400 拒绝 |
| P1-3 | 数据层健壮性 | `Cache=SqliteMode.Shared` 改默认；`ImageEditService` 单流 `Identify`+`Load`；路径校验加实体解析（`ResolveLinkTarget`） | SQLite 并发写不再出现 `SQLITE_LOCKED`；编辑服务的 TOCTOU 用例通过 |
| P1-4 | 缩略图磁盘缓存 | 命中路径改用索引库 `file_size`/`modified_utc`（避免源文件 stat）；内存表设条目上限 | 机械盘上翻页时 IO 次数明显下降（计时对比） |

### P2 —— 季度规划

| # | 目标 | 说明 |
|---|---|---|
| P2-1 | 大图分级解码 | 补齐 1024/2048 中间档（对标 Immich 的多尺寸），移除对 8192 硬上限的依赖 |
| P2-2 | 依赖精简 | 评估 WASDK 精简元包，剔除 ONNX Runtime / DirectML（约 38 MB 且无功能用途） |
| P2-3 | 可访问性与国际化 | 建立 `AutomationProperties` 覆盖清单；中文硬编码抽到 `.resw` 资源 |
| P2-4 | 供应链持续化 | Dependabot + CycloneDX SBOM 产物 + 每次发版的许可变更评审 |

### 优先级决策矩阵

| 批次 | 主要收益 | 用户可感知 | 风险 | 依赖 |
|---|---|---|---|---|
| P0-1 依赖升级 | 安全 | 低（无功能变更） | 解码行为回归（有测试兜底） | 无 |
| P0-2 CI 门禁 | 供应链 | 低 | 无 | 无 |
| P0-3 文档 | 可信度 | 中（误判成本） | 无 | 无 |
| P0-4 许可 | 合规 | 低 | 无 | 无 |
| P1-1 索引与查询 | 性能 | 中（大库用户） | 迁移需谨慎（新索引只增不改） | 无 |
| P1-2 Web 抗 DoS | 安全/稳定 | 低 | 无 | 无 |

---

## 九、第八部分：复现命令与证据索引

```powershell
# 1. 依赖漏洞实测（本报告 2.1 的来源）
& "C:\Program Files\dotnet\dotnet.exe" list src\Pixbian\Pixbian.csproj package --vulnerable --include-transitive

# 2. CI 等价构建（本报告 2.2 的来源）
& "C:\Program Files\dotnet\dotnet.exe" build Pixbian.sln -c Release -warnaserror

# 3. 全量测试基线核对
& "C:\Program Files\dotnet\dotnet.exe" test Pixbian.sln -c Debug

# 4. 锁文件缺失核对
Get-ChildItem -Recurse -Filter packages.lock.json
```

| 关键证据 | 位置 |
|---|---|
| 漏洞依赖实测输出 | `SQLitePCLRaw.lib.e_sqlite3 2.1.6 High`、`SixLabors.ImageSharp 3.1.7 Moderate` |
| CI 构建实测输出 | `已成功生成。0 个警告 0 个错误` |
| README 与实现矛盾 | `README.md:37` vs `src/Pixbian/Pixbian.csproj:110` |
| 索引定义全量 | `src/Pixbian.Data/Sqlite/SchemaMigrations.cs:112-117,136-137,149-150,170-171,201,245` |
| 连接串 | `src/Pixbian.Data/Sqlite/SqliteDatabaseInitializer.cs:43-49` |
| 路径守卫 | `src/Pixbian.Core/Utilities/PathGuard.cs:24-86` |
| 对账内存 | `src/Pixbian.Core/Services/MediaIndexingService.cs:67-78,142-161` |
| 限流与鉴权 | `src/Pixbian.WebServer/Security/AuthService.cs:62,79,80,258,346,394-399,426` |
| 连接治理 | `src/Pixbian.WebServer/WebAccessServer.cs:82,94,301,307,318-325` |

---

## 十、结论

**Pixbian 的领域层与数据层安全基本功（全参数化 SQL、PathGuard 纵深防御、PBKDF2 + 恒定时间比较、ReDoS 四道防线）经本轮复核仍然**明显优于同类个人项目**，性能工程在缩略图与分页两处已达产品级水平；前轮 11 项"已闭环"中 9 项经代码核实属实。**

本轮把风险重心从"设计缺陷"移到了**两处低成本但被忽视的运维缺口**：

1. **供应链无人看管**——2 个已知漏洞依赖（含 1 个 High）可被局域网触发，且 CI 门禁因流程设计缺口**实测不会拦截**。这是本轮唯一的 High，也是修复成本最低的一项（改版本号 + 加一个 CI 步骤）。
2. **文档与实现反向漂移**——README 在 FFmpeg、运行时版本、测试规模三处与实现矛盾，其中"FFmpeg 未引入"直接影响体积、许可义务与攻击面的判断。

此外，数据层"查询-索引"匹配度（复合索引缺失、每页两次全表 COUNT、对账三份全量集合）是 10 万条量级的线性恶化点，应纳入下个迭代。

完成 P0 四项（约 2 人日）后，本仓可达到 **A−** 水准；P1 完成后在"本地面向个人的媒体浏览器"这一品类中，其工程质量将进入第一梯队。

---

**报告产出**：本轮为独立复审，未修改任何产品代码。前轮实施记录完整保留在 `docs/审计与对标报告-2026-09-10.md`。
